# 110584 전략 변경 및 실험 기록

작성일: 2026-08-25

## 목적

오리지널 `110584`는 빈 거점을 확보할 때 해당 거점이 최종 승패에 실제로
필요한지 충분히 구분하지 않고 확장을 시도하는 경향이 있었다. 특히 중앙과
승패에 별 영향이 없는 외곽 거점을 비슷하게 취급하면서 병력과 골드가
분산되는 문제를 확인했다.

이번 실험의 목적은 다음과 같다.

1. 중앙 거점 우선만으로 성능이 개선되는지 확인한다.
2. 각 거점이 최종 승패에 미치는 가치를 계산해 확장 대상을 거르는 로직을
   검증한다.
3. 동시 확장 수 제한과 미러 시뮬레이션이 추가 이득을 주는지 각각 분리해
   확인한다.

## 평가 조건

- 상대: `110584_original.exe`
- 맵: 공식 경기 로그에서 추출한 고유 맵 340개 중 seed 42로 고정 선택한
  동일한 100개
- 진영: 변경판을 A 진영에 고정
- 실행: 8개 worker를 이용한 병렬 시뮬레이션
- 최대 턴: 200턴
- 결정 경기 승률: `승 / (승 + 패)`
- 승점률: `(승 + 0.5 × 무) / 전체 경기`

모든 단계에서 맵과 상대를 동일하게 유지했다. 따라서 단계 사이의 결과
차이는 전략 변경에 의한 것으로 비교할 수 있다.

## 중앙 거점과 승패의 관계

별도로 생성한 100개 맵에서 중앙 로직 변경판과 오리지널을 대전시킨 결과는
다음과 같았다.

| 중앙 선점 결과 | 경기 수 | 승 | 무 | 패 | 전체 경기 기준 승률 |
|---|---:|---:|---:|---:|---:|
| 변경판이 중앙을 먼저 확보 | 60 | 29 | 23 | 8 | 48.3% |
| 변경판이 중앙을 먼저 확보하지 못함 | 40 | 0 | 21 | 19 | 0.0% |

이 표는 중앙 선점과 승리 사이에 강한 상관관계가 있음을 보여준다. 다만 중앙
선점 자체가 승리의 유일한 원인이라는 뜻은 아니다. 중앙을 먼저 먹을 정도의
경제·이동 우위가 이미 존재했을 가능성도 있으므로, 중앙 확보만 강제하는
전략은 별도 대조 실험으로 확인했다.

## 변경 사항별 누적 실험

강제 2인 중앙 오프닝은 모든 단계에서 제외했다. 공격 개시 조건도 오리지널
로직을 그대로 유지했다.

| 단계 | 누적 변경 | 승 | 무 | 패 | 결정 경기 승률 | 승점률 |
|---|---|---:|---:|---:|---:|---:|
| 1 | 중앙 거점 1인 우선 | 29 | 53 | 18 | 61.7% | 55.5% |
| 2 | 단계 1 + 거점 가치 필터 | 35 | 51 | 14 | **71.4%** | 60.5% |
| 3 | 단계 2 + 동시 신규 확장 최대 3개 | **38** | 46 | 16 | 70.4% | **61.0%** |
| 4 | 단계 3 + 미러 승리 시 후반 확장 억제 | 36 | 48 | 16 | 69.2% | 60.0% |

### 결과 해석

- 가장 큰 개선은 거점 가치 필터를 추가한 단계 2에서 발생했다.
  중앙 우선만 적용한 단계 1보다 6승이 늘고 4패가 줄었으며, 승점률은
  55.5%에서 60.5%로 5.0%p 상승했다.
- 동시 확장 3개 제한은 승수를 35승에서 38승으로 늘렸지만 패배도 14패에서
  16패로 증가했다. 승점률 개선은 0.5%p로 작으므로 추가 표본 검증이 필요하다.
- 미러 기반 후반 확장 억제는 승점률을 61.0%에서 60.0%로 낮췄다. 현재
  조건으로는 채택 근거가 없다.

## 1차 채택 후보: 거점 가치 필터

오리지널과의 자체 대전만 보면 **단계 2의 거점 가치 필터**가 가장 유망했다.
다만 아래 pool 교차 검증에서 일반화 성능 저하가 확인되어, 현재는 실험판에만
유지하고 실제 제출판 채택은 보류한다.

### 판단 방법

매 턴 현재 상태를 기준으로 다음 결과를 계산한다.

1. 양측이 추가 공격이나 확장을 하지 않고 본부 업그레이드와 경제 운영만
   계속했을 때의 기준 승패를 계산한다.
2. 빈 거점 `t`를 내가 확보했다고 가정한 상태의 승패를 계산한다.
3. 같은 거점 `t`를 상대가 확보했다고 가정한 상태의 승패를 계산한다.
4. 세 결과와 양측 예상 도착 시간을 비교해 실제 확장 후보를 선택한다.

### 후보로 유지하는 거점

- 두 본부 좌표의 중점에 가장 가까운 중앙 거점
- 내 본부에서 상대 본부보다 지나치게 멀지 않은 전선·자연 영토
- 양측 도착 시간이 비슷하고, 내가 먹으면 기준 승패가 개선되는 거점
- 상대가 먹었을 때 현재 결과가 패배로 떨어지는 것을 막아야 하는 거점
- 이전 턴부터 이미 `BUILD` 목적으로 병력이 이동 중인 확정 목표

상대 본부 쪽 후방 거점처럼 승패 기여가 작고 이동 비용이 큰 곳은 신규 확장
후보에서 제외한다. 중앙은 가장 높은 우선순위를 가지며, 승패 개선과 패배
방지 효과가 있는 후보에는 추가 우선순위를 준다.

## 채택하지 않은 변경

### 강제 2인 중앙 오프닝

첫 턴에 병력 2명을 무조건 중앙으로 보내는 방식은 제거했다. 중앙 경합 상황과
무관하게 본부 병력을 과도하게 비우고 초기 경제 및 방어 선택지를 줄였다.

### 중앙 경합 포기 조건

상대 예상 완공이 더 빠르거나 즉시 방어가 어렵다는 이유로 중앙을 바로
포기하는 실험판은 동일한 공식 맵 100개에서 24승 52무 24패, 결정 경기 승률
50.0%에 그쳤다. 따라서 이 조건은 가치 필터의 채택안에 포함하지 않는다.

### 미러 기반 후반 확장 억제

100턴 이후 정적 경제 계산과 미러 시뮬레이션이 3턴 연속 승리를 예측하면
신규 확장을 중단하는 조건이다. 단계 3보다 승점률이 1.0%p 낮아졌으므로
현재는 제외한다. 미러 시뮬레이션 자체는 분석·디버깅 도구로 남길 수 있지만,
현재 조건을 행동에 직접 반영하지 않는다.

## 변경하지 않은 영역

- 공격 개시 조건
- 공격 병력 산정과 공성 시뮬레이션
- 기존 방어 병력 산정과 위협 대응
- 본부 및 기존 거점 업그레이드 조건
- 병력 생산 제한과 유지비 계산

이번 결론은 거점 확보 대상의 선별에만 적용한다. 다른 행동 조건을 함께
바꾸면 거점 가치 필터 자체의 효과를 분리해서 판단할 수 없기 때문이다.

## Pool 교차 검증과 적 점거 거점 처리 수정

오리지널 상대 자체 대전만으로는 과적합 여부를 알 수 없어, pool의 각 모델과
공식 고유 맵 100개(seed 42)로 추가 검증했다. 특히 `87838`에서는 오리지널이
이기지만 초기 value filter가 지는 맵이 반복적으로 나타났다.

### Pool 전체 19모델 결과

각 상대에 동일한 공식 고유 맵 100개를 사용해 총 1,900판을 진행했다.

| 버전 | 승 | 무 | 패 | 전체 승률 | 결정 경기 승률 | 승점률 |
|---|---:|---:|---:|---:|---:|---:|
| 오리지널 | 801 | 1,040 | 59 | 42.16% | **93.14%** | **69.53%** |
| 초기 value filter | **902** | 798 | 200 | **47.47%** | 81.85% | 68.47% |
| 적 점거 전투비용 반영판 | 844 | 810 | 246 | 44.42% | 77.43% | 65.74% |

적 점거 전투비용 반영판은 초기 filter보다 58승이 줄고 46패가 늘어 승점률이
2.73%p 하락했다. 분류와 비용 계산은 현실에 가까워졌지만, 현재 행동 게이트는
승리 기회를 무승부·패배로 바꾸는 부작용이 더 크다.

| `87838` 상대 100맵 | 승 | 무 | 패 | 전체 승률 | 승점률 |
|---|---:|---:|---:|---:|---:|
| 오리지널 | 60 | 40 | 0 | 60.0% | **80.0%** |
| 초기 value filter | 46 | 40 | 14 | 46.0% | 66.0% |
| 적 점거 전투비용 반영판 | 38 | 49 | 13 | 38.0% | 62.5% |

초기 value filter의 14패 중 오리지널이 이겼던 8개 맵만 따로 재현했을 때는
다음과 같이 변했다.

| 버전 | 승 | 무 | 패 |
|---|---:|---:|---:|
| 오리지널 | 8 | 0 | 0 |
| 초기 value filter | 0 | 0 | 8 |
| 적 점거 전투비용 반영판 | 2 | 4 | 2 |

### 발견한 원인

초기 반사실 계산은 상대 병력이 이미 있는 무건물 거점에도 곧바로 아군 기지를
추가하고, `건설비 + 빌더 1명`만 비용으로 냈다. 실제 행동에서도 해당 위치를
빈 거점으로 취급해 `BUILD` 병력을 한 명씩 보냈다. 즉 상대 주둔군을 제거하는
전투비용과 전사자가 가치 계산에서 빠져 있었다.

### 수정한 처리

1. 상대 병력이 0명인 무건물 거점만 평화적 `BUILD` 후보로 분류한다.
2. 상대가 점거한 무건물 거점은 건설 로직이 아니라 공격 시뮬레이션으로
   보낸다.
3. 가용 잉여 병력과 추가 훈련 병력을 넣어 야전전을 계산하고, 전사자와
   생존 병력 HP를 반사실 상태에 반영한다.
4. 훈련비, 이동비, 건설비를 모두 차감한 뒤에도 정적 HQ 승패가 개선되거나
   상대 건설로 패배하는 상황을 막을 때만 실제 공격 후보로 유지한다.
5. 적을 제거한 뒤에만 다음 턴의 평화적 `BUILD` 로직이 작동한다.

이 수정은 잘못된 `BUILD` 파견을 없애고 8개 회귀맵 중 6개를 비패배로
회복했지만, `87838` 전체 100맵 승점률은 초기 filter보다도 낮았다. 따라서
분류 버그의 수정과 value filter의 채택 여부는 별개다. 적 점거 거점을 전투로
처리하는 구조는 맞지만, 현재 가치식은 승리를 무승부로 바꾸는 보수성이 커서
추가 조정이 필요하다.

## 거리 기반 필수 영토 회수 실험

value filter를 제거하고, 최단 hop 기준으로 `내 HQ → 거점` 거리가
`상대 HQ → 거점` 거리보다 짧거나 같은 거점을 필수 영토로 정의했다.
필수 영토를 상대가 점유하면 다른 빈 거점 확장을 멈추고 목표를 잠근 뒤,
적 건물이 있으면 공성전, 병력만 있으면 야전 시뮬레이션으로 필요 병력을
계산해 생산·집결·공격한다. 적을 제거한 뒤에는 신규 기지 비용 300골드를
남겨 즉시 건설할 때까지 잠금을 유지한다.

실험 파일은
`summission_result/submission/NEXT NATION/110584_territory_reclaim.cpp`이다.
오리지널과 기존 value filter 파일은 변경하지 않았다.

### 474 공식 패배 맵 재현

| 공식 맵 | 결과 | 관찰 |
|---|---|---|
| Pretest 29 `tc-1-474-vs-1558.log` | **166턴 승리** | R41을 68턴에 탈환·건설한 뒤 다른 필수 영토 회수를 계속함 |
| Pretest 30 `tc-1-474-vs-1558.log` | **93턴 패배** | R44에 4명을 동시 투입했지만 탈환하지 못하고 HQ가 파괴됨 |

### Pool 19모델 × 100판

각 행은 새 회수판을 A로 두고 100판씩 실행한 결과다. `--seed 42`는 첫 판에만
고정되고 나머지 99판은 실행기가 무작위 seed를 뽑으므로, 상대별 맵 목록이
완전히 동일한 paired test는 아니다.

| 상대 | 승 | 무 | 패 | 승점률 |
|---|---:|---:|---:|---:|
| `107981` | 6 | 26 | 68 | 19.0% |
| `108830` | 10 | 14 | 76 | 17.0% |
| `109203` | 22 | 47 | 31 | 45.5% |
| `110193` | 2 | 21 | 77 | 12.5% |
| `110496` | 7 | 23 | 70 | 18.5% |
| `110584` | 10 | 20 | 70 | 20.0% |
| `65205` | 92 | 5 | 3 | 94.5% |
| `65257` | 94 | 4 | 2 | 96.0% |
| `70784` | 37 | 45 | 18 | 59.5% |
| `70833` | 47 | 38 | 15 | 66.0% |
| `79541` | 56 | 31 | 13 | 71.5% |
| `83284` | 49 | 43 | 8 | 70.5% |
| `83304` | 49 | 41 | 10 | 69.5% |
| `84206` | 34 | 29 | 37 | 48.5% |
| `87838` | 23 | 31 | 46 | 38.5% |
| `91531` | 12 | 35 | 53 | 29.5% |
| `93978` | 10 | 34 | 56 | 27.0% |
| `96816` | 6 | 11 | 83 | 11.5% |
| `97302` | 7 | 28 | 65 | 21.0% |
| **합계** | **573** | **526** | **801** | **44.0%** |

약한 `65205`, `65257` 두 상대를 제외하면 승점률은 약 38.0%다. 따라서 이
버전은 현재 제출 후보로 채택하지 않는다.

### 핵심 결함: 공격 시뮬레이션의 시간축 부재

필수 영토 정의보다 먼저 고쳐야 할 부분은 공격 시뮬레이션이다. 현재 계산은
`현재 건물 HP + 현재 주둔군`에 선택한 아군 병력을 즉시 한꺼번에 투입한다.
다음 요소가 빠져 있어 실제 도착 시점의 전투력을 크게 과대평가한다.

1. 집결지까지 모이는 시간과 집결지에서 목표까지 이동하는 시간
2. 그 시간 동안 목표로 들어오는 상대 증원
3. 상대의 추가 병력 생산과 이동
4. 목표 건물의 업그레이드·수리
5. 이동 중인 아군의 서로 다른 도착 시점

Pretest 30에서는 T51에 4명이면 충분하다고 계산했지만, T52에 출발한 직후
같은 계산이 이미 추가 1명을 요구했다. 즉 전략 규칙이 아니라 입력한 전투
예측이 한 턴 뒤 상태조차 맞히지 못했다. 다음 실험은 필수 영토 규칙을 더
조정하기 전에 공격 시뮬레이션을 도착 ETA별 병력 wave와 목표 성장까지
포함하는 시간축 시뮬레이션으로 교체해야 한다.

## 474식 선발대 + 연속 증원 개선

474의 공식 대전 기록 6개에서 최초 적대 공격 34건을 역산했다. 공격 출발
시점의 건물과 주둔군만 고정해 전투를 계산하면 34건 중 33건(97.1%)은 후속
증원 없이도 이미 승리 가능한 병력이었다. 초반의 반복 패턴은 계산상 최소
3명이 필요한 목표에 4명을 동시에 출발시키고, 이후 생산되는 병력을 같은
목표에 한 명씩 계속 보내는 방식이었다.

이를 `110584_territory_reclaim.cpp`의 필수 영토 회수전에 다음처럼 반영했다.

1. 최초 공격은 현재 건물·현재 주둔군을 이기는 최소 병력에 1명을 더해
   계산한다.
2. 집결지로 이동 중인 병력은 전투에 이미 도착한 것으로 착각하지 않는다.
   전원이 실제 집결하고 전체 이동비를 낼 수 있을 때만 동시에 출발한다.
3. 선발대 출발 후에는 목표를 잠그고, 방어·노동 예약을 통과한 모든 잉여와
   매 턴 최소 1명의 생산 수요를 같은 목표의 후속 증원으로 보낸다.
4. 최초 선발대가 전멸하면 한 명씩 계속 던지지 않고 다시 `최소+1` 집결
   단계로 돌아간다.
5. 상대를 제거하면 전투 상태를 끝내고 기존 필수 영토 건설 단계로 넘어간다.

### 474 공식 로그 고정 대본 재생

474를 A의 공식 명령 대본으로 고정하고 개선판을 B로 실행했다. 개선판의
행동 때문에 원래 로그와 게임 상태가 갈라지는 반사실 재생이므로 실제
쌍방 라이브 승률은 아니지만, 해당 공격 패턴에 대응해 작전 상태가 정상적으로
전환되는지 확인하는 회귀 시험이다.

| 공식 기록 | 결과 | 종료 턴 | 선발대 출발 | 선발대 전멸 후 재집결 | 후속 증원 이동 |
|---|---:|---:|---:|---:|---:|
| Pretest #23 | 승 | 189 | 3회 | 0회 | 28회 |
| Pretest #25 | 승 | 150 | 2회 | 0회 | 6회 |
| Pretest #26 | 승 | 146 | 1회 | 0회 | 0회 |
| Pretest #27 | 승 | 146 | 0회 | 0회 | 0회 |
| Pretest #29 | 승 | 175 | 5회 | 1회 | 18회 |
| Pretest #30 | 승 | 191 | 2회 | 1회 | 4회 |

특히 기존 거리 기반 회수판이 Pretest #30에서 93턴에 패배했던 것과 달리,
이번 판은 첫 4명 선발대가 깨진 뒤 재집결해 두 번째 3명 선발대를 출발시키고
R44를 회수한 다음 191턴에 승리했다. Pretest #29도 승리를 유지했다. 따라서
대량 pool 검증 전 단계에서 의도한 `ASSEMBLE → REINFORCE → 실패 시 ASSEMBLE`
상태 전환과 474 대응은 확인되었다.

### Pool 19모델 × 공식 고유 맵 100개

후보를 LEFT에 두고, 오리지널 평가와 동일한 공식 고유 맵 100개(seed 42)를
pool의 19모델 각각에 적용해 총 1,900판을 실행했다.

| 버전 | 승 | 무 | 패 | 오류 | 전체 승률 | 결정 경기 승률 | 승점률 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 오리지널 | 801 | 1,040 | 59 | 0 | 42.16% | 93.14% | 69.53% |
| 필수 영토 회수 + 선발대/증원 | 607 | 573 | 720 | 0 | 31.95% | 45.74% | 47.03% |

가장 큰 승점률 하락은 `96816` -39.5%p, `87838` -35.5%p,
`110496` -34.5%p였다. 개선판이 오리지널과 동률을 유지한 상대는
`65205`, `65257`뿐이며 나머지 17모델에서는 모두 하락했다.

따라서 선발대/연속 증원 상태 머신은 의도대로 작동하지만, 이를 발동시키는
`내 HQ까지의 hop이 상대보다 짧거나 같은 모든 거점은 반드시 즉시 회수`라는
게이트를 현재 형태로 채택하면 안 된다. 474 고정 대본 6판의 승리는 특정
상대 대응 효과이고, 일반전에서는 경제·본부 업그레이드를 중단한 장기 회수전이
대량 패배를 만든다. 결과 원본은 `territory_reclaim_wave_vs_pool_100maps.csv`다.

### `87838` 패배 로그 원인

`87838` 상대 100맵에서 개선판은 29승 31무 40패였고, 이 40패는 모두 같은
맵에서 오리지널이 승리 또는 무승부였던 순수 회귀다. 패배 40판의 디버그
로그를 전부 다시 실행해 회수 상태를 집계했다.

| 지표 | 결과 |
|---|---:|
| 패배판 중 회수 모드 발동 | 40/40 |
| 첫 회수 발동 턴 중앙값 | 6턴 |
| 한 회수 목표에 잠긴 평균 시간 | 148.1턴 |
| 선발대 출발 | 평균 2.75회 |
| 선발대 전멸 | 평균 1.85회 |
| 후속 증원 이동 | 평균 12.82회 |
| HQ 업그레이드 0회 | 19/40 |

가장 빨리 진 Pretest #23 `tc-1-1558-vs-1409.log`에서는 R25가 6턴에 회수
목표로 잠겼다. 29턴 3명, 52턴 3명, 81턴 4명의 선발대가 모두 전멸했고,
104턴에 HQ가 파괴될 때까지 R25 잠금이 풀리지 않았다. 개선판은 104턴까지
HQ 업그레이드 0회, 거점 1개에서 0개, 병력 0명이 되었다. 같은 맵의
오리지널은 52턴에 HQ 2레벨, 50턴에 거점 5개를 확보했고 최종적으로
200턴 HQ 체력 25 대 20으로 승리했다.

직접 원인은 다음 네 조건의 결합이다.

1. 동거리 거점까지 전부 필수 영토로 잡아 상대가 6턴에 경계 거점 하나를
   먹는 것만으로 회수 모드가 너무 일찍 발동한다.
2. 회수 공세 분기가 매 턴 조기 `return`하여 집결 중에도 HQ/기지 업그레이드
   로직에 도달하지 못한다.
3. 전투 중 기지 건설비 300을 계속 예약하므로 초기 저수입 상태에서는 신병을
   매 턴 만들지 못하지만, 상대는 생산과 업그레이드를 계속한다.
4. `현재 수비 최소+1`은 출발 이후의 상대 생산·증원을 반영하지 않아 이동하는
   동안 낡은 계산이 되고, 실패해도 목표 포기 조건이 없어 같은 투자를
   반복한다.

따라서 이번 패배는 연속 증원 구현 자체보다 `즉시 강제 회수 + 무기한 잠금 +
공세 중 경제 중단`이 핵심 원인이다. 다음 수정에는 최소한 회수 발동 지연,
경제/업그레이드 병행, 작전 손실·시간 한도에 따른 포기 조건이 필요하다.

### 정정: 공격 중 건설비 예약 제거 + 공격 전용 예산

대표 로그를 턴 단위로 다시 확인한 결과, 장기 잠금보다 먼저 발생한 직접
실패 원인은 공격 모드에서도 신규 기지 건설비 300을 예약한 것이었다.
T29에 선발대 3명이 출발한 뒤 T30~T33에는 매 턴 증원 1명을 요구했지만,
보유 골드 349~411에서 300을 먼저 빼 `attackGold`가 훈련비 120보다 작아져
전부 `TRAIN 0`이 되었다. 첫 증원은 T34 생산, T35 출발로 너무 늦었고
T37 선발대가 전멸했다.

사용자 전략 정의에 맞춰 회수 공격 모드를 다음처럼 수정했다.

1. 공격 모드 진입 순간 그 턴 앞부분에서 계획한 건설, 업그레이드, 방어 이동,
   관련 예산 예약을 모두 취소한다.
2. 노동 인구만 남기고 나머지 유휴 병력을 공격에 동원한다.
3. 적이 살아 있는 동안 건설비 300을 예약하지 않는다.
4. 최초 돌격을 명령한 같은 턴부터 후속 신병 1명 생산을 시도한다.
5. 선발대 생존 중에는 매 턴 신병 1명을 최우선 생산하고, 등장 즉시 같은
   목표로 보낸다.
6. 적을 제거한 뒤에만 공격 모드를 종료하고 기지 건설비를 사용한다.

대표 Pretest #23 맵은 기존 104턴 패배에서 200턴 판정승으로 바뀌었다.
수정 후에는 T20에 4명이 출발했고, 남은 골드가 부족했던 T20~T21을 지나
골드가 121이 된 T22 즉시 신병을 생산하여 T23 바로 증원했다. 공격 전용
모드 14턴 동안 비공격 건설·업그레이드·방어 이동은 0건이었고, 적 제거 후
T37에만 R25 기지를 건설했다.

#### 수정 후 Pool 19모델 × 100맵

| 버전 | 승 | 무 | 패 | 오류 | 전체 승률 | 결정 경기 승률 | 승점률 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 오리지널 | 801 | 1,040 | 59 | 0 | 42.16% | 93.14% | 69.53% |
| 건설비를 예약하던 회수판 | 607 | 573 | 720 | 0 | 31.95% | 45.74% | 47.03% |
| **공격 전용 예산 회수판** | **1,057** | **666** | **177** | **0** | **55.63%** | 85.66% | **73.16%** |

공격 전용 예산판은 이전 회수판보다 450승이 늘고 543패가 줄었으며,
오리지널보다 승점률이 3.63%p 높다. 다만 오리지널보다 패배 수는 118판
많고 결정 경기 승률은 7.48%p 낮아, 더 공격적으로 승부를 내는 대신 안전한
무승부 일부를 패배로 바꾸는 특성이 있다. 결과 원본은
`territory_reclaim_attack_only_vs_pool_100maps.csv`다.

### 필수 영토·중앙 확보 후 상대 거점 연속 확장

필수 영토를 지키는 데 성공한 뒤에는 기존 회수 로직이 더 이상 작동하지
않고 일반 총공세 조건으로 돌아갔다. 이를 다음 조건부 확장으로 바꿨다.

1. `hop(내 HQ, 거점) <= hop(상대 HQ, 거점)`인 필수 거점에 아군 기지가
   모두 완공되어 있어야 한다.
2. 두 HQ 좌표의 중점에 가장 가까운 중앙 거점을 아군이 소유해야 한다.
3. 현재 유휴 병력과 남은 턴의 HQ 생산 한도를 넣은 정적 전투
   시뮬레이션에서 공략 가능한 상대 거점만 후보로 삼는다.
4. 후보 중 집결지에서 빨리 닿고, 준비 병력이 적게 필요한 거점을 잠근다.
5. 이후에는 필수 거점 탈환과 똑같이 `최소 필요 병력+1` 동시 출격,
   공격 전용 예산, 매 턴 한 명 연속 증원, 적 제거 후 기지 완공까지 수행한다.
6. 공세 중 필수 영토나 중앙을 잃으면 상대 진영 확장을 즉시 해제하고 필수
   영토 탈환으로 복귀한다.
7. 선발대 출발 전에 최신 시뮬레이션이 `공략 불가`로 바뀌면 선택 공세는
   포기한다. 또한 `extraToTrain==-1`을 준비 완료로 오인해 출격하던 기존
   비교 오류를 고쳐, 필요 생산량이 정확히 0일 때만 최초 돌격한다.

고정 Pretest #23 맵의 수정본 대 오리지널 검증에서는 T67에 조건이 충족되어
R31을 선택했고, T75 최초 돌격, T84 재돌격 뒤 T89에 아군 기지를 완공했다.
그 다음 턴인 T90에는 다음 상대 거점 R21을 선택했다. 공식 맵 10개 스모크
테스트는 1승 8무 1패, 실행 오류 0건이었다. 이 10판은 동작 확인용이며 승률
판단 자료로 사용하지 않는다.

#### 상대 거점 연속 확장판 Pool 19모델 × 100맵

| 버전 | 승 | 무 | 패 | 오류 | 전체 승률 | 결정 경기 승률 | 승점률 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 오리지널 | 801 | 1,040 | 59 | 0 | 42.16% | 93.14% | 69.53% |
| 공격 전용 예산 회수판 | 1,057 | 666 | 177 | 0 | 55.63% | 85.66% | 73.16% |
| **조건부 상대 거점 연속 확장판** | **553** | **786** | **561** | **0** | **29.11%** | **49.64%** | **49.79%** |

연속 확장판은 직전 회수판보다 504승이 줄고 384패가 늘어 승점률이
23.37%p 하락했다. 19개 상대 모두 승점률이 낮아졌으며, 특히 `83304`
상대는 72.5%에서 27.0%로 45.5%p 하락했다.

집계만으로 원인을 단정하지 않고, `83304` 상대에서 직전판 승리에서 현재판
패배로 바뀐 동일 맵 28개를 양 버전으로 다시 실행해 디버그 로그를 직접
비교했다.

- 28판 모두 선택한 상대 거점에 끝내 아군 기지를 건설하지 못했다.
- 13판은 병력을 모으기만 하다가 최초 돌격조차 시작하지 못했다.
- 19판은 선택 공세 도중 중앙 거점을 잃고 중앙 강제 탈환전으로 전환했다.
- 26판에서는 `RECLAIM_ATTACK_ONLY`가 같은 턴에 계산된 방어 이동을 실제
  명령에서 취소했다.
- 최초 선택 공세가 유지된 기간의 중앙값은 41.5턴이었다.

대표 Pretest #16 `battle-136473`에서는 T41에 R33 선택 공세가 발동했다.
T47~T54에 중앙 R31로 보내려던 방어 이동이 매 턴 계산됐지만 공격 전용
초기화가 모두 취소했고, T54 중앙을 빼앗긴 뒤 T55부터 R31 강제 탈환전에
들어가 160턴에 HQ가 파괴됐다. 직전판은 같은 맵에서 중앙 증원을 실제로
실행하고 HQ를 64턴에 2레벨로 올린 뒤 195턴에 상대 HQ를 파괴했다.

따라서 직접 원인은 단순히 다음 목표를 계속 고르는 것이 아니다. 상대의
미래 생산·증원을 무시한 정적 시뮬레이션이 선택 공세를 공략 가능하다고
오판하고, 필수 탈환용 `공격 전용 예산/행동 취소`를 선택 공세에도 그대로
적용하면서 중앙 방어와 경제 성장을 동시에 중단한 것이 핵심이다. 결과
원본은 `territory_push_vs_pool_100maps.csv`, 재현 로그는
`analysis_push_vs83304_all28/`에 저장했다.

#### 선택 공세 중 방어 허용

기존 `110584_territory_push.cpp`는 보존하고
`110584_territory_push_defense.cpp`를 새로 만들었다. 필수 영토 강제 탈환은
기존의 완전 공격 전용 행동을 유지하지만, 상대 진영으로 들어가는 선택
공세에서는 다음을 다르게 처리한다.

1. 같은 턴 앞부분에서 계산된 `WPurpose::MOVE` 방어 이동을 취소하지 않는다.
2. 각 거점의 `need` 방어 예약 병력은 공격 후보에서 제외한다.
3. 방어 결원과 전체 병력 HP 열세에 따른 생산 수요를 공격 생산 수요와 함께
   반영한다.
4. 건설·업그레이드·일반 확장은 선택 공세 동안 계속 중단한다.

대표 Pretest #16 `battle-136473`은 160턴 HQ 파괴 패배에서 200턴 무승부로
바뀌었고 중앙 R31도 유지했다. `83304` 100맵에서는 8승 38무 54패,
승점률 27.0%에서 7승 56무 37패, 승점률 35.0%로 개선됐다.

| 버전 | 승 | 무 | 패 | 오류 | 전체 승률 | 결정 경기 승률 | 승점률 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 방어 없는 연속 확장판 | 553 | 786 | 561 | 0 | 29.11% | 49.64% | 49.79% |
| **방어 허용 연속 확장판** | **575** | **966** | **359** | **0** | **30.26%** | **61.56%** | **55.68%** |
| 직전 필수 영토 회수판 | 1,057 | 666 | 177 | 0 | 55.63% | 85.66% | 73.16% |

방어 허용으로 패배가 202판 줄고 승점률이 5.89%p 올랐으므로 원인 진단과
수정 방향은 맞았다. 그러나 직전 회수판보다 승점률이 여전히 17.48%p 낮다.
선택 공세가 실제로 상대 거점을 먹지 못하는 정적 시뮬레이션 오판과, 공세
중 HQ/거점 업그레이드를 계속 취소하는 경제 중단이 남은 차이의 핵심
후보다. 결과 원본은 `territory_push_defense_vs_pool_100maps.csv`다.

#### 병력 분류 일관화 + 공세 종료 조건

기존 파일은 보존하고 `110584_territory_push_coherent.cpp`를 새로 만들었다.
`83304` 상대에서 직전 회수판은 이겼지만 방어 허용 공세판은 진 15개 맵을
재현한 결과, 15판 모두 200턴 HQ HP 판정패였으며 새 공세판의 HQ는 한 번도
업그레이드되지 않아 최대 HP가 10에 머물렀다. 선택 공세는 경기당 125~160턴
유지됐고, 9판은 선택한 공격 거점을 하나도 확보하지 못했다.

대표 맵에서는 T40 목표 선택기가 4명이면 공격 가능하다고 판단했지만 실제
실행 단계는 방어 예약을 다시 적용해 가용 병력을 0명으로 계산했다. 집결
병력도 도착 후 `STATIONARY`가 되면 공격 소속을 잃고 방어 병력으로 다시
예약됐다. 한 명이라도 이동 중이면 출격하지 않는 조건까지 겹쳐 최초 목표는
T169까지 잠겼고, 그 사이 상대 증원으로 추가 필요 병력이 31명까지 증가했다.

수정 내용은 다음과 같다.

1. 목표 선택과 실제 실행 모두 동일한 `need` 방어 예약량을 사용한다.
2. 공세에 배정한 병력 번호를 작전 상태에 저장해 집결지 도착 후에도 공격
   소속을 유지한다.
3. 현재 집결지 병력만으로 전투 시뮬레이션을 다시 통과하면 다른 증원이 이동
   중이어도 즉시 출격한다. 이동 중 병력은 후속 증원으로 처리한다.
4. 선택 공세가 20턴 안에 출발하지 못하면 취소하고 15턴 휴식한다.
5. 선봉 전멸은 한 번만 재시도하고 두 번째 실패에는 공세를 취소한다.
6. 목표 완공 뒤에도 15턴 동안 다음 선택 공세를 금지한다.
7. 공세 진입 전에 가능하다고 계산된 HQ 업그레이드는 취소하지 않고, 그
   비용을 공격 이동·훈련 예산보다 먼저 차감한다.

문제를 만들었던 동일 15맵 재검증은 기존 `0승 0무 15패`에서
`8승 7무 0패`로 바뀌었다. 15판 모두 HQ가 실제로 업그레이드됐으며 HP 10
판정패가 사라졌다. 같은 공식 맵 100개에서 `83304` 상대 결과는
`61승 31무 8패`, 승점률 76.5%였다. 직전 회수판의 72.5%보다 4.0%p 높고,
방어 허용 공세판의 35.0%보다 41.5%p 높다.

| 버전 | 승 | 무 | 패 | 오류 | 승점률 |
|---|---:|---:|---:|---:|---:|
| 직전 필수 영토 회수판 | 1,057 | 666 | 177 | 0 | 73.16% |
| 방어 허용 연속 확장판 | 575 | 966 | 359 | 0 | 55.68% |
| **병력 분류 일관화 공세판** | **1,159** | **586** | **155** | **0** | **76.42%** |

pool 19모델 × 공식 100맵에서는 직전 회수판보다 102승이 늘고 22패가 줄어
승점률이 3.26%p 상승했다. 19개 상대 중 15개에서 유지 또는 개선됐지만,
`87838`은 74.5%에서 69.5%로 5.0%p, `109203`은 69.0%에서 67.5%로
1.5%p 하락했다. 전체 결과는 `coherent_push_vs_pool_100maps.csv`, 문제
15맵의 재현 로그는 `analysis_push_coherent_vs83304_regressions/`에 저장했다.

#### 필수 탈환과 선택 공세의 상태 기계 분리

`87838` 퇴행 맵을 비교하니 새로 생긴 실제 패배 5판은 모두 선택 공세가
한 번도 발동하지 않았고, 필수 거점 `RECLAIM`에 선택 공세용 병력 커밋과
집결지 재시뮬레이션을 적용한 것이 원인이었다. 따라서 기존 파일을 보존하고
`110584_territory_push_scoped.cpp`를 새로 만들었다.

- 필수 거점 탈환은 직전 회수 기준판의 병력 분류·출격 조건을 사용한다.
- 병력 ID 고정, 집결지 현재 전력 재시뮬레이션, 이동 중 증원 허용,
  20턴 제한·실패 제한·휴식은 상대 거점 선택 공세 `PUSH`에만 적용한다.
- 선택 공세 중 방어 이동과 가능한 HQ 업그레이드 보존은 유지한다.

직전판에서 새로 패배한 `87838` 5맵은 `4승 1무 0패`로 복구됐다. 같은
공식 100맵 전체에서는 `61승 28무 11패`, 승점률 75.0%로 직전 coherent의
69.5%보다 5.5%p, 공세 전 기준판의 74.5%보다 0.5%p 높았다.

| 버전 | 승 | 무 | 패 | 오류 | 승점률 |
|---|---:|---:|---:|---:|---:|
| 공세 전 기준판 | 1,057 | 666 | 177 | 0 | 73.16% |
| 병력 분류 일관화 공세판 | 1,159 | 586 | 155 | 0 | 76.42% |
| **필수 탈환·선택 공세 분리판** | **1,167** | **533** | **200** | **0** | **75.45%** |

pool 전체에서는 직전 coherent보다 승점률이 0.97%p 낮고 패배가 45판
늘었다. 반면 `87838`은 5.5%p 개선됐다. 따라서 필수 탈환의 새 상태 기계가
무조건 나빴던 것이 아니라 상대에 따라 공격성을 높여 유리하게 작동한 경우도
있었고, `87838`에서는 장기 잠금으로 치명적이었다. 결과 원본은
`scoped_push_vs_pool_100maps.csv`다.

scoped의 다른 상대 퇴행 원인을 추가로 확인하기 위해 손실 증가가 컸던
`110584`, `110496`, `70784`, `83304`, `96816`, `83284`의 동일 100맵을
두 버전으로 다시 맞췄다. coherent에서는 승리 또는 무승부였지만 scoped에서
패배한 경우는 44건이었다. 이 44건을 모두 재실행한 조건부 통계는 다음과
같다.

| 지표 | coherent | scoped | 변화 |
|---|---:|---:|---:|
| 경기당 필수 탈환 상태 턴 | 64.7 | 134.9 | +70.2 |
| T150 이후 필수 탈환 상태 턴 | 11.8 | 44.8 | +33.0 |
| T199에도 필수 탈환 중 | 11/44 | 36/44 | +25경기 |
| 경기당 선택 공세 상태 턴 | 52.4 | 13.9 | -38.5 |
| 최종 HQ HP | 20.45 | 11.02 | -9.43 |
| 최종 아군 거점 수 | 9.50 | 4.00 | -5.50 |
| 최종 아군 병력 수 | 38.89 | 20.50 | -18.39 |

원인은 선택 공세가 아니라 필수 탈환 실행 방식이다. scoped는 필수 탈환
병력의 ID 커밋을 사용하지 않으므로 집결지에 도착한 병력이 다시 일자리·방어
인원으로 계산되고 `best_help` 재배치에 빠질 수 있다. 또한 필수 탈환의
출격은 계산된 모든 이동 병력이 집결하고 추가 생산 필요량이 0이 될 때까지
기다린다. coherent는 현재 집결한 병력만으로 최신 수비를 이기는지 다시
시뮬레이션해 가능하면 먼저 출격한다. scoped가 기다리는 동안 상대가 목표를
증원하면 필요 병력이 다시 늘고, 집결 병력이 재배치되면서 대기가 반복된다.

대표적으로 `83304` 회귀 맵에서는 coherent가 R60을 14턴, R32를 14턴 동안
탈환한 뒤 T81부터 선택 공세로 넘어가 최종 거점 `13:2`, HQ `25:20`으로
이겼다. scoped는 R32에 66턴, R60에 81턴 묶여 T199에도 R60 탈환 중이었고
최종 거점 `5:11`, HQ `15:20`으로 졌다. 전체 재현 요약은
`analysis_scoped_vs_coherent_regressions/all_new_losses_summary.csv`에 있다.

#### 필수 탈환 적응형 재시도 하이브리드

coherent의 높은 전체 성능과 scoped의 `87838` 대응력을 함께 남기기 위해
기존 파일을 보존하고 `110584_territory_push_hybrid.cpp`를 새로 만들었다.

- 첫 필수 거점 공세는 coherent처럼 작전 병력 ID를 고정하고, 집결지의 현재
  전력으로 다시 계산해 이길 수 있으면 바로 출격한다.
- 실제 선발대 전멸이 관측된 뒤에만 재시도 모드로 전환한다. 이때 죽은
  선발대의 ID는 즉시 버리고 현재 살아 있는 공격 병력으로 계획을 다시 세운다.
- 재시도는 계산된 병력과 이동 중 증원이 집결한 뒤 동시 출격한다. 12턴
  동안 집결이 끝나지 않으면 목표는 포기하지 않고 작전 병력 소속만 최신
  상태로 재동기화한다.
- 실패 횟수 때문에 필수 거점을 공격 후보에서 제외하지 않는다. 선택 공세의
  실패 제한과 휴식 규칙도 그대로 유지한다.

scoped에서 새로 패배했던 44맵을 다시 실행하니 하이브리드는 `19승 22무
3패`로, 41판을 비패배로 복구했다. 동일 공식 100맵 × pool 19모델 결과는
다음과 같다.

| 버전 | 승 | 무 | 패 | 오류 | 승점률 |
|---|---:|---:|---:|---:|---:|
| 병력 분류 일관화 공세판(coherent) | 1,159 | 586 | 155 | 0 | 76.42% |
| 필수 탈환·선택 공세 분리판(scoped) | 1,167 | 533 | 200 | 0 | 75.45% |
| **적응형 재시도 하이브리드** | **1,188** | **547** | **165** | **0** | **76.92%** |

하이브리드는 coherent보다 29승이 늘고 10패가 늘었지만 무승부가 39판
줄어, 최종 승점률은 0.50%p 높아졌다. `87838`은 `53승 33무 14패`에서
`56승 32무 12패`로 2.5%p 개선됐다. 상대별 coherent 대비 최대 하락은
`93978`의 -1.0%p였고, 나머지 하락 상대는 -0.5%p였다. 결과 원본은
`hybrid_push_vs_pool_100maps.csv`다.

추가로 직전 실패 파동의 인원수를 다음 재시도의 최소 인원으로 강제하는
`110584_territory_push_retry_floor.cpp`도 시험했다. 그러나 수비가 이미
약해진 상황에서도 과거의 큰 파동을 다시 모으느라 출격이 늦어졌다. 선별한
87838 재현 5맵 결과가 하이브리드의 `3승 1무 1패`에서 `2승 2무 1패`로
하락했고, 남아 있던 회귀 3맵도 복구하지 못했다. 따라서 이 변형은 pool
대량전을 진행하지 않고 기각했다.

#### 필수 탈환 선발대 승계·연속 증원

`91531` 패배 로그를 다시 추적하니, 필수 거점 공격의 실패 판정이 실제
공세 종료보다 빨랐다. 최초 선발대 ID 3명이 전멸한 시점에도 같은 목표로
보낸 후속 증원 5명이 전투 중이었지만, 상태 기계는 선발대 ID만 검사해
`ASSEMBLE`로 돌아갔다. 이후 방어 재배치 명령은 내부 `idle` 후보에서는
병력을 제거하면서 필수 탈환 분기에서 실제 명령만 취소해, 후속 병력이
공격에도 방어에도 쓰이지 않는 유령 재배치도 발생했다.

기존 최고 파일을 보존하고
`110584_territory_push_continuous_reinforce.cpp`를 별도로 만들었다.

- 필수 탈환에서 최초 선발대가 전멸해도 살아 있는 동일 작전의 `ATTACK`
  병력을 새 선발대로 승계해 `REINFORCE`를 유지한다.
- 동일 작전 병력이 정말 0명이 되었을 때만 실패 파동으로 판정하고
  재집결한다. 필수 목표 자체는 포기하거나 공격 후보에서 제외하지 않는다.
- 필수 탈환 중에도 이미 계산된 기존 아군 거점 방어 이동은 실제 명령으로
  보존한다. 새 거점 확장·건설은 계속 중단한다.
- 이번 턴 골드로 이미 가능한 HQ 업그레이드는 보존한다. 아직 살 수 없는
  미래 업그레이드 비용을 미리 예약하지는 않는다.
- 연속 증원을 선택 공세까지 적용하면 이기고 있던 판에서도 업그레이드와
  다음 목표 전환을 막았다. 따라서 무제한 승계는 필수 탈환에만 적용하고,
  선택 공세는 기존의 2회 실패 후 중단·휴식 규칙을 유지한다.

대표 `91531` 패배 맵
`Pretest #16/battle-135514/tc-1-1365-vs-1558.log`는 하이브리드에서
200턴 HQ `10:25` 패배였다. 필수 탈환 연속 증원만 넣고 방어를 취소한
중간 실험은 상대의 본부 직공을 허용해 180턴에 HQ가 파괴됐다. 방어 이동을
실제로 보존한 최종판은 200턴 HQ `25:20` 승리로 뒤집혔다.

처음에는 선발대 승계를 선택 공세에도 적용했고, `91531` 동일 100맵에서
`38승 35무 27패`로 하락했다. 범위를 필수 탈환으로 제한한 최종판은 기존
하이브리드의 `46승 34무 20패`에서 `54승 30무 16패`로 개선됐다.

동일 공식 100맵 × pool 19모델 결과는 다음과 같다.

| 버전 | 승 | 무 | 패 | 오류 | 승점률 |
|---|---:|---:|---:|---:|---:|
| 적응형 재시도 하이브리드 | 1,188 | 547 | 165 | 0 | 76.92% |
| **필수 탈환 연속 증원·방어 병행판** | **1,216** | **544** | **140** | **0** | **78.32%** |

전체 승점률은 1.40%p 상승했고 패배는 25판 줄었다. 상대별 최대 개선은
`91531`의 +6.0%p이며, `83304` +5.5%p, `83284` +4.0%p,
`93978` +3.5%p였다. 최대 하락은 `108830`과 `110584`의 -2.0%p다.
결과 원본은 `mandatory_continuous_vs_pool_100maps.csv`, `91531` 단독
결과는 `mandatory_continuous_vs_91531_100_games.csv`다.

#### 반응 가능한 상대 예비대 전부를 포함한 출격 게이트

필수 탈환의 최초 출격이 목표에 현재 서 있는 적만 보고 상대 증원을
과소평가하는 문제를 확인했다. 기존 최고 파일은 보존하고
`110584_territory_push_projected_defense.cpp`를 별도로 만들어, 돌격 이동과
초반 전투 4턴 안에 목표로 합류할 수 있는 현재 상대 병력을 모두 수비군으로
간주했다. 실제 목표가 비공개인 이동 병력은 직전 위치보다 목표에 가까워진
경우만 포함했고, 정지 병력은 다음 턴 반응 지연 1턴을 더했다.

대표 `Pretest #15/battle-126678/tc-1-423-vs-1558.log`에서는 T11의 중앙
R51 주둔군 1명과 반응 가능 예비대 3명을 함께 계산해 4명 돌격을 취소했다.
결과는 T145 HQ 파괴 패배에서 200턴 HQ `10:10` 무승부로 개선됐다.
하지만 양측이 중앙을 두고 병력만 생산해 한 번도 출격하지 않는 과보수
대치가 발생했다.

동일 공식 100맵 × pool 19모델 결과는 `425승 617무 858패`, 승점률
`38.61%`였다. 현재 최고 연속 증원판의 `1,216승 544무 140패`, `78.32%`
보다 39.71%p 낮다. 최대 하락은 `96816`의 -68.5%p였고 모든 상대에서
하락했다. 현재 존재하는 예비대를 전부 처음부터 수비군으로 합치는 방식은
도착 시점과 기회비용을 잃어버려 지나치게 보수적이므로 기각한다. 다음
시도는 병력을 한꺼번에 합치지 않고 양측 도착 턴별로 전투 시뮬레이션에
투입해야 한다. 결과 원본은 `projected_defense_vs_pool_100maps.csv`다.

#### 후보별 최종 승점 차이 `ATTACK(t) - HOLD` 실험

기존 파일은 보존하고 `110584_territory_push_delta.cpp`를 새로 만들었다.
필수 영토 탈환은 건드리지 않고, 필수 영토와 중앙을 모두 확보한 뒤의 선택
공세 목표에만 다음 반사실 비교를 적용했다.

- `HOLD`: 현재 배치를 고정하고 양측이 HQ 업그레이드만 했을 때의 200턴
  공식 승점(패 0, 무 0.5, 승 1).
- `TAKE(t)`: 우리만 후보 `t`에 병력을 보내는 국지 롤아웃.
- `CONTEST(t)`: 양측이 후보 `t`에 파병·추가 생산하는 국지 롤아웃.
- `ATTACK(t)`: `TAKE`와 `CONTEST` 중 낮은 최종 승점.
- `ATTACK(t) - HOLD > 0`이고 두 공격 시나리오 모두 실제 점령에 성공한
  후보만 허용한다. 여러 후보가 남으면 승점 차이, 준비 턴, 필요 병력,
  실제 거리 순으로 고른다.

국지 롤아웃에는 기존 병력 파견, HQ 추가 생산, 이동 턴, 매일 전투, 건물
파괴 후 건설, 수입과 유지비를 반영하고, 국지전이 끝난 상태를 다시 남은
턴의 HQ 경쟁 계산기에 넘겼다.

과거 `87838` 대표 회귀 맵에서 T52의 후보들은 모두 `HOLD=무`,
`TAKE=무`, `CONTEST=무`로 평가되어 공격을 거부했다. 실제 결과는 scoped의
HQ `20:20` 무승부에서 `30:30` 무승부로 바뀌어 불필요한 지출은 막았지만
공식 승점은 개선하지 못했다.

pool 19모델 × 동일 공식 100맵 결과는 다음과 같다.

| 버전 | 승 | 무 | 패 | 오류 | 승점률 |
|---|---:|---:|---:|---:|---:|
| 필수 탈환·선택 공세 분리판 | 1,167 | 533 | 200 | 0 | 75.45% |
| **최종 승점 차이 필터판** | **1,045** | **662** | **193** | **0** | **72.42%** |

패배는 7판 줄었지만 승리도 122판 줄어 승점률이 3.03%p 하락했다. 특히
`87838`은 75.0%에서 64.5%로 10.5%p 하락했다. 동일 100맵 전환은
`W→D 23`, `W→L 2`인 반면 개선은 `D→W 5`, `L→W 1`뿐이었다. 즉 이
기준은 위험한 공격뿐 아니라 승리를 만드는 동률 승점 공격도 대부분 막는
과보수 필터다. 따라서 현재 채택하지 않고 실험 파일로만 보존한다. 결과
원본은 `delta_push_vs_pool_100maps.csv`와
`delta_push_vs_87838_100_games.csv`다.

#### 거점 시간축 전투 + 동시 기지 건설/업그레이드 예측 실험

기존 최고 파일은 보존하고 `110584_territory_push_timeline_combat.cpp`를
새로 만들었다. 필수 거점 탈환에 한해 정적 `garrison` 계산을 다음 시간축
계산으로 교체했다.

- 아군 집결대, 이미 이동 중인 병력, 상대 수비대와 예비대를 실제 목표 도착
  턴에만 전투에 합류시킨다.
- 상대 이동 방향이 목표의 최단 경로와 일치할 때만 이동 중 예비대로 센다.
- 양측 모두 현재 추정 골드와 순수입으로 생산 가능한 후속 병력을 본부부터
  이동시킨다.
- 상대 병력만 빈 거점에 있고 추정 골드가 300 이상이면, 명령 동시성 때문에
  그 턴 전투 전에 L1 기지가 건설된다고 예측해 건물 HP와 포탑을 반영한다.
- 이미 적 기지가 있고 업그레이드 조건을 만족하면 한 단계 상승한 HP와 포탑,
  비용을 반영한다.
- 임의의 `최소 선발대 3명` 같은 숫자 하한은 두지 않는다. 필요한 병력은 위
  상태를 넣은 전투 결과로만 정한다.

`87838` 회귀 맵 `Pretest #16/battle-136473/tc-1-418-vs-1558.log`에서는
T13에 `RECLAIM_TIMELINE_FORTIFY predicted=BUILD R31`을 기록했다. 동시 건설을
빼먹은 초기 시간축판은 한 명을 즉시 보냈지만, 요새화 예측판은 L1 포탑까지
계산해 3명을 모은 뒤 T17에 출격했다. 결과는 HQ 파괴 패배에서 HQ `20:20`
무승부로 개선됐다. `91531` 회귀 맵은 HQ 파괴 패배에서 HQ `25:20` 승리로
회복됐다.

pool 19모델 × 공식 100맵 전체 결과는 다음과 같았다.

| 버전 | 승 | 무 | 패 | 승점률 |
|---|---:|---:|---:|---:|
| 현재 최고 연속 증원판 | 1,216 | 544 | 140 | 78.32% |
| 시간축 + 동시 기지 건설/업그레이드 예측판 | 1,046 | 700 | 154 | 73.47% |

대표 조기 돌격 패배 맵은 HQ 파괴 패배에서 무승부가 됐지만, 전체적으로는
승리가 무승부로 바뀌는 과보수 성향이 남아 승점률이 4.85%p 하락했다.
상대별 최대 하락은 `110496 -14.0%p`, `110193 -12.0%p`,
`110584 -11.0%p`, `108830 -10.5%p`, `87838 -10.0%p`였다. `70784`,
`70833`, `83284`는 동일했고, `65205`, `65257`만 각각 +1.0%p였다. 따라서
현재 최고본을 대체하지 않고 실험판으로 보존한다. 결과 원본은
`timeline_combat_fortify_vs_pool_100maps.csv`다.

#### 시간축 시뮬레이션 대 실제 상대 대응 정확도

시간축판의 전략은 바꾸지 않고 예측 로그만 확장한 뒤, pool 19모델 각각
공식 맵 100개(총 1,900판)를 다시 실행했다. 매 턴의 가상 계획 전체를 독립
표본으로 세지 않고, 실제로 `launch=0` 판정을 받아 출격한 필수 탈환 공세
3,064건만 실제 프레임과 대조했다. 따라서 아래의 `공격 성공률`은 이
시뮬레이터가 성공이라고 판정해 실행한 공세의 정밀도이며, 공격하지 않은
상황까지 포함한 분류 정확도나 재현율은 아니다.

| 측정 항목 | 결과 |
|---|---:|
| 결국 목표를 제거/점령 | 2,742 / 3,064 = 89.49% |
| 예측 점령 턴 안에 제거/점령 | 2,470 / 3,064 = 80.61% |
| 성공 공세의 점령 턴 정확히 일치 | 53.28% |
| 성공 공세의 점령 턴 ±1 이내 | 62.58% |
| 성공 공세의 점령 턴 ±2 이내 | 70.20% |
| 성공 공세 점령 턴 평균 절대 오차 | 3.36턴 |
| 점령 예정 시점까지 수비대 수 정확히 일치 | 60.18% |
| 수비대 수 ±1 이내 | 70.33% |
| 수비대 수 평균 절대 오차 | 2.22명 |
| 첫 수비 증원 유무/ETA 정확히 일치 | 69.61% |
| 양쪽 모두 증원을 예측/관측한 경우 ETA 평균 오차 | 0.57턴 |
| 동시 건설/업그레이드 예측 적중 | 1,272 / 8,923 = 14.26% |

공격 성공 공세 중 1,461건은 점령 턴까지 정확했고, 1,009건은 예측보다
빨랐으며, 272건은 예측보다 늦었다. 실패는 322건이었다. 성공한 경우만
보면 실제 점령이 예측보다 평균 1.31턴 빨랐다.

가장 큰 불일치는 상대 정책 가정이다. 시뮬레이션은 점령 예정 시점 수비대를
평균 4.06명으로 계산했지만 실제로 목표에 있었거나 도착한 고유 수비대는
평균 2.97명이었다. 수비대 수를 과대 추정한 공세가 27.28%, 과소 추정한
공세가 12.53%로, 평균 1.10명 과대 추정했다. 첫 증원도 `올 것`이라고 했지만
안 온 경우가 475건, `안 올 것`이라고 했지만 실제 온 경우가 225건이었다.

동시 기지 건설/업그레이드 예측은 더 명확하다. 상대가 그 턴 비용을 낼 수 있으면 반드시
건설/업그레이드한다고 가정했지만, 8,923번 중 실제 행동한 경우는 1,272번뿐
이었다. 행동한 경우 BUILD/UPGRADE 종류는 모두 맞았지만, 7,651번(85.74%)은
상대가 돈이 있어도 요새화하지 않았다. 이 가정이 일부 조기 돌격을 막은
대신 전체적으로 과도한 집결과 승리 지연을 만드는 주원인이다.

연속 두 턴에 같은 목표를 계산했을 때 출격 카운트다운이 정확히 1 줄어든
비율도 49.59%, 점령 카운트다운은 37.67%뿐이었다. 새 관측이 들어올 때마다
예측이 크게 흔들린다는 뜻이다. 결론적으로 현재 시간축 시뮬레이션은
`출격하면 결국 먹는가`는 약 89.5%로 쓸 만하지만, `상대가 무엇을 언제
보낼지`와 `정확히 언제 먹는지`를 결정하는 정책 모델은 아직 행동 게이트로
쓰기에는 부정확하고 과보수적이다.

#### 관측 기반 상대 대응과 첫 반응 실험

상대가 우리 집결 목표를 미리 알고 전 맵의 잉여 병력과 미래 생산을 전부
보낸다는 가정을 제거하기 위해 기존 시간축판을 보존하고 두 파일을 새로
만들었다.

- `110584_territory_push_causal_response.cpp`: 현재 목표 주둔군과 실제 이동
  방향이 목표 쪽으로 관측된 병력만 수비 ETA에 포함한다. 미래 상대 생산은
  제외한다. 상대의 같은 턴 기지 건설/업그레이드는 미래 계획마다 확정하지
  않고, 오늘 즉시 출격할 때만 동시 명령 위험 분기로 검사한다.
- `110584_territory_push_causal_first_response.cpp`: 위 조건에 더해 공격대가
  실제 목표에 도착해 목표가 확정된 다음 턴, 거리·골드상 가장 빨리 가능한
  첫 수비 반응 한 개만 포함한다. 그 이후 행동은 미리 올인시키지 않고 다음
  턴 관측으로 넘긴다.

동일 공식 맵 20개 × pool 19모델, 후보는 왼쪽 고정으로 선별 검증했다.

| 버전 | 승 | 무 | 패 | 승점률 | 실제 출격 공세 성공 | 예상 수비대 편향 | 수비대 MAE |
|---|---:|---:|---:|---:|---:|---:|---:|
| 기존 시간축판 | 203 | 139 | 38 | 71.71% | 91.09% | +1.35명 | 2.54명 |
| 관측만 반영 | 216 | 107 | 57 | 70.92% | 84.64% | -1.08명 | 1.34명 |
| 관측 + 첫 반응 | 215 | 110 | 55 | 71.05% | 85.41% | -0.76명 | 1.34명 |

오늘 출격할 때만 같은 턴 기지 건설/업그레이드를 검사하자 그 행동 예측
적중률은 기존 15.88%에서 관측판 35.47%, 첫 반응판 36.53%로 상승했다.
수비대 숫자의 절대 오차도 크게 줄었지만, 미래 대응을 전부 제거하면 실제
수비대를 과소평가해 패배가 늘었다. 첫 반응 하나를 넣으면 편향은 조금
줄었지만 승점률은 기존 시간축판보다 0.66%p 낮았다. 따라서 두 파일 모두
현실성 교정 실험으로 보존하되 현재 채택 후보를 대체하지 않는다. 100맵
대량 검증도 아직 진행하지 않는다.

#### 일반 공격 목표의 방어 도착 여유 실험

필수 거점 탈환에서 미래 수비대를 최소 공격 인원에 직접 포함하는 방식은
과보수적이었다. 따라서 현재 최고판을 보존하고
`110584_territory_push_response_slack.cpp`를 새로 만들어, 일반 공격의
선발대 계산은 그대로 둔 채 목표 선택에만 다음 값을 추가했다.

`방어 도착 여유 = 현재 수비대 예상 점령 턴 - 보이는 첫 대응 병력 도착 턴`

초기 구현은 미래 신병 한 명도 즉시 방어한다고 가정하고, 방어가 점령보다
일찍 도착하는 턴 수를 전부 페널티로 더했다. 동일 20맵 선별전에서 241승
121무 18패, 79.34%로 기준판의 80.00%보다 낮았다. 다시 미래 생산을 빼고,
현재 목표 쪽으로 이미 이동 중인 병력과 노동 인구를 제외한 정지 예비대만
계산했다. 방어가 점령 전 또는 같은 턴에 도착할 때도 목표 점수에 1점만
더해 불확실한 상대 대응을 확정 수비대로 취급하지 않았다.

| 버전 | 맵/상대 | 승 | 무 | 패 | 승점률 |
|---|---:|---:|---:|---:|---:|
| 연속 증원 기준판 | 100 × 19 | 1216 | 544 | 140 | 78.32% |
| 보이는 대응 1점 페널티 | 100 × 19 | 1222 | 538 | 140 | **78.47%** |

패배 수는 그대로이고 무승부 6개가 승리로 바뀌었다. 상대별로는 `83284`,
`83304`, `110496`에서 주로 개선됐고 `70784`, `70833`, `109203`,
`110584`에서는 각각 소폭 하락했다. 전체 차이는 0.15%p로 작으므로 큰
개선으로 단정하지 않지만, 미래 방어를 병력 수에 강제로 넣지 않고 목표
위험도의 약한 보조 점수로만 쓰는 방향은 기준판의 성능을 유지했다.

#### 매 턴 연속 증원 재판단

`110584_territory_push_adaptive_reinforce.cpp`를 새로 만들었다. 기존에는
선발대가 출발하고 살아 있는 동안 현재 승산을 다시 계산하지 않고 모든
잉여 병력과 매 턴 신병 한 명을 목표에 계속 보냈다. 새 판은 매 턴 다음
전력을 실제 도착일에 합류시켜 현재 증원 정책으로 점령 가능한지 검사한다.

- 목표에서 교전 중인 아군
- 이미 이동 중인 작전 병력
- 이번 턴 파견 가능한 잉여
- 현재 골드·순수입·새 병력 유지비로 생산 가능한 아군 1명/턴
- 현재 목표 수비대와 목표 방향 이동이 실제로 관측된 상대 병력

처음에는 상대 방어 반응이 한 번 관측되면 남은 게임 내내 상대도 매 턴 한
명을 생산한다고 계산했다. 오리지널 상대 패배 한 판은 145턴 본부 파괴에서
200턴 무승부로 바뀌었지만, pool 20맵에서는 225승 131무 24패, 76.45%로
퇴행했다. 실제로 한 번 반응했다는 사실은 영구 올인을 뜻하지 않기 때문이다.

최종판은 목표 방향 이동 또는 목표 수비대 증가를 실제 방어 시작 신호로
사용하되, 실측한 탐지 후 3턴 내 출발 병력 평균 2.92명에 맞춰 상대 미래
생산을 다음 3턴까지만 계산한다. 이후 행동은 다음 턴의 새 관측으로 다시
판단한다. 계속 보내도 점령 불가면 선택 공세는 중단하고, 필수 탈환은 목표를
유지한 채 직접 증원을 멈추고 동기화 재집결로 돌아간다.

| 버전 | 맵/상대 | 승 | 무 | 패 | 승점률 |
|---|---:|---:|---:|---:|---:|
| 방어 도착 여유판 | 100 × 19 | 1222 | 538 | 140 | 78.47% |
| 적 반응 후 영구 방어 생산 | 20 × 19 | 225 | 131 | 24 | 76.45% |
| 적 반응 후 3턴 관측 창 | 100 × 19 | **1247** | **531** | **122** | **79.61%** |

3턴 관측 창은 직전 최고보다 승리 25판이 늘고 패배 18판이 줄어 1.14%p
상승했다. `91531`에서는 2.5%p, `83304`에서는 1.0%p 하락했지만,
`70784`, `70833`에서 각각 3.5%p, `108830`, `110193`에서 각각 3.0%p
개선됐다.

#### 전선 붕괴 뒤 증원 도착 순서 게이트

Pretest #15 `battle-126678/tc-1-423-vs-1558.log`의 원본 상대 패배를
재생했을 때, 기존 적응형 증원 시뮬레이션은 20턴에 현재 교전 병력이 다음
턴 전멸하고 상대 증원이 아군 증원보다 먼저 도착하는데도 지원을 계속했다.
원인은 공격 병력이 0명이 된 기간을 실패로 취급하지 않고, 남은 게임 동안
생산할 아군을 같은 공격의 누적 피해로 계산한 것이었다. 그래서 실제로는
전선이 끊겼는데도 `CAPTURE day=33` 같은 먼 미래 점령 결과가 나왔다.

기존 최고본을 보존하고 `110584_territory_push_continuity_gate.cpp`를 새로
만들었다. 전투가 시작된 뒤 아군 전선이 전멸하면 다음 아군·상대 증원의
실제 도착일을 비교하고, 상대가 먼저 또는 같은 날 도착하면 주도권 상실로
판정해 직접 증원을 멈추고 재집결한다. 동일 맵에서는 17턴 계산이
`collapse_day=6, next_atk=8, next_def=7`을 찾아냈고, 문제의 20턴에는 목표
R51로 추가 이동을 내지 않았다. 최종 결과도 기존 HQ 10:15 패배에서
200턴 HQ 10:10 무승부로 바뀌었다.

다만 같은 공식 맵 20개 × pool 19모델 선별전에서는 기존 최고본이
247승 116무 17패(80.26%)였던 데 비해 새 게이트는 242승 119무
19패(79.34%)로 0.92%p 하락했다. 특정 오판은 고쳤지만 모든 전선 단절을
즉시 재집결 신호로 쓰면 다소 과보수적이므로 현재 최고본으로 승격하지
않는다. 결과 원본은 `continuity_gate_vs_pool_20maps.csv`, 재현 리플레이는
`replay_continuity_gate_vs_original_pretest15.html`이다.

#### 오리지널 중앙 2인 고정·반응형 오프닝

순수 오리지널을 기준으로 두 대조군을 별도 파일로 만들었다.

- `110584_original_center2.cpp`: 시작 병력 두 명을 좌표상 중앙 거점으로
  직행시킨다. 최종 목표는 한 번만 명령하고 엔진의 자동 이동에 맡기며,
  경로상의 거점을 목적지로 오인해 건설하지 않게 한다.
- `110584_original_center2_reactive.cpp`: 첫 턴에는 두 명을 중앙 방향의 바로
  다음 지역까지만 보낸다. 다음 턴 상대 첫 이동 중 중앙 최단경로와 일치하는
  병력이 2명 이상이면 강제를 해제하고, 0~1명이면 두 명 모두 중앙 직행을
  확정한다. 직행판보다 추가 이동비는 두 명 합계 20골드다.

고정 맵에서 상대가 중앙으로 2명을 보낸 경우 반응형은 1턴에
`CENTER2_RELEASE`를 내고 두 병력을 오리지널 판단으로 되돌렸다. 상대가
1명만 보낸 경우에는 `CENTER2_COMMIT` 뒤 2대1 전투로 그 병력을 제거했다.

공식 고유 맵 100개(seed 42) × pool 19모델, 후보 왼쪽 고정 1,900판 결과는
다음과 같다.

| 버전 | 승 | 무 | 패 | 결정 경기 승률 | 승점률 |
|---|---:|---:|---:|---:|---:|
| 순수 오리지널 | 801 | 1,040 | 59 | 93.14% | **69.53%** |
| 중앙 2인 고정 | 750 | 919 | 231 | 76.45% | 63.66% |
| 중앙 2인 반응형 | 714 | 953 | 233 | 75.40% | 62.66% |

반응형은 오리지널보다 `108830`, `110193`, `110496`, `110584`에는
9.0~12.5%p 강해졌지만, `79541`, `83284`, `70784`, `70833`, `83304`,
`109203`에서 21.0~25.5%p 하락했다. 중앙 1인 오프닝을 잡는 국소 효과는
확인됐지만, 첫 두 병력과 초기 생산·건설 순서가 고정되면서 일반 상대에
대한 패배가 너무 많이 늘었다. 반응 해제도 고정 2인판보다 승점률을
1.00%p 더 낮췄으므로 두 버전 모두 비채택한다. 원본 결과는
`original_center2_fixed_vs_pool_100maps.csv`와
`original_center2_reactive_vs_pool_100maps.csv`다.

위 반응형은 요구한 분기를 반대로 구현한 대조군이었다. 실제 의도는 상대가
중앙으로 1명 이상 보내면 내 두 명을 모두 중앙에 유지하고, 상대 시작 병력이
중앙을 비우고 외곽으로 분산하면 한 명만 중앙에 남기고 다른 한 명을 최속
비중앙 거점으로 보내는 것이다. 이를
`110584_original_center2_split_response.cpp`로 다시 분리했다. 상대 최종
목적지는 보이지 않으므로 첫 두 턴은 한 칸씩 전진하며 경로를 확인한다.
고정 Pretest #15 맵에서 외곽 분산 상대에게 A1→중앙 R51, A2→비중앙 R22가
선택됐고, 중앙 1인 상대에게는 A1·A2가 모두 R51로 향했다. 두 표적 검증은
각각 HQ 30:25 승리였다.

그러나 동일한 공식 고유 맵 100개 × pool 19모델 1,900판에서는 466승
772무 662패, 결정 경기 승률 41.31%, 승점률 44.84%에 그쳤다. 순수
오리지널보다 24.69%p, 중앙 2인 고정판보다도 18.82%p 낮다. 특히
`91531`, `87838`, `93978`, `96816`, `107981`, `97302`에서 오리지널보다
34.0~46.0%p 하락했다. 외곽 분산 시 중간 위치에서 고른 최속 거점이
오리지널의 본부 기준 확장 순서와 크게 달라지고, 두 턴 탐색 비용·지연까지
겹친 결과다. 따라서 이 분기 역시 비채택한다. 전체 결과는
`original_center2_split_response_vs_pool_100maps.csv`다.

## BUILD 목적 범위·교전 증원·완공 턴 우선 실험

2026-08-27에 Pretest #14의 1281 및 217 맵 패배 리플레이를 기준으로
건설 예약, 탈환전 증원 경로, 필수 빈 거점 우선순위를 점검했다.

### BUILD 목적이 경유지 건설로 해제되는 버그

Pretest #14 `battle-122747/tc-1-1281-vs-1558.log`에서 A1은 T0에 R38
건설을 목표로 출발했지만, T2에 경유지 R2에서 다른 기지를 지을 때
`BUILD` 목적이 풀렸다. 건설 후 해제 조건이 해당 지역에 있는 모든
`BUILD` 병력을 대상으로 했고, 각 병력의 실제 `target`을 검사하지 않은 것이
원인이었다.

그 결과 A1이 T5에 R38에 도착한 뒤에도 건설비 300 골드가
`reserved_build`에 잡히지 않았다. T11에 본부의 A2를 이동시킨 10 골드 때문에
R38 기지 건설이 반사실 T13에서 실제 T14로 한 턴 밀렸다. 건설 후
`w.target == region`인 병력의 목적만 해제하도록 수정했다.

| 1281 맵/100맵 검증 | 수정 전 | BUILD 목적 범위 수정 후 |
|---|---:|---:|
| 1281 맵 | HQ 10:20 패배 | HQ 15:15 무승부 |
| `110584` 상대 공식 100맵 | 50승 37무 13패 | 53승 34무 13패 |

### 교전이 남아 있을 때 탈환전 증원 직행

Pretest #14 `battle-118863/tc-1-217-vs-1558.log`에서 R21의 적 기지가
파괴된 직후 잠깐 빈 땅으로 보이자 `REINFORCE` 상태가 풀렸다. 이후
교전이 재개된 T35에 계획은 `fixed=2, send=1`로 추가 한 명을 요구했지만,
A9는 R21로 직행하지 않고 집결지 R25로 향했다. 기존 A8과 B7은
동수라 상호 전사했고, A9가 늦게 합류하며 점령을 내줬다.

상대의 미래 증원을 예측해 상태를 유지하는 방식은 해당 맵을 HQ 20:20
무승부로 바꾸고 `110584` 상대 100맵에서 51승 37무 12패를 기록했지만,
불확실한 상대 행동 예측에 의존하므로 채택하지 않았다. 필수 탈환 목표의
기지가 완공될 때까지 무조건 상태를 유지하는 방식도 같은 맵은 무승부로
바꾸었으나 100맵 45승 40무 15패로 하락해 비채택했다.

현재 소스는 미래 예측 대신 목표에 아군과 적군이 현재 동시에 있을 때만
`activeReclaimBattle`로 분류한다. 이때 계획이 요구한 추가 병력은 재집결시키지
않고 현재 교전 목표로 직행한다. 목표가 정리되어 한쪽 병력이 0이 되면
정상적으로 `REINFORCE`를 풀어 오래된 작전에 계속 과투입하는 것을 막는다.

### 필수 빈 거점의 예상 완공 턴 우선

기존 `findHqDispatchTarget` 주석은 예상 완공 턴을 먼저 비교한다고
설명했지만, 실제 비교식은 중앙축 직선과의 거리 `axisD2`를 완공 턴
`myTurn`보다 먼저 비교했다. 따라서 중앙축에 가까운 먼 거점이 더 빨리
완공할 수 있는 거점보다 먼저 선택될 수 있었다.

비교 순서를 `예상 완공 턴 → 중앙축 거리 → 전선 거리 → 실제 경로 거리`로
바꿔, 완공 턴이 더 느려지지 않는 동률 후보에서만 기존 중앙축 조건을
타이브레이커로 사용했다.

| 검증 | 결과 |
|---|---:|
| Pretest #14 1281 맵 | HQ 20:20 무승부 |
| Pretest #14 217 맵 | 151턴 승리 |
| `110584` 상대 공식 100맵 | 74승 22무 4패, 승점률 85.00% |
| pool 19모델 × 공식 100맵 | 1,203승 568무 129패, 승점률 78.26% |

완공 턴 우선판은 `110584` 자체 대전에서는 크게 개선됐지만, pool 승점률은
기존 최고인 `territory_push_adaptive_reinforce`의 79.61%(1,247승 531무
122패)보다 1.34%p 낮다. 따라서 현재 최고 기록을 교체한 것으로 보지
않고, 우선순위 규칙을 검증하는 별도 실험판으로 유지한다.

### 기존 최고판과 상대별 paired 비교

`adaptive_reinforce_lookahead3_vs_pool_100maps.csv`의 기존 최고판과
`completion_first_vs_full_pool_100maps.csv`의 완공 턴 우선판은 같은 공식
고유 맵 100개(seed 42), 같은 LEFT 진영, 같은 pool 19모델을 사용했다.
따라서 아래 증감은 상대별로 직접 비교할 수 있는 paired 결과다. 증감은
`완공 턴 우선판 - 기존 최고판`으로 계산했다.

| 상대 | 기존 최고 승/무/패 | 기존 승점률 | 완공 턴 우선 승/무/패 | 현재 승점률 | 승점률 증감 |
|---|---:|---:|---:|---:|---:|
| `83284` | 75/21/4 | 85.5% | 55/39/6 | 74.5% | **-11.0%p** |
| `79541` | 75/22/3 | 86.0% | 65/27/8 | 78.5% | **-7.5%p** |
| `83304` | 68/28/4 | 82.0% | 58/35/7 | 75.5% | **-6.5%p** |
| `110496` | 67/28/5 | 81.0% | 58/33/9 | 74.5% | **-6.5%p** |
| `110193` | 63/32/5 | 79.0% | 59/33/8 | 75.5% | -3.5%p |
| `87838` | 58/31/11 | 73.5% | 55/33/12 | 71.5% | -2.0%p |
| `70784` | 76/21/3 | 86.5% | 74/22/4 | 85.0% | -1.5%p |
| `70833` | 76/21/3 | 86.5% | 74/22/4 | 85.0% | -1.5%p |
| `93978` | 46/44/10 | 68.0% | 46/41/13 | 66.5% | -1.5%p |
| `97302` | 56/35/9 | 73.5% | 51/42/7 | 72.0% | -1.5%p |
| `107981` | 56/35/9 | 73.5% | 51/42/7 | 72.0% | -1.5%p |
| `108830` | 59/35/6 | 76.5% | 58/34/8 | 75.0% | -1.5%p |
| `84206` | 57/32/11 | 73.0% | 54/38/8 | 73.0% | 0.0%p |
| `65205` | 99/0/1 | 99.0% | 100/0/0 | 100.0% | +1.0%p |
| `65257` | 99/0/1 | 99.0% | 100/0/0 | 100.0% | +1.0%p |
| `96816` | 61/31/8 | 76.5% | 63/31/6 | 78.5% | +2.0%p |
| `91531` | 48/37/15 | 66.5% | 52/36/12 | 70.0% | **+3.5%p** |
| `109203` | 46/47/7 | 69.5% | 56/38/6 | 75.0% | **+5.5%p** |
| `110584` | 62/31/7 | 77.5% | 74/22/4 | 85.0% | **+7.5%p** |

완공 턴 우선판은 6모델에서 개선, 1모델에서 동일, 12모델에서
악화됐다. 전체로는 44승이 줄고 무승부가 37판, 패배가 7판 늘어
승점률이 79.61%에서 78.26%로 1.34%p 하락했다. 가장 큰 회귀는
`83284`(-11.0%p), `79541`(-7.5%p), `83304`·`110496`(-6.5%p)에서
나타났다. 반면 `110584`(+7.5%p), `109203`(+5.5%p), `91531`(+3.5%p)
상대로는 명확히 개선됐다. 즉 완공 턴 우선은 오리지널 계열 대응에는
강하지만 `83284`/`79541` 계열에서 기존 중앙축 우선이 제공하던 이점을
크게 잃는 패턴이다.

### 원점 최근접 한 곳만 필수 거점으로 지정

기존에는 `내 HQ→거점 hop <= 상대 HQ→거점 hop`인 내 쪽 여러
거점을 모두 필수 영토로 분류했다. 이를 좌표 `(0,0)`과의 제곱거리가
가장 작은 거점 딱 하나만 필수 영토로 분류하도록 바꿘다. 거리 동률은
지역 번호가 작은 거점을 택한다.

첫 구현은 `mandatoryTerritory`만 하나로 줄였는데, 이 배열이 강제 탈환
목표와 평시 빈 거점 확장 목록을 동시에 겸하고 있었다. 그 결과 중앙
하나를 지은 뒤 다른 빈 자연 영토를 모두 무시하고 즉시 상대 거점
선택 공세로 넘어갔다. `110584` 상대 100맵 결과가 0승 3무 97패로
붕괴한 것으로 이 숨은 결합을 확인했다.

최종 구현은 두 목록을 분리했다.

1. `mandatoryTerritory`: `(0,0)` 최근접 거점 한 곳만 포함하며,
   빼앗겼을 때 강제 탈환할지를 판단한다.
2. `expansionTerritory`: 기존 HQ hop 기준의 내 자연 영토를 유지하며,
   주인 없는 빈 거점에 대한 평시 건설 후보로만 사용한다.

또한 평화적으로 확장할 수 있는 자연 영토가 남아 있으면 선택 공세를
시작하지 않게 했다. 적이 이미 점거한 곳은 평화 확장으로 보지 않아
선택 공세 후보로 남겨둔다.

| 검증 | 완공 턴 우선판 | 원점 한 곳만 필수 탈환 |
|---|---:|---:|
| Pretest #14 1281 맵 | HQ 20:20 무승부 | HQ 25:25 무승부 |
| Pretest #14 217 맵 | 151턴 승리 | 151턴 승리 |
| `110584` 상대 공식 100맵 | 74승 22무 4패, 85.0% | 51승 41무 8패, 71.5% |

요청한 필수 거점 정의와 평시 확장을 분리하여 동작 오류는 해소했지만,
`110584` 상대 승점률은 직전판보다 13.5%p 낮아졌다. pool 19모델
전체 검증은 아직 수행하지 않았다.

### 남은 턴 내 불가능한 HQ 업그레이드 예약 해제

기존에는 HQ 레벨이 상대보다 낮거나 내 순수입이 더 많으면,
남은 턴에 실제로 업그레이드 비용을 모을 수 있는지와 무관하게 병력
생산과 기지 업그레이드를 멈추고 HQ 저축을 계속했다. 다음 낙관적
상한을 계산해도 비용에 도달하지 못하면 예약을 해제하도록 바꿘다.

`HQ 저축 상한 = 현재 사용 가능 골드 + max(0, 현재 순수입) × 남은 턴`

이번 턴에 HQ 업그레이드 명령을 이미 냈다면 예약 판정에서는 업그레이드
후 레벨을 현재 레벨로 보고 그 다음 비용을 비교한다. 상한이 비용보다
작으면 `hqUpgradeReachableInTime=false`로 두어 다음 두 잠금을 풀었다.

1. HQ 레벨 열세 때문에 병력 생산을 0으로 만드는 `hqBehind` 잠금
2. HQ 저축을 위해 기지 업그레이드를 넘기는 `waitForHqUpgrade` 잠금

`83284` 상대 Pretest #17 865 맵에서 T194의 L4→L5 비용은
3,600 골드였지만, 현재 골드와 남은 6턴 순수입을 모두 더한 상한이
3,599였다. T194~T199에 `HQ_RESERVE_RELEASE`가 기록되어 의도한 경계값
판정은 확인했다. 다만 이 맵에서는 남은 턴이 너무 적어 기지 업그레이드
수익성 규칙도 거절했고, 추가 병력 수요도 0이어서 실제 명령과 HQ 25:25
무승부 결과는 변하지 않았다.

| 100맵 회귀 검증 | 예약 해제 전 | 예약 해제 후 |
|---|---:|---:|
| `83284` | 56승 39무 5패 | 56승 39무 5패 |
| `110584` | 51승 41무 8패 | 51승 41무 8패 |

즉 예약 해제 조건은 정상적으로 발동하며 기존 200판의 승무패를 바꾸지
않았다. 예약을 풀어도 남은 골드를 소비할 후속 행동 수요가 없을 수 있다는
것은 별개의 문제로 남아 있다.

### 남은 턴 내 파괴 가능한 상대 HQ 직접 공세

`83284` 상대 공식 100맵의 39무승부를 전수 검사하니, 최종 동원 가능
병력 HP가 상대보다 100 이상 높은데도 HQ를 파괴하지 못한 판이 10개였다.
대표 Pretest #25 / 1414 맵은 200턴에 다음 상태로 끝났다.

| 항목 | 수정 전 최종 상태 |
|---|---:|
| HQ HP | 25:25 무승부 |
| 전사 수 | 43:6 |
| 동원 병력 HP | 244:36 |
| 거점 수 | 11:0 |
| 골드 | 3,537:68 |

T175에는 내 41명 전원이 종료 전 상대 HQ에 도착할 수 있었고, T190에도
43명 전원이 남은 턴 안에 도착 가능했다. 그런데 직접 HQ 목표는
`내 HQ 만렙 + 전체 거점 70% 이상 + 병력 HP 우위`일 때만 선택됐고,
이 목표 선정 자체도 `수입 열세 또는 HQ 레벨 우위`라는 일반 공세 게이트
안에 있었다. 평시 목표 검색은 HQ를 명시적으로 제외한다. 따라서 압도적
우위와 높은 수입이 오히려 HQ 공격 검토를 막는 구조였으며, 문제는 전투
시뮬레이터보다 그 앞의 이중 게이트였다.

다음과 같이 수정했다.

1. 내 HQ 만렙 조건을 제거하고 거점 70% 이상·동원 병력 HP 우위만 1차
   안전 조건으로 유지한다.
2. 실제 실행과 같은 최근접 아군 집결지를 고르고, 각 거점의 방어 수요와
   내 HQ 노동 인구를 남긴 현재 유휴 병력만 후보로 잡는다.
3. 기존 HQ 결전 안전 마진을 포함해 현재 병력만으로 HQ를 파괴할 수 있는지
   계산한다. 미래 생산은 성공 판정에 포함하지 않는다.
4. 후보 병력의 집결 턴, 집결지→HQ 이동 턴, 현재 HQ 건물·포탑·주둔군과의
   일별 전투를 순서대로 계산해 마지막 플레이 가능 턴까지 파괴되는 경우만
   `DIRECT_HQ_FEASIBLE`로 인정한다. 실제 이동비도 현재 골드로 지불 가능해야
   한다.
5. 이 확정 파괴 판정은 수입 기반 일반 공세 게이트를 우회한다. 한 번
   `DIRECT_HQ_LOCK`이 걸리면 상대 HQ가 남아 있는 동안 목표를 유지한다.
   이미 HQ로 이동 중인 병력은 후속 턴 계획의 고정 전력으로 승계하고,
   첫 파동에 반영한 안전 마진을 반복 적용해 불필요하게 추가 훈련하지
   않는다.

대표 맵에서는 T181에 `staging=R56 force=18 capture_day=3`으로 직접
공세가 승인됐고, 18명이 즉시 R72 상대 HQ로 출격해 T184에 파괴했다.
수정 전 200턴 25:25 무승부가 184턴 승리로 바뀌었다.

| 공식 고유 맵 100개, seed 42, LEFT, 16 workers | 수정 전 | 수정 후 | 승점률 변화 |
|---|---:|---:|---:|
| 대 `83284` | 56승 39무 5패, 75.5% | **60승 35무 5패, 77.5%** | **+2.0%p** |
| 대 `110584` | 51승 41무 8패, 71.5% | **51승 41무 8패, 71.5%** | 0.0%p |

`83284` 상대 순수 승률은 56%에서 60%로 4%p 상승했고 패배 수는 늘지
않았다. 원본 상대 100맵도 완전히 동일하므로, 확인된 압도적 우위 무승부를
승리로 회수하면서 이 두 회귀 세트에서는 손해가 없었다.

#### pool 19모델 전체 검증

현재 누적 수정본을 pool 19모델 각각에 공식 고유 맵 100개(seed 42),
LEFT 진영, 16 workers로 총 1,900판 검증했다. 이 현재본에는 직접 HQ
공세뿐 아니라 앞선 원점 최근접 한 곳만 필수 탈환, HQ 예약 해제 변경도
함께 들어 있다. 따라서 아래 직전 전체 풀 판과의 차이를 직접 HQ 변경
하나의 인과 효과로 해석하면 안 되고, 현재 누적 파일의 실제 성능으로
봐야 한다.

| 상대 | 현재 승/무/패 | 현재 승점률 | 완공 턴 우선판 대비 | 기존 최고 대비 |
|---|---:|---:|---:|---:|
| `65205` | 100/0/0 | 100.0% | 0.0%p | +1.0%p |
| `65257` | 99/1/0 | 99.5% | -0.5%p | +0.5%p |
| `70784` | 70/21/9 | 80.5% | -4.5%p | -6.0%p |
| `70833` | 70/21/9 | 80.5% | -4.5%p | -6.0%p |
| `79541` | 66/27/7 | 79.5% | +1.0%p | -6.5%p |
| `83284` | 60/35/5 | 77.5% | **+3.0%p** | -8.0%p |
| `83304` | 60/38/2 | 79.0% | **+3.5%p** | -3.0%p |
| `84206` | 54/34/12 | 71.0% | -2.0%p | -2.0%p |
| `87838` | 48/43/9 | 69.5% | -2.0%p | -4.0%p |
| `91531` | 46/42/12 | 67.0% | -3.0%p | +0.5%p |
| `93978` | 31/40/29 | 51.0% | **-15.5%p** | **-17.0%p** |
| `96816` | 46/33/21 | 62.5% | **-16.0%p** | **-14.0%p** |
| `97302` | 42/41/17 | 62.5% | -9.5%p | -11.0%p |
| `107981` | 42/41/17 | 62.5% | -9.5%p | -11.0%p |
| `108830` | 46/39/15 | 65.5% | -9.5%p | -11.0%p |
| `109203` | 45/40/15 | 65.0% | -10.0%p | -4.5%p |
| `110193` | 45/40/15 | 65.0% | -10.5%p | -14.0%p |
| `110496` | 45/42/13 | 66.0% | -8.5%p | -15.0%p |
| `110584` | 51/41/8 | 71.5% | **-13.5%p** | -6.0%p |

| 전체 1,900판 | 승/무/패 | 승점률 | 현재본 대비 증감 |
|---|---:|---:|---:|
| 기존 최고 `adaptive_reinforce` | 1247/531/122 | **79.61%** | +7.21%p |
| 완공 턴 우선판 | 1203/568/129 | **78.26%** | +5.87%p |
| 현재 누적 수정본 | **1066/619/215** | **72.39%** | 기준 |

완공 턴 우선판과 비교하면 개선 3모델(`79541`, `83284`, `83304`), 동일
1모델(`65205`), 악화 15모델이다. 특히 `96816`(-16.0%p),
`93978`(-15.5%p), `110584`(-13.5%p) 회귀가 크다. 직접 HQ 수정이 목표로
삼았던 `83284`에서는 개선이 재현됐지만, 현재 누적 파일 전체는 풀 기준으로
채택할 수 없는 수준이다.

### 기존 최고판에 HQ 직접 공세만 이식

누적 실험판의 다른 변경을 버리고 기존 최고
`110584_territory_push_adaptive_reinforce.cpp`에 HQ 직접 공세만 분리해
이식했다. 처음에는 앞선 실험과 똑같이 현재 HQ 주둔군만 상대로 남은 턴 내
파괴 가능성을 검사했다. 대표 Pretest #25 / 1414는 T141 HQ 파괴 승리로
바뀌었지만 전체 pool은 1226승 552무 122패, 79.05%로 기존 최고
79.61%보다 0.56%p 낮았다. `110193`, `110496`, `108830` 등에서 너무 이른
HQ 직행이 원래 승리를 무승부로 바꿨다.

회귀 대표 Pretest #15 / 188을 확인하니 T156에 현재 주둔군만 상대로
`force=14 capture_day=12`가 나와 결전을 잠갔지만, 공격대가 집결·이동하는
동안 상대 HQ가 계속 수비병을 생산해 실제로는 파괴하지 못했다. 이를 다음과
같이 보정했다.

1. HQ 만렙과 수입 열세 조건은 계속 제거하고, 거점 70% 이상·동원 병력 HP
   우위 조건만 1차 안전장치로 둔다.
2. 내 현재 유휴 병력만 공격 후보로 사용하며 미래 아군 생산은 성공 판정에
   포함하지 않는다.
3. 상대 HQ의 현재 골드, 현재 순수입, HQ 레벨별 훈련 슬롯으로 공격대가
   집결·이동·공성하는 매 턴 생산 가능한 최대 수비병을 HQ 도착 전력에
   넣는다. HQ는 수비병 생산지이자 공격 목표이므로 별도 이동비/이동시간은
   없다.
4. 현재 후보를 한 명씩 늘려 이 최대 수비 생산까지 포함한 시간축 전투에서
   마지막 플레이 가능 턴 안에 HQ가 파괴되는 최소 인원을 찾는다. 현재
   이동비로 그 인원을 모두 보낼 수 있어야 한다.
5. 검증에 사용한 최소 인원을 실제 첫 집결 규모로 그대로 넘긴다. HQ 잠금
   뒤 경유지로 이동 중인 `ATTACK` 병력도 작전 전력으로 승계하고, 첫 파동에
   이미 적용한 1/3 안전 마진은 후속 턴에 중복 적용하지 않는다.

보정 후 Pretest #15 / 188은 다시 25:20 승리로 복구됐고, 목표였던
Pretest #25 / 1414는 T139에 상대 HQ를 파괴했다.

| 상대 | 기존 최고 승/무/패 | 수정 후 승/무/패 | 기존 승점률 | 수정 후 | 변화 |
|---|---:|---:|---:|---:|---:|
| `65205` | 99/0/1 | 99/0/1 | 99.0% | 99.0% | 0.0%p |
| `65257` | 99/0/1 | 99/0/1 | 99.0% | 99.0% | 0.0%p |
| `70784` | 76/21/3 | 76/21/3 | 86.5% | 86.5% | 0.0%p |
| `70833` | 76/21/3 | 76/21/3 | 86.5% | 86.5% | 0.0%p |
| `79541` | 75/22/3 | 77/20/3 | 86.0% | **87.0%** | **+1.0%p** |
| `83284` | 75/21/4 | 76/20/4 | 85.5% | **86.0%** | **+0.5%p** |
| `83304` | 68/28/4 | 68/28/4 | 82.0% | 82.0% | 0.0%p |
| `84206` | 57/32/11 | 59/30/11 | 73.0% | **74.0%** | **+1.0%p** |
| `87838` | 58/31/11 | 59/30/11 | 73.5% | **74.0%** | **+0.5%p** |
| `91531` | 48/37/15 | 51/34/15 | 66.5% | **68.0%** | **+1.5%p** |
| `93978` | 46/44/10 | 46/44/10 | 68.0% | 68.0% | 0.0%p |
| `96816` | 61/31/8 | 61/31/8 | 76.5% | 76.5% | 0.0%p |
| `97302` | 56/35/9 | 58/33/9 | 73.5% | **74.5%** | **+1.0%p** |
| `107981` | 56/35/9 | 58/33/9 | 73.5% | **74.5%** | **+1.0%p** |
| `108830` | 59/35/6 | 61/33/6 | 76.5% | **77.5%** | **+1.0%p** |
| `109203` | 46/47/7 | 45/48/7 | 69.5% | 69.0% | -0.5%p |
| `110193` | 63/32/5 | 63/32/5 | 79.0% | 79.0% | 0.0%p |
| `110496` | 67/28/5 | 68/27/5 | 81.0% | **81.5%** | **+0.5%p** |
| `110584` | 62/31/7 | 62/31/7 | 77.5% | 77.5% | 0.0%p |

| 전체 1,900판 | 승/무/패 | 승점률 | 변화 |
|---|---:|---:|---:|
| 기존 최고 `adaptive_reinforce` | 1247/531/122 | 79.61% | 기준 |
| HQ 최대 수비 생산 포함 직접 공세판 | **1262/516/122** | **80.00%** | **+0.39%p** |

9모델 개선, 9모델 동일, 1모델 악화이며 승리가 15판 늘고 패배는 122판으로
그대로다. 따라서 이 보정판을 새 최고 후보로 채택한다.

### 실제 교전 지역 증원 직행 실험

`territoryReinforcement` 상태에서 목표 지역에 아군과 적군이 함께 있어 실제
전투가 진행 중이면 `pickWaypoint`로 아군 거점을 경유하지 않고, 이동 명령의
최종 목표를 교전 지역 자체로 지정하도록 바꿨다. 교전 전 최초 집결과 전투가
없는 목표의 안전 경유는 기존대로 유지했다.

상태기가 잠시 풀린 교전까지 강제로 REINFORCE로 복구하는 1차안은 70784
상대가 86.5%에서 83.0%로 악화되어 제거했다. 직행 인원을 정적 최소치나
시간축 최소치로 제한하는 안도 각각 70784 82.0%/80.0%로 더 나빠 제거했다.
최종 별도 실험판에는 기존 증원 상태와 인원 계산을 전혀 바꾸지 않고 이동
목적지만 직행으로 바꾼 순수안만 남겼다.

| 전체 1,900판 | 승/무/패 | 승점률 | 최고판 대비 |
|---|---:|---:|---:|
| HQ 직접 공세 보정 최고판 | 1262/516/122 | **80.00%** | 기준 |
| 교전 지역 증원 순수 직행판 | 1196/546/158 | **77.32%** | **-2.68%p** |

직행판은 `87838` +2.5%p, `110193` +0.5%p, `110584` +2.0%p였지만
`91531` -6.5%p, `97302`/`107981` -5.5%p, `70784`/`70833`/`110496`
-5.0%p 등 전체 회귀가 컸다. 아군 건물 경유의 무료 이동이 사라져 이동비가
늘고 먼 잉여 병력이 전투에 장기간 묶이는 영향이다. 요청한 동작 확인용으로
별도 파일에 보존하되, 80.00% 최고판에는 채택하지 않는다.

## 관련 파일

| 파일 | 용도 |
|---|---|
| `summission_result/submission/NEXT NATION/110584_value_filter.cpp` | 채택 후보: 중앙 우선 + 거점 가치 필터 |
| `summission_result/submission/NEXT NATION/110584_value_filter_cap3.cpp` | 가치 필터 + 동시 확장 3개 제한 실험판 |
| `summission_result/submission/NEXT NATION/110584_center_priority_only.cpp` | 중앙 1인 우선만 적용한 대조군 |
| `summission_result/submission/NEXT NATION/110584_center_only.cpp` | 중앙 경합 포기 조건을 포함했던 비채택 실험판 |
| `value_filter_vs_original_official100_summary.json` | 단계 2의 100판 요약 |
| `value_filter_cap3_vs_original_official100_summary.json` | 단계 3의 100판 요약 |
| `no_center2_vs_original_official100_summary.json` | 단계 4의 100판 요약 |
| `original_vs_87838_official100_summary.json` | 오리지널 대 `87838` 100맵 결과 |
| `value_filter_vs_87838_official100_summary.json` | 초기 value filter 대 `87838` 100맵 결과 |
| `value_filter_attack_cost_gated_vs_87838_official100_summary.json` | 적 점거 전투비용 반영판 결과 |
| `value_filter_attack_cost_gated_vs_pool_100maps.csv` | 수정판 대 pool 19모델 × 100맵 결과 |
| `summission_result/submission/NEXT NATION/110584_territory_reclaim.cpp` | 거리 기반 필수 영토 회수 실험판(직전 기준판) |
| `summission_result/submission/NEXT NATION/110584_territory_push.cpp` | 필수 영토·중앙 확보 후 상대 거점으로 확장하는 별도 실험판 |
| `summission_result/submission/NEXT NATION/110584_territory_push_defense.cpp` | 선택 공세 중 기존 거점 방어를 허용한 별도 실험판 |
| `summission_result/submission/NEXT NATION/110584_territory_push_coherent.cpp` | 목표 선택·실행 병력 분류 통일, 공세 제한, HQ 업그레이드 병행 실험판 |
| `summission_result/submission/NEXT NATION/110584_territory_push_scoped.cpp` | 필수 탈환은 기준판, 새 상태 기계는 선택 공세에만 적용한 분리판 |
| `summission_result/submission/NEXT NATION/110584_territory_push_hybrid.cpp` | coherent 첫 공세 + 실패 뒤 동기화 재집결을 결합한 직전 최고 후보 |
| `summission_result/submission/NEXT NATION/110584_territory_push_continuous_reinforce.cpp` | 필수 탈환 선발대 승계 + 연속 증원 + 공세 중 방어를 결합한 직전 최고 후보 |
| `summission_result/submission/NEXT NATION/110584_territory_push_response_slack.cpp` | 일반 공격 목표 선택에 보이는 첫 방어 도착 위험을 1점만 반영한 직전 최고 후보 |
| `summission_result/submission/NEXT NATION/110584_territory_push_adaptive_reinforce.cpp` | 반응 관측 후 3턴 방어 창으로 연속 증원 지속 여부를 매 턴 재판단하는 현재 최고 후보 |
| `summission_result/submission/NEXT NATION/110584_territory_push_adaptive_reinforce_empty_reclaim_direct_reinforce_prelaunch_axis_priority.cpp` | BUILD 목적 범위, 교전 중 증원 직행, 완공 턴 우선, 시간 내 확정 HQ 직접 공세를 결합한 실험판 |
| `summission_result/submission/NEXT NATION/110584_territory_push_continuity_gate.cpp` | 전선 붕괴 뒤 상대 증원이 먼저 도착하면 재집결하는 비채택 실험판 |
| `summission_result/submission/NEXT NATION/110584_original_center2.cpp` | 순수 오리지널에 중앙 2인 직행만 고정한 비채택 대조군 |
| `summission_result/submission/NEXT NATION/110584_original_center2_reactive.cpp` | 첫 한 칸 뒤 상대 중앙 2인 여부에 따라 해제/직행하는 비채택 대조군 |
| `summission_result/submission/NEXT NATION/110584_original_center2_split_response.cpp` | 상대 중앙 병력이 있으면 2인 유지, 외곽 분산이면 중앙 1인+최속 외곽 1인으로 나누는 실험판 |
| `summission_result/submission/NEXT NATION/110584_territory_push_projected_defense.cpp` | 반응 가능한 상대 예비대를 전부 수비군에 포함한 과보수 비채택 실험판 |
| `summission_result/submission/NEXT NATION/110584_territory_push_timeline_combat.cpp` | 병력 도착 턴·양측 증원·동시 건설/업그레이드를 반영한 시간축 실험판 |
| `summission_result/submission/NEXT NATION/110584_territory_push_causal_response.cpp` | 관측된 목표 주둔군·목표 방향 이동만 반영하고 즉시 출격 때만 같은 턴 건설/업그레이드를 검사하는 실험판 |
| `summission_result/submission/NEXT NATION/110584_territory_push_causal_first_response.cpp` | 관측 기반 계산에 목표 확인 뒤 가장 빠른 첫 수비 반응 한 개를 추가한 실험판 |
| `analyze_timeline_accuracy.py` | 시간축 예측과 실제 출격·점령·수비 증원·요새화 행동을 자동 대조하는 분석기 |
| `timeline_accuracy_pool_100_events.csv` | pool 19모델 × 100맵의 실제 출격 공세별 예측/관측 원본 |
| `timeline_accuracy_pool_100_fortification.csv` | 상대 동시 건설/업그레이드 예측별 실제 행동 원본 |
| `timeline_accuracy_pool_100_countdowns.csv` | 연속 턴 출격·점령 카운트다운 안정성 원본 |
| `timeline_accuracy_pool_100_summary.json` | 전체 및 상대별 정확도 요약 |
| `summission_result/submission/NEXT NATION/110584_territory_push_retry_floor.cpp` | 실패 파동 크기를 재시도 하한으로 둔 비채택 실험판 |
| `summission_result/submission/NEXT NATION/110584_territory_push_delta.cpp` | 후보별 `ATTACK(t)-HOLD` 최종 승점 비교를 행동에 적용한 비채택 실험판 |
| `analyze_474_wave.py` | 474 공식 공격의 출발 시점 병력과 정적 최소 병력을 역산하는 분석기 |
| `completion_first_vs_full_pool_100maps.csv` | 완공 턴 우선판 대 pool 19모델 × 공식 100맵 결과 |
| `completion_first_vs_adaptive_reinforce_comparison.csv` | 완공 턴 우선판과 기존 최고 `adaptive_reinforce` 간 상대별 승·무·패/승점률 증감 |
| `origin_only_mandatory_vs_original_100_summary.json` | `(0,0)` 최근접 한 곳만 필수 탈환하는 판 대 `110584` 공식 100맵 요약 |
| `hq_reserve_release_vs_83284_100_summary.json` | 남은 턴 내 불가능한 HQ 저축 해제판 대 `83284` 공식 100맵 요약 |
| `hq_reserve_release_vs_original_100_summary.json` | 남은 턴 내 불가능한 HQ 저축 해제판 대 `110584` 공식 100맵 요약 |
| `direct_hq_feasible_vs_83284_100_summary.json` | 남은 턴 내 확정 파괴 HQ 직접 공세판 대 `83284` 공식 100맵 요약 |
| `direct_hq_feasible_vs_original_100_summary.json` | 남은 턴 내 확정 파괴 HQ 직접 공세판 대 `110584` 공식 100맵 요약 |
| `direct_hq_feasible_vs_full_pool_100maps.csv` | 현재 누적 수정본 대 pool 19모델 × 공식 100맵 결과 |
| `direct_hq_feasible_vs_prior_full_pool_comparison.csv` | 현재 누적 수정본을 완공 턴 우선판·기존 최고판과 상대별 비교한 표 |
| `replay_direct_hq_conservative_draw_vs_83284_pretest25_1414.html` | 직접 HQ 수정 전, 병력 43:6·거점 11:0인데도 끝난 25:25 무승부 리플레이 |
| `replay_direct_hq_feasible_win_vs_83284_pretest25_1414.html` | 직접 HQ 수정 후 같은 맵을 T184 HQ 파괴 승리로 바꾼 리플레이 |
| `debug_A_direct_hq_feasible_win_vs_83284_pretest25_1414.txt` | 대표 승리판 `DIRECT_HQ_FEASIBLE/LOCK` 및 출격 디버그 로그 |
| `replay_completion_first_vs_original_pretest14_1281.html` | 완공 턴 우선판 대 오리지널, Pretest #14 1281 맵 리플레이 |
| `adaptive_best_direct_hq_vs_full_pool_100maps.csv` | 기존 최고에 현재 주둔군만 본 HQ 직행을 이식한 비채택 79.05% 결과 |
| `adaptive_best_direct_hq_defense_vs_full_pool_100maps.csv` | 상대 HQ 최대 수비 생산까지 포함한 새 최고 80.00% 결과 |
| `adaptive_best_direct_hq_defense_vs_83284_100_summary.json` | 새 최고판 대 `83284` 공식 100맵 요약 |
| `adaptive_best_direct_hq_defense_vs_110193_100_summary.json` | 회귀 보정 확인용 대 `110193` 공식 100맵 요약 |
| `adaptive_best_direct_hq_defense_vs_110584_100_summary.json` | 회귀 확인용 대 `110584` 공식 100맵 요약 |
| `replay_adaptive_best_direct_hq_defense_win_vs_83284_pretest25_1414.html` | 새 최고판이 T139에 HQ를 파괴한 대표 리플레이 |
| `debug_A_adaptive_best_direct_hq_defense_win_vs_83284_pretest25_1414.txt` | 대표판 직접 HQ 검증·잠금 디버그 로그 |
| `summission_result/submission/NEXT NATION/110584_territory_push_adaptive_reinforce_direct_battle.cpp` | 실제 교전 지역 증원이 경유지 없이 직행하는 별도 실험판 |
| `summission_result/submission/NEXT NATION/110584_territory_push_adaptive_reinforce_direct_battle_test.exe` | 교전 지역 직행 실험판 실행 파일 |
| `adaptive_best_direct_battle_route_only_vs_full_pool_100maps.csv` | 교전 지역 순수 직행판 pool 1,900판 결과(77.32%) |

## 결론

오리지널 상대 자체 대전에서는 거점 가치 필터가 좋아 보였지만 pool 교차
검증에서는 오리지널보다 약했다. 이후 거리 기반 필수 영토 회수와 상대 거점
공세를 붙이는 과정에서도 병력 분류 불일치와 무한 공세 잠금 때문에 크게
퇴행했다. coherent의 첫 공세 방식은 유지하고 실제 실패 뒤에만 동기화
재집결로 바꾸는 `territory_push_hybrid`까지는 pool 승점률 76.92%였다.
그 뒤 실제 전투가 계속되는 동안 후속 병력을 선발대로 승계하되 이를 필수
탈환에만 제한하고, 공세 중 방어 이동을 실제로 실행한
`territory_push_continuous_reinforce`가 78.32%를 기록했다. 이후 일반 공격
목표 선택에 보이는 첫 방어 도착 위험만 약하게 반영한
`territory_push_response_slack`이 같은 100맵 조건에서 78.47%를 기록했다.
그 위에 실제 방어 반응이 관측된 뒤 다음 3턴만 상대 생산을 보고 연속 증원
지속 여부를 매 턴 다시 판단한 `territory_push_adaptive_reinforce`가 1247승
531무 122패, 79.61%로 상승했다. 기존 제출 파일과 앞선 최고 파일은 덮지
않았으며 이 별도 파일을 현재 최고 후보로 유지한다.

`ATTACK(t)-HOLD`를 공식 승패 세 단계만으로 엄격하게 거른 실험은 승리를
무승부로 바꾸는 경우가 많아 채택하지 않는다. 다음 개선에서는 같은 공식
승점 안에서도 HQ HP 차이, 점령 완료 턴, 투입 병력 손실과 이후 생산력을
연속값으로 비교하되, 그 연속값이 실제 최종 승패와 상관관계가 있는지 먼저
로그로 검증해야 한다.

시간축 전투 실험은 정적 수비 계산의 구조적 오류와 동시 건설 누락은
해결했지만, 100맵 전체 승점률이 73.47%로 현재 최고 78.32%보다 낮았다.
따라서 다음 단계는 시간축 계산을 폐기하는 것이 아니라, 상대가 실제로
어떤 예비대를 방어에 보낼지에 대한 정책 예측을 보정하는 것이다. 지금처럼
가능한 정지 예비대가 모두 즉시 반응한다고 보는 가정은 여전히 승리를
무승부로 바꾸는 경향이 있다.

추가로 BUILD 목적 범위, 현재 교전 중 증원 직행, 완공 턴 우선을
결합한 실험판은 pool 19모델 × 100맵에서 승점률 78.26%를 기록했다.
`110584` 자체 대전은 74승 22무 4패로 강했지만 pool 전체는 현재 최고
79.61%보다 1.34%p 낮으므로, 최고 후보는 계속
`territory_push_adaptive_reinforce`로 남겨둔다.

그 실험판의 후속 변경 위에서 직접 HQ 공세의 만렙·수입 이중 게이트를
실제 남은 턴 집결·이동·공성 시뮬레이션으로 교체한 결과, `83284` 상대
100맵은 56승 39무 5패에서 60승 35무 5패로 개선되고 `110584` 상대는
51승 41무 8패로 동일했다. 따라서 직접 HQ 수정 자체는 채택한다. 다만
원점 최근접 한 곳만 필수 탈환하도록 바꾼 앞선 변경 때문에 전체 기준판의
절대 성능이 이미 낮아진 상태이므로, 이 결과만으로 현재 최고 후보 파일을
교체하지는 않는다.

이후 수행한 pool 전체 1,900판에서 현재 누적 수정본은 1066승 619무
215패, 승점률 72.39%에 그쳤다. 완공 턴 우선판 78.26%보다 5.87%p,
기존 최고 79.61%보다 7.21%p 낮다. 따라서 직접 HQ 판단 로직은 분리해
가져갈 가치가 있지만, 원점 필수 거점 변경까지 합쳐진 현재 파일 전체는
최고 후보로 채택하지 않는다.

마지막으로 그 직접 HQ 판단만 기존 최고 `adaptive_reinforce`에 이식하고,
공격대 도착 전과 공성 중 상대 HQ가 현재 골드·순수입·훈련 슬롯으로 생산할
수 있는 최대 수비병까지 시간축에 포함했다. 이 최종판은 pool 19모델 × 공식
100맵에서 **1262승 516무 122패, 승점률 80.00%**를 기록했다. 기존 최고보다
승리가 15판 늘고 패배는 같아 **+0.39%p** 개선됐으므로, 현재 최고 후보는
수정된 `110584_territory_push_adaptive_reinforce.cpp`이다.

## 상대 집결군 일제출발 역습 실험 (비채택, 2026-08-27)

상대 비본부 거점의 직전 정지 병력 수와 초과 집결 수를 저장하고, 다음 턴
같은 거점의 정지 병력이 초과 집결 수 이상 줄면서 같은 첫 이동 지역으로 그
수 이상의 병력이 한꺼번에 출발하면 공세 시작으로 판정했다. 이때 기존
미러 수비 거점에 모인 노동 인구 초과분을 상대가 비운 집결 거점으로 경유지
없이 곧바로 역습시키도록 실험했다. 상대 병력 ID는 추적하지 않고 턴 사이의
병력 수·이동 상태 변화만 사용했다.

공식 맵 seed 42, 좌측, worker 16, pool 19모델 × 10맵의 동일 조건 비교에서
기존 최고판은 **130승 51무 9패, 81.84%**였지만 최종 역습 실험판은
**109승 69무 12패, 75.53%**로 **-6.31%p** 하락했다. 상대 출발지를 치는
동안 실제 공격축 수비와 기존 공세 흐름이 함께 약해져 무승부와 패배가 모두
늘었다. 전체 1,900판 검증 전에도 하락 폭이 충분히 커서 비채택하고, 메인
소스는 역습 코드가 들어가기 전의 80.00% 최고판으로 복구했다.

비교 결과 파일은 `adaptive_best_direct_hq_defense_vs_full_pool_10maps_recheck.csv`와
`adaptive_best_staging_counterraid_vs_full_pool_10maps.csv`이며, 실험 실행 파일은
`adaptive_best_staging_counterraid_test.exe`로 남겨 두었다.

## 현재 최고판의 pool 전체 패배 리플레이 (2026-08-27)

현재 80.00% 최고판을 공식 고유 맵 100개(seed 42), LEFT 조건으로 pool
19모델과 다시 대전해 기존 전체 결과표의 상대별 패배 수와 일치하는지
검증했다. 기존 결과와 정확히 같은 총 **122패**가 재현됐다.

`adaptive_best_pool_loss_replays_100maps/index.html`에서 상대 모델별 모든 패배
리플레이를 열 수 있다. 각 리플레이 옆에는 같은 판의 후보 측 `debug_A` 로그를
함께 두었고, 전체 맵 경로·종료 사유·파일 경로는
`adaptive_best_pool_loss_replays_100maps/loss_manifest.csv`에 정리했다. 생성기는
`generate_pool_loss_replays.py`이며, 산출물 검증 결과 HTML 122개, 디버그 로그
122개, 누락 파일과 깨진 인덱스 링크는 모두 0개였다.

목록 페이지가 아니라 실제 뷰어 하나에서 전부 볼 수 있도록 122개 리플레이를
gzip으로 내장한 단일 파일 `adaptive_best_pool_122_losses_replay.html`도 만들었다.
상단 드롭다운에서 상대 모델과 패배 맵을 선택하면 같은 파일 안에서 해당
리플레이가 로드된다(19모델, 122판, 약 5.4 MiB).

## 방어·공격 공용 중앙축 집결 허브 실험 (2026-08-27)

상대 비본부 거점에 노동 인구 초과 병력이 집결하면, 아군 BASE 중 상대
집결군보다 내 HQ까지의 거리가 짧은 곳만 후보로 삼았다. 후보 점수는
`원점-내 HQ 직선축 수직거리 + 상대 HQ 거리 - 상대 집결군 거리`이며,
점수가 가장 낮은 곳을 방어·선택 공세 공용 집결지로 사용한다. 조건을
만족하는 BASE가 없을 때만 HQ를 폴백으로 허용한다. 선택 공세가 시작되면
상대 집결군이 이동해 정지 집결 감지가 사라져도 해당 허브 위치를 유지한다.

실험 소스와 실행 파일은 각각
`110584_territory_push_adaptive_reinforce_shared_axis_hub.cpp`와
`110584_territory_push_adaptive_reinforce_shared_axis_hub_test.exe`이다. 기존
80.00% 최고 소스는 덮지 않았다.

대표 `adaptive_best_pool_122_losses_replay.html#3`의 70784 / Pretest #18 /
1558 패배 맵은 수정본에서 T195 상대 HQ 파괴 승리로 바뀌었다. 재현 파일은
`replay_shared_axis_hub_vs_70784_pretest18_1558_battle146914.html`이다. 반면
`#7`의 70833 / Pretest #23 / 1558 맵은 공용 허브가 기존 공격·방어 집결지와
같은 R33으로 선택되어, 네 명을 출격시켜 R33을 비우는 별도의 출격 게이트
문제는 해결하지 못했고 여전히 패배했다.

공식 고유 맵 10개(seed 42), LEFT, pool 19모델, worker 16의 190판에서는
**121승 61무 8패, 79.74%**였다. 기존 최고판의 같은 표본
**130승 51무 9패, 81.84%**보다 **-2.10%p** 낮다. `110584` 단독 공식
100맵에서는 **58승 32무 10패, 74.00%**로, 기존 최고판의
**62승 31무 7패, 77.50%**보다 **-3.50%p** 낮았다. 결과 파일은
`adaptive_best_shared_axis_hub_vs_full_pool_10maps.csv`와
`adaptive_best_shared_axis_hub_vs_110584_100_games.csv`이다.

후속 요청으로 허브 점수의 `- 상대 집결군 거리`를 제거하고
`+ 상대 집결군 거리`로 뒤집었다. 따라서 새 점수는 `원점-내 HQ 축
수직거리 + 상대 HQ 거리 + 상대 집결군 거리`이며, 최소 점수를 고르므로
상대 집결군에서 멀수록 감점된다. 완전 동점의 마지막 타이브레이크도 먼
후보가 아니라 가까운 후보를 고르도록 함께 변경했다. 동일 공식 고유 맵
10개(seed 42), LEFT, pool 19모델, worker 16의 결과는 **118승 63무
9패, 승점률 78.68%**다. 종전 먼 거리 가점판 **79.74%**보다
**-1.05%p** 낮고, 최고판 동일 표본 **81.84%**보다 **-3.16%p** 낮다.
`70784`, `70833`, `91531`은 각각 +5.0%p 개선됐지만 `79541` -15.0%p,
`87838` -10.0%p, `83284`/`84206` -5.0%p로 악화됐다. 실행 파일과 결과는
`110584_territory_push_adaptive_reinforce_shared_axis_hub_enemy_proximity_test.exe`,
`adaptive_best_shared_axis_hub_enemy_proximity_vs_full_pool_10maps.csv`다.
이 표본의 9패 리플레이와 디버깅 로그는
`shared_axis_hub_enemy_proximity_pool_losses_10maps/index.html`에 정리했다.

이 가운데 `79541` / Pretest #19 / map 809 / battle 157677 패배를
분석했다. 구형 출격 전 계산은 목표 R54의 기존 수비 4명만 보고 5명이면
점령 가능하다고 판정한 뒤 마진 1명을 더해 6명을 보냈다. 실제로는 R80의
정지 병력 2명이 T92, 후속 병력 5명이 T94~T96에 합류해 총 7명이 추가
방어했고 공격대는 전멸했다. 당시 출격 후 계산도 아군은 남은 게임 전체
생산을 넣으면서 상대는 관측 반응 뒤 3턴 생산만 넣어 출격 전후 입력이
서로 달랐다.

이에 공용 허브 실험 소스의 필수 거점 회수전에 12턴 공용 시간축을 넣었다.
목표 주둔군, 목표 방향 이동군, 목표에 가장 가까운 상대 건물 한 곳의 정지
예비대, HQ에서 생산한 후속 병력을 실제 도착일에 합류시킨다. 출격 전에는
현재 파동만 아군으로 넣고, 출격 후에는 양측 후속 생산을 같은 12턴 창으로
본다. 최초 마진 1명판은 동일 미니풀에서 **108승 43무 39패, 68.16%**였고,
요청에 따라 필수 회수전 마진을 0명으로 낮춘 판은 **99승 41무 50패,
62.89%**였다. 결과 원본은 각각
`adaptive_best_shared_axis_hub_enemy_proximity_reactive_stream_vs_full_pool_10maps.csv`,
`adaptive_best_shared_axis_hub_enemy_proximity_reactive_stream_margin0_vs_full_pool_10maps.csv`다.

마진 0 시간축을 점검하던 중, 출격 명령을 내리는 현재 턴을 `day 0`으로
두면서 상대가 그 동시 명령의 최종 목표를 이미 안다고 가정해 목표 전용
생산을 즉시 시작하는 한 턴 선견지명을 확인했다. 이를 다음과 같이 고쳤다.

- 출격 전 `day 0`에는 상대가 최종 목표를 모른다.
- 첫 이동 결과를 본 다음 의사결정인 `day 1`부터 경로와 일치하지 않는
  상대 건물 후보를 제거한다. 목표가 하나로 좁혀진 실제 탐지일을 반응
  시작일로 사용한다.
- 상대의 목표 전용 생산과 정지 예비대 출발을 모두 그 탐지일부터 허용한다.
  정지 예비대 이동비 역시 반응일에 지출한다.
- 이미 출격한 파동을 다음 턴에 재평가할 때는 목표가 드러난 상태이므로
  `day 0` 반응을 허용한다.

수정 후 동일 공식 고유 맵 10개(seed 42), LEFT, pool 19모델, worker 16의
190판 결과는 **118승 58무 14패, 승점률 77.37%**다. 선견지명 수정 전
마진 0판보다 **+14.48%p**, 패배는 **50판에서 14판**으로 줄었다. 공용
허브 거리판의 78.68%보다는 1.31%p 낮다. 문제 맵은 **200턴 HQ 30:25
승리**를 유지했고, 출격 전 로그에는 경로에 따라 `reaction_day=1~4`가
기록된다. 실행 파일, 결과, 리플레이와 디버그 로그는 각각
`110584_territory_push_adaptive_reinforce_shared_axis_hub_enemy_proximity_reactive_stream_margin0_detection_test.exe`,
`adaptive_best_shared_axis_hub_enemy_proximity_reactive_stream_margin0_detection_vs_full_pool_10maps.csv`,
`shared_axis_hub_reactive_margin0_detection_case_79541_pretest19_809/replay_detection_vs_79541_pretest19_809.html`,
`shared_axis_hub_reactive_margin0_detection_case_79541_pretest19_809/debug_detection_vs_79541_pretest19_809.txt`다.
동일 미니풀의 14패 HTML과 디버그 로그는
`shared_axis_hub_reactive_margin0_detection_pool_losses_10maps/index.html`에
모델별로 정리했다(79541 1패, 84206 3패, 87838 4패, 97302 2패,
107981 2패, 108830 1패, 110193 1패).

`84206` / Pretest #16 / map 418의 T66 실패에서는 출격 직전 R15가 빈
거점이고 상대 정지 병력 B5 한 명만 있었지만, 상대가 같은 T66 명령으로
L1 기지를 건설했다. 기존 반응형 시뮬은 빈 필드로 계산해 T71 점령을
예측했으나, 실제 기지 HP 6과 포탑 1이 공성을 늦춘 사이 R49·HQ의 기존
예비대가 도착해 공격대가 전멸했다. 이에 반응형 시간축에도 다음 동시 건설
예측을 이식했다.

- 빈 필수 거점에 상대 정지 병력이 있고 상대 추정 골드가 300 이상이면,
  공격 탐지와 무관하게 `day 0`에 L1 기지를 건설한다고 본다.
- 전투에 기지 HP 6과 포탑 1을 넣고 상대 골드에서 건설비 300을 차감한다.
- 건설 병력 한 명까지 작업한다고 보아 상대 순수입에 매 턴 15골드를
  더하고, 이후 수비 생산 가능 시점에도 반영한다.

재현 로그에서는 T8부터 `fortify=1 fortify_income=15`가 기록됐고 필요한
파동이 1명에서 3명으로 증가했다. 이 때문에 문제 맵의 전개가 T66 이전부터
달라져 동일 장면은 재발하지 않았지만, 전체 판은 T166 아군 HQ 파괴 패배로
끝났다. 재현 HTML과 로그는
`shared_axis_hub_reactive_margin0_detection_fortify_case_84206_pretest16_418/replay_fortify_vs_84206_pretest16_418.html`,
`shared_axis_hub_reactive_margin0_detection_fortify_case_84206_pretest16_418/debug_fortify_vs_84206_pretest16_418.txt`다.

동일 공식 고유 맵 10개(seed 42), LEFT, pool 19모델, worker 16의 190판
결과는 **114승 57무 19패, 승점률 75.00%**다. 동시 건설 예측 전
**118승 58무 14패, 77.37%**보다 **-2.37%p**이고 패배가 5판 늘었다.
`107981`, `87838`, `97302`에는 각각 +20.0%p였으나 `110584` -25.0%p,
`109203`/`110496`/`91531` -15.0%p, `70784`/`70833`/`84206` -10.0%p였다.
실행 파일과 결과 원본은
`110584_territory_push_adaptive_reinforce_shared_axis_hub_enemy_proximity_reactive_stream_margin0_detection_fortify_test.exe`,
`adaptive_best_shared_axis_hub_enemy_proximity_reactive_stream_margin0_detection_fortify_vs_full_pool_10maps.csv`다.

추가 점검 결과 T24 첫 출격에서도 동시 건설 예측이 발동하지 않은 이유는
상대 B3이 실제로는 T23에 R31에 도착해 정지했지만, 상대의 비공개 최종
목적지를 모르는 내부 추적에서는 직전 턴 이동 병력을 한 턴 더 `MOVING`으로
표시했기 때문이다. 또한 아군 A3이 이미 여러 턴 R31로 이동해 목표를
공개했는데도 새 파동의 R14 출발부터 탐지 시간을 다시 세어
`reaction_day=2`로 늦췄다. 다음과 같이 보정했다.

- 빈 거점에 상대 병력이 현재 존재하면 내부 `STATIONARY/MOVING` 표기와
  무관하게 그 턴에 멈춰 동시 건설할 수 있다고 본다.
- 아군 병력이 이미 같은 목표에 도착했거나 이동 중이면 새 파동 출격 전부터
  목표가 공개된 것으로 보아 `reaction_day=0`을 사용한다.

같은 T24 계산은 기존 `fortify=0 reaction_day=2 ready=2/2`에서
`fortify=1 reaction_day=0 ready=2/5`로 바뀌어 잘못된 2명 출격을 차단했다.
그러나 대기 중 상대가 중앙 R31에 병력을 계속 쌓으면서 요구량이 6~8명으로
증가했고, 수정본은 첫 파동 자체를 출격하지 못한 채 T158에 아군 HQ가
파괴됐다. 재현 파일은
`shared_axis_hub_reactive_margin0_detection_fortify_arrivalaware_case_84206_pretest16_418/replay_arrivalaware_vs_84206_pretest16_418.html`이다.

동일 미니풀 190판 결과도 **108승 37무 45패, 66.58%**로, 도착 상태
보정 전 건설 예측판의 **75.00%**보다 **-8.42%p**, 건설 예측 전 탐지판의
**77.37%**보다 **-10.79%p**였다. 계산 누락은 해소했지만 필수 중앙 거점에서
상대의 생산 증가를 계속 추종하며 영원히 출격하지 못하는 정책 회귀가
커졌다. 실행 파일과 결과는
`110584_territory_push_adaptive_reinforce_shared_axis_hub_enemy_proximity_reactive_stream_margin0_detection_fortify_arrivalaware_test.exe`,
`adaptive_best_shared_axis_hub_enemy_proximity_reactive_stream_margin0_detection_fortify_arrivalaware_vs_full_pool_10maps.csv`다.

후속 요청으로 필수 거점 탈환과 선택 공세의 구분을 없앴다. 중앙 또는 내
쪽 거점을 별도 우선 목표로 잠그지 않고, 상대가 점유한 모든 거점을 동일한
공세 후보군으로 평가한다. 방어 예약 뒤 남는 현재 병력을 목표까지의 실제
도착일에 넣고, 동시 건설·정지 예비대·상대 후속 생산을 포함한 반응형 12턴
시간축에서 점령되는 목표만 고른다. 예상 점령일, 필요 병력, 전선 거리
순으로 정렬한다. 실패하거나 20턴 안에 집결하지 못하면 해당 목표만 15턴
제외하고 다른 점령 가능한 거점을 바로 탐색한다. 따라서 필수 거점 전용
무한 재집결과 시뮬레이션 우회 재공격은 더 이상 사용되지 않는다.

84206 / Pretest #16 / map 418에서는 중앙 R31 무한 돌격 대신 R33, R46,
R49, R48, R47을 상황에 따라 선택·중단했고, T151 상대 HQ 직접 공세로
전환해 **T171 상대 HQ 파괴 승리**로 바뀌었다. 재현 HTML과 로그는
`shared_axis_hub_unified_offense_case_84206_pretest16_418/replay_unified_offense_vs_84206_pretest16_418.html`,
`shared_axis_hub_unified_offense_case_84206_pretest16_418/debug_unified_offense_vs_84206_pretest16_418.txt`다.

그러나 동일 미니풀 190판 결과는 **82승 48무 60패, 승점률 55.79%**로,
구분 통합 전 도착 상태 보정판의 **66.58%**보다 **-10.79%p** 낮았다.
`84206`은 +40.0%p, `87838`은 +25.0%p 개선됐지만 `110193` -55.0%p,
`108830` -50.0%p, `110496` -40.0%p, `97302`/`107981` -30.0%p로
악화됐다. 공세 제한이 사라져 순간적으로 점령 가능한 목표가 생길 때마다
경제·업그레이드보다 연속 공격을 우선한 영향이다. 실행 파일과 결과는
`110584_territory_push_adaptive_reinforce_shared_axis_hub_unified_offense_test.exe`,
`adaptive_best_shared_axis_hub_unified_offense_vs_full_pool_10maps.csv`다.

## 출격 전후 통합 시간축·빈 거점 즉시 건설 예측 실험 (2026-08-27)

`plan_attack_force`의 출격 전 정적 계산과 출격 후
`simulate_reinforcement_stream`의 시간축 계산이 서로 다른 병력·기간을 보던
문제를 별도 후보
`110584_territory_push_adaptive_reinforce_unified_stream.cpp`에서 통합했다.
기존 최고 소스는 `UNIFIED_RECLAIM_STREAM` 매크로가 없으면 종전 동작을
유지하고, 실험 래퍼만 해당 매크로를 켠다.

- 출격 전후 모두 실제 도착일에 병력을 합류시키는 같은 stream 전투 엔진과
  입력 생성기를 사용한다.
- 목표 방향으로 이미 이동 중인 적과, 목표에 가장 가까운 적 건물 한 곳의
  정지 병력을 반응 가능한 예비대로 넣는다. 과거처럼 전 맵의 모든 예비대를
  즉시 주둔군으로 합치지는 않는다.
- 출격 후 양측 미래 생산은 동일한 12턴 전술 창, 동일한 골드·순수입·유지비
  규칙으로 계산한다.
- 출격 전에는 미래 생산을 양쪽 모두 제외하고, 현재 존재하는 병력만으로
  stream을 통과하는 최소 동기화 파동을 찾는다.

대표 `adaptive_best_pool_122_losses_replay.html#5`, 70784 / Pretest #30 /
1558에서는 기존 T35의 5명 출격을 `ready=5/7`로 차단했고, 시험 당시 T50에
체력이 오른 6명 파동을 보내 T163 상대 HQ 파괴 승리로 바뀌었다. 첫 번째
전 맵 예비대 입력안은 공식 10맵 × 19모델에서 97승 85무 8패, 73.42%로
기존 동일 표본 81.84%보다 과보수였고, 이후 예비대 범위를 가장 가까운 적
건물 한 곳으로 축소했다. 축소판의 전체 100맵 평가는 사용자의 리플레이
우선 요청으로 중단했으므로 아직 채택 판정하지 않는다.

회귀 확인용으로 84206 / Pretest #16 / 1248 동일 맵에서 기존 최고판의
200턴 HQ 25:20 승리와 통합판의 T141 HQ 파괴 패배를 한 파일에서 전환하는
`replay_compare_baseline_vs_unified_stream_84206_pretest16_1248.html`을
생성했다. 이 비교본은 아래 즉시 건설 예측을 추가하기 전 축소판 결과다.

추가로 빈 거점에 상대 정지 병력만 있을 때 상대 추정 골드가 300 이상이면,
양측 동시 명령에서 상대가 즉시 L1 기지를 건설한다고 가정하도록 보정했다.
이 경우 전투 입력에 기지 HP 6과 포탑 1을 넣고 상대 골드에서 건설비 300을
차감한다. 새 기지의 `work_cap=1`까지 목표 주둔군이 다음 턴부터 일한다고
보아 상대 순수입에도 하루 15골드를 추가하며, 이 증가분은 같은 12턴 창의
상대 수비병 생산 가능 시점에 반영된다.

87838 / Pretest #16 / 418 검증에서는 T62~T64 빈 R15에
`RECLAIM_FORTIFY predicted=BUILD cost=300 income=15`와
`fortify=1 fortify_income=15`가 기록됐고, 수정본은 T196 상대 HQ 파괴
승리로 끝났다. 리플레이와 로그는 각각
`replay_unified_stream_fortify_income_check_vs_87838_pretest16_418.html`,
`debug_unified_stream_fortify_income_check_vs_87838_pretest16_418.txt`이다.
현재 통합·건설 예측판은 실험 후보이며 80.00% 최고판을 대체하지 않았다.

즉시 건설·수입 보정까지 포함한 현재 후보를 공식 고유 맵 10개(seed 42),
LEFT, pool 19모델, worker 16으로 미니풀 190판 평가했다. 결과는 **98승
70무 22패, 승점률 70.00%**로, 기존 최고판의 같은 표본 **130승 51무
9패, 81.84%**보다 **-11.84%p** 낮았다. 패배도 13판 늘었다. 상대별 최대
하락은 `84206`/`109203` -30.0%p, `87838`/`110193` -25.0%p이며,
`70784`/`70833`만 각각 +5.0%p 개선됐다. 빈 거점 건설 예측 자체의 효과와
통합 stream·예비대 변경의 효과가 합쳐진 누적 후보 결과이므로 건설 예측만의
인과 효과로 해석할 수는 없지만, 현재 누적판은 명백한 비채택이다. 결과
원본은 `adaptive_best_unified_stream_fortify_income_vs_full_pool_10maps.csv`다.

요청에 따라 통합 후보에서 필수 거점의 정적 최소 병력 `+1` 마진과 출격
직전 재검사의 `+1` 마진을 모두 0명으로 제거했다. 동일 미니풀 결과는
**94승 60무 36패, 65.26%**로 마진 1명판의 **98승 70무 22패,
70.00%**보다 **-4.74%p** 하락했고 패배가 14판 늘었다. 특히 `87838`은
마진 1명판 4승 3무 3패에서 1승 3무 6패로 -30.0%p 하락했다. 현재
stream의 미관측 대응 오차를 1명이 완충하고 있었다는 증거이며, 마진 0판은
더 강한 비채택 결과다. 원본은
`adaptive_best_unified_stream_fortify_income_margin0_vs_full_pool_10maps.csv`다.

이후 요청에 따라 통합 필수 거점 마진을 2명으로 올려 동일 미니풀을 다시
평가했다. 결과는 **96승 49무 45패, 63.42%**로, 필수 거점 마진 1명판의
**98승 70무 22패, 70.00%**보다 **-6.58%p** 낮고 패배가 22판에서
45판으로 늘었다. 더 큰 파동을 기다리는 사이 상대 생산과 경제가 성장해
출격 시점을 놓치는 회귀가 커진 것으로 보인다. 결과 원본은
`adaptive_best_unified_stream_fortify_income_margin2_vs_full_pool_10maps.csv`다.

다음 요청에서는 통합 필수 거점 마진을 다시 **+1명**으로 복구하고, 필수
거점과 별개인 일반 공세의 `ATTACK_SAFETY_MARGIN`을 **+5명에서 +1명**으로
낮췄다. 상대 HQ 결전은 여전히 `max(일반 마진, 전체 아군 병력의 1/3)`을
사용하므로 전체 병력의 1/3 마진은 유지된다. 동일 공식 고유 맵 10개,
seed 42, LEFT, pool 19모델, worker 16의 190판 결과는 **109승 57무
24패, 승점률 72.37%**다. 일반 +5명/필수 +1명판의 **70.00%**보다
**+2.37%p** 개선됐지만, 최고판 동일 표본 **81.84%**보다는
**-9.47%p** 낮아 아직 비채택이다. `96816`, `93978`, `110496`,
`110193`, `108830` 상대로 각각 +15.0%p 개선됐고, `70784`, `70833`
상대로는 각각 -10.0%p 하락했다. 결과 원본은
`adaptive_best_unified_stream_fortify_income_reclaim1_attack1_vs_full_pool_10maps.csv`다.
이 표본의 24패는 디버깅 로그와 함께
`unified_stream_reclaim1_attack1_pool_losses_10maps/index.html`에 모델별로
정리했다.

## 80.00% 최고판에 탐지형 12턴 회수전 이식 (2026-08-27)

요청한 `탐지 시점 이후 상대 12턴 생산 + 목표에 가장 가까운 적 건물 한
곳의 정지 예비대 + 필수 회수 마진 0` 조합을 shared-axis 누적 실험판이
아니라 80.00% 최고판에 직접 이식했다. 먼저 현재 소스에 뒤섞인 후속 실험을
배제하기 위해 `ATTACK_SAFETY_MARGIN=5`를 복원한 독립 소스
`110584_territory_push_adaptive_reinforce_highest_reconstructed.cpp`를 만들었다.
이 복원본은 공식 고유 맵 10개(seed 42), LEFT, pool 19모델에서 기존 최고
실행 파일과 상대별 CSV까지 완전히 같은 **130승 51무 9패, 81.84%**를
재현했다. 따라서 아래 비교의 기준 소스가 최고판과 일치함을 먼저 검증했다.

그 복원본을 기반으로 별도 후보
`110584_territory_push_adaptive_reinforce_highest_detection_reactive_margin0.cpp`
를 만들었다. 첫 출격 게이트는 현재 병력과 실제 도착일을 사용하되 아군 미래
생산은 제외하고, 이동 경로로 목표가 하나로 좁혀지는 날부터 상대 HQ의 향후
생산을 최대 12턴 창에 넣는다. 같은 시점에 목표와 가장 가까운 적 건물 한
곳의 정지 병력만 예비대로 출발시킨다. 출격 뒤 연속 증원 판단은 목표가 이미
공개됐다고 보고 양측의 미래 생산을 같은 12턴 창에 넣는다. 필수 거점의 정적
계획 마진과 출격 직전 마진은 모두 0명이다. 빈 거점 즉시 건설·건설 수입,
모든 정지 예비대, 통합 공세 목표 선택 등 다른 누적 실험은 포함하지 않았다.

동일 미니풀 190판 결과는 **116승 56무 18패, 승점률 75.79%**였다. 최고판
**130승 51무 9패, 81.84%**보다 **-6.05%p**, 승리 -14판, 무승부 +5판,
패배 +9판으로 명확히 악화됐다. `110584`/`93978`은 각각 +10.0%p,
`109203`/`91531`은 +5.0%p 개선됐지만, `87838` -30.0%p, `83304`
-25.0%p, `83284` -20.0%p, `110193`/`84206` -15.0%p 하락했다. 이 정도
회귀라 전체 1,900판 평가는 진행하지 않았고 최고 실행 파일도 교체하지
않았다.

대표적으로 87838 / Pretest #16 / map 418에서는 T61 첫 게이트가
`reaction_day=3`, `ready=4/4`, `CAPTURE day=8`로 4명을 출격시켰지만,
다음 턴의 출격 후 롤아웃은 목표를 즉시 공개된 것으로 바꾸면서 T62에
`FAIL`이 됐다. 이후 R15에 1~3명 재집결 파동을 반복하다 T177 아군 HQ가
파괴됐다. 즉 새 출격 전에 계산한 미래 탐지일과 출격 후 매 턴 day 0으로
초기화하는 반응 시점이 이어지지 않아, 출격 직후 필요 수비가 앞당겨지고
작은 파동·재집결이 반복되는 문제가 최고판의 필수 거점 잠금과 결합했다.

결과 원본은
`adaptive_best_highest_detection_reactive_margin0_vs_full_pool_10maps.csv`,
상대별 최고판 대비 표는
`adaptive_best_highest_detection_reactive_margin0_vs_highest_10maps_comparison.csv`다.
18개 패배 리플레이와 디버깅 로그는
`adaptive_best_highest_detection_reactive_margin0_pool_losses_10maps/index.html`
에 정리했다.

같은 190개 상대·맵 조합을 최고판과 수정판으로 다시 paired 실행해 결과가
달라진 90판의 양쪽 리플레이와 디버그 로그도 생성했다. 전환 분포는
`D→W` 31판, `W→D` 38판, `W→L` 11판, `L→W` 4판, `D→L` 4판,
`L→D` 2판이다. 직접 승패가 뒤집힌 `W↔L` 15판만 모은 단일 HTML은
`adaptive_best_highest_detection_reactive_margin0_changed_replays_10maps/direct_win_loss_flips.html`,
무승부 변화를 포함한 90판 전체 비교 HTML은 같은 폴더의
`paired_changed_replays.html`이다. 각 경기에서 기존 최고판과 12턴 수정판을
버튼 또는 숫자 키 1/2로 즉시 전환할 수 있다.

## 상대가 먼저 완공 가능한 빈 거점 제외 실험 (2026-08-27)

80.00% 최고판 복원본을 기반으로 별도 후보
`110584_territory_push_adaptive_reinforce_highest_skip_enemy_preclaim.cpp`를
만들었다. 빈 거점 확보 후보를 평가할 때 상대의 현재 여유 병력이 먼저
도착해, 내 BUILD 유닛 도착 전에 L1 기지를 완공할 수 있으면 후보에서
제외한다.

- 상대 자기 건물의 `work_cap`만큼은 노동 인력으로 남기고, 이를 초과한
  정지 병력만 여유 병력으로 본다. 건물 밖 정지 병력과 이미 해당 빈 거점
  방향으로 이동 중인 병력도 포함한다.
- 상대 이동비 10, 건설비 300, 현재 추정 골드와 관측 순수입을 날짜별로
  반영해 가장 이른 건설 명령 가능 턴을 계산한다.
- 게임에서 신규 기지 건설은 즉시 완료되지만 BUILD 단계가 MOVE보다 먼저라,
  h hop 병력은 h턴 뒤에야 건설할 수 있다. 상대 완공 턴이 내 도착 턴보다
  엄밀히 빠른 경우만 제외하며, 동시 도착은 먼저 완공한 것이 아니므로
  제외하지 않는다.
- 이렇게 제외된 곳은 `hasCapturableEmptyStronghold`에서도 빼서 공세를
  막는 공짜 거점으로 세지 않는다. 이후 상대가 실제로 기지를 세우면 기존
  필수 탈환·공세 로직이 전투 목표로 처리한다.

공식 고유 맵 10개(seed 42), LEFT, pool 19모델, worker 16의 미니풀에서는
**148승 36무 6패, 87.37%**로 최고판의 **130승 51무 9패, 81.84%**보다
**+5.53%p** 높았다. 그러나 같은 조건을 공식 고유 맵 100개, 총 1,900판으로
확대하자 **1192승 531무 177패, 76.71%**로 내려갔다. 최고판의 **1262승
516무 122패, 80.00%** 대비 **-3.29%p**, 승리 -70판, 무승부 +15판,
패배 +55판이다. `65205`/`65257`만 각각 +1.0%p 개선됐고, `110496`,
`84206`, `87838` -7.0%p, `93978` -6.0%p, `83284` -5.0%p 등 대부분의
상대에서 악화됐다. 10맵 표본의 큰 개선은 일반화되지 않았으며, 상대가
이론상 먼저 지을 수 있다는 이유만으로 아군 쪽 빈 거점까지 포기해 상대
경제 성장을 허용하는 회귀가 더 컸다. 따라서 최고판은 교체하지 않는다.

실행 파일은
`110584_territory_push_adaptive_reinforce_highest_skip_enemy_preclaim_test.exe`,
결과 원본은
`adaptive_best_highest_skip_enemy_preclaim_vs_full_pool_10maps.csv`와
`adaptive_best_highest_skip_enemy_preclaim_vs_full_pool_100maps.csv`, 상대별
최고판 비교는
`adaptive_best_highest_skip_enemy_preclaim_vs_highest_100maps_comparison.csv`다.

전체 177패 대신 대표 5판만 요청해, 미니풀 손실 중 서로 다른 상대
`79541`, `84206`, `93978`, `110496`, `110584`의 패배를 골랐다. 단일 선택형
HTML은 `adaptive_best_highest_skip_enemy_preclaim_5_losses_replay.html`이고,
각 판의 개별 HTML과 디버그 로그는
`adaptive_best_highest_skip_enemy_preclaim_pool_losses_5/index.html`에 있다.

### 상대 선점 제외를 한 거점으로 제한

후속 검토에서 기존 판정이 상대의 동일한 여유 병력과 골드를 모든 빈 거점
후보에 독립적으로 재사용해, 실제로는 한 곳만 지을 수 있는 상황에서도 여러
곳을 한꺼번에 포기한다는 문제가 확인됐다. 이에 선점 조건으로 제외하는
거점을 판단 전체에서 **최대 한 곳**으로 제한했다.

- 전략상 확보 대상인 빈 거점 중 아직 아군 BUILD 병력이 향하지 않고, 상대
  병력이 현재 점유하지 않은 곳만 선점 제외 후보로 본다.
- 상대 예상 완공 턴이 내 빌더 예상 도착 턴보다 엄밀히 빠른 후보만 남긴다.
- 그중 상대 예상 완공 턴이 가장 빠른 한 곳만 제외한다. 동률이면 기존 아군
  확장 우선순위, 내 도착 턴, 전선까지 거리, 본부와 실제 거리, 거점 ID
  순으로 결정한다.
- 선택된 동일한 한 거점만 BUILD 파병 대상에서 제외하며, 나머지 후보는
  상대가 이론상 먼저 지을 수 있어도 정상적인 빈 거점 확보 후보로 유지한다.
- 공격 목표나 공격 병력 수 산정 로직은 변경하지 않았다.

수정 실행 파일은
`110584_territory_push_adaptive_reinforce_highest_skip_one_enemy_preclaim_test.exe`다.
공식 고유 맵 10개(seed 42), LEFT, pool 19모델, worker 16의 190판 결과는
**145승 40무 5패, 86.84%**였다. 전부 제외한 판의 87.37%보다 -0.53%p지만
최고판 동일 표본의 81.84%보다 +5.00%p였다.

같은 조건을 고유 맵 100개, 총 1,900판으로 확대하자 **1197승 537무
166패, 77.13%**였다. 전부 제외한 판의 **1192승 531무 177패, 76.71%**보다
승리 +5판, 무승부 +6판, 패배 -11판, 승점률 +0.42%p로 일부 회복했다.
그러나 기존 최고판의 **1262승 516무 122패, 80.00%**보다는 승리 -65판,
무승부 +21판, 패배 +44판, 승점률 -2.87%p다. 상대별로 `65205`와
`65257`만 각각 +1.0%p 개선됐고, `87838` -8.0%p, `93978` -6.5%p,
`110496` -6.0%p, `96816` -5.5%p, `84206` -4.5%p 등 다수 상대에서
악화됐다. 따라서 한 곳 제한은 과도한 다중 제외를 완화했지만, 선점 가능성을
이유로 빈 거점을 포기하는 정책 자체의 전체 회귀를 해소하지 못해 최고판에는
채택하지 않는다.

결과 원본은
`adaptive_best_highest_skip_one_enemy_preclaim_vs_full_pool_10maps.csv`와
`adaptive_best_highest_skip_one_enemy_preclaim_vs_full_pool_100maps.csv`다.

### 한 거점 제한 + 실제 이동 중인 상대 병력만 선점 예측

정지 상태의 상대 여유 병력은 아직 목적지가 정해지지 않았는데도 모든 빈
거점으로 출발할 수 있다고 가정하는 것이 과도하게 보수적이었다. 이에 위의
한 거점 제한을 유지하되, 상대의 이전 지역에서 현재 지역으로의 이동 방향이
특정 빈 거점 최단경로와 일치해 **이미 그 거점 방향으로 이동 중인 병력만**
선점 예측에 포함했다. 상대 건물의 정지 잉여 병력과 건물 밖 정지 병력은
선점 판단에서 모두 제외했다. 제외할 한 곳을 고르는 기준과 BUILD 파병 외의
공격 목표·공격 병력 수 계산은 그대로 유지했다.

실행 파일은
`110584_territory_push_adaptive_reinforce_highest_skip_one_moving_enemy_preclaim_test.exe`다.
공식 고유 맵 10개(seed 42), LEFT, pool 19모델, worker 16의 190판에서는
**151승 35무 4패, 88.68%**로, 정지 병력까지 보던 한 곳 제외판의 86.84%보다
+1.84%p, 기존 최고판의 81.84%보다 +6.84%p였다.

고유 맵 100개, 총 1,900판에서도 **1287승 496무 117패, 80.79%**를 기록했다.
정지 병력까지 보던 한 곳 제외판의 **1197승 537무 166패, 77.13%**보다 승리
+90판, 무승부 -41판, 패배 -49판, 승점률 +3.66%p다. 기존 80.00% 최고판의
**1262승 516무 122패**와 비교하면 승리 +25판, 무승부 -20판, 패배 -5판,
승점률 **+0.79%p**로 새 최고 결과다.

기존 최고판 대비 `108830` +4.5%p, `107981`/`97302` +3.0%p,
`110193` +3.0%p, `109203`/`96816` +2.5%p, `110584` +2.0%p 등에서
개선됐다. 반면 `83284` -3.5%p, `83304` -2.0%p,
`70784`/`70833`/`91531` -1.5%p, `79541` -1.0%p는 악화됐다.

결과 원본은
`adaptive_best_highest_skip_one_moving_enemy_preclaim_vs_full_pool_10maps.csv`와
`adaptive_best_highest_skip_one_moving_enemy_preclaim_vs_full_pool_100maps.csv`다.

### 필수 탈환 마진 0 + 이동 중 적 증원 1회 포함

80.79% 새 최고 후보를 기반으로, 필수 거점 첫 출격의 +1명 마진을 0으로
낮추고 현재 목표 방향으로 이미 이동 중인 것이 관측된 상대 병력을 첫 출격
계산에 각각 한 번씩 포함하는 조합을 시험했다. 정지 예비대와 상대 미래
생산은 포함하지 않았다. 이동 중 증원 포함은 작전 계획 단계와 집결 완료
직전 재검증에 동일하게 적용했다. 별도 wrapper 소스와 실행 파일은 각각
`110584_territory_push_adaptive_reinforce_highest_moving_preclaim_reclaim_moving_def_margin0.cpp`,
`110584_territory_push_adaptive_reinforce_highest_moving_preclaim_reclaim_moving_def_margin0_test.exe`다.

문제가 드러난 70784 / Pretest #21 / map 1558을 먼저 재실행했다. 기존에는
R68의 현재 주둔 1명만 보고 +1명 마진을 더해 4명을 출격했지만, 수정판은
마진 0 때문에 상대 증원이 실제 이동을 시작해 관측되기 전인 T33에 3명을
더 일찍 출격했다. T34부터 이동 중 적 2명을 정상적으로 탐지했으나 이미
선발대가 출발한 뒤였다. 결과는 기존 T178 본부 파괴 패배에서 200턴 HQ
15 대 20 판정패로 바뀌었을 뿐 승리하지 못했다.

공식 고유 맵 10개(seed 42), LEFT, pool 19모델, worker 16의 190판 결과는
**122승 49무 19패, 77.11%**였다. 새 최고 후보의 **151승 35무 4패,
88.68%**보다 승리 -29판, 무승부 +14판, 패배 +15판, 승점률 -11.57%p다.
`70784`, `70833`, `79541`, `91531`은 각각 +5.0%p 개선됐지만,
`110496` -50.0%p, `108830`/`110193` -35.0%p,
`84206`/`87838`/`96816` -20.0%p 등 회귀가 훨씬 컸다. 이동 중 증원을
포함하는 방향 자체보다, 마진 0으로 출격 시점을 앞당겨 아직 관측되지 않은
증원을 피하지 못하는 효과가 지배적이었다. 따라서 1,900판 평가는 생략하고
비채택하며, 80.79% 최고판은 유지한다.

결과 원본은
`adaptive_best_highest_moving_preclaim_reclaim_moving_def_margin0_vs_full_pool_10maps.csv`다.

### 필수 탈환 마진 +1 유지 + 이동 중 적 증원 1회 포함

마진 0이 증원 관측 전에 더 이른 소수 출격을 허용한 문제를 분리하기 위해,
80.79% 최고 후보의 필수 탈환 마진 +1을 유지하면서 이동 중 적 증원만 첫
출격 계산에 포함했다. 상대 미래 생산과 정지 예비대는 여전히 제외했다.
별도 wrapper 소스와 실행 파일은 각각
`110584_territory_push_adaptive_reinforce_highest_moving_preclaim_reclaim_moving_def_margin1.cpp`,
`110584_territory_push_adaptive_reinforce_highest_moving_preclaim_reclaim_moving_def_margin1_test.exe`다.

70784 / Pretest #21 / map 1558에서는 T34에 R68 방향 이동 중 적 2명을
탐지한 뒤 기존 4명 출격을 취소했다. T47까지 7명을 모아 출격했고, 기존
T178 본부 파괴 패배가 200턴 HQ 25 대 11 승리로 전환됐다.

그러나 공식 고유 맵 10개(seed 42), LEFT, pool 19모델, worker 16의
190판 결과는 **103승 31무 56패, 62.37%**로 크게 악화됐다. 최고 후보의
**151승 35무 4패, 88.68%**보다 승리 -48판, 무승부 -4판, 패배 +52판,
승점률 -26.31%p다. 이번 구현은 이동 중 병력의 실제 도착일을 시간축으로
처리하지 않고 첫 전투 시작부터 목표 주둔군에 합쳐 계산했다. 따라서 우리
공격대보다 늦게 오거나 다른 곳에서 멈출 수 있는 병력도 즉시 수비하는
것처럼 취급해 필요 파동을 과대평가하고 출격을 지나치게 늦춘 것이 주된
회귀 원인이다. 1,900판 평가는 생략하고 비채택하며 최고판은 유지한다.

결과 원본은
`adaptive_best_highest_moving_preclaim_reclaim_moving_def_margin1_vs_full_pool_10maps.csv`다.

## 출격 후 증원 롤아웃 하이퍼파라미터 스윕 (2026-08-27)

필수 거점의 첫 출격 계산과 +1명 마진은 80.79% 최고 후보 그대로 유지하고,
선발대 출격 후 `REINFORCE_ROLLOUT`만 분리해 조정했다. 기존 롤아웃에는
상대가 방어 반응을 보인 뒤 HQ에서 생산할 미래 수비대를 3턴만 생성하는
값이 있었고 별도 롤아웃 마진은 없었다. 이를 다음 두 하이퍼파라미터로
명시했다.

- `response_turns`: 반응 관측 뒤 상대 미래 수비 생산을 넣는 턴 수
- `survivor_margin`: 목표 점령 시 살아 있어야 하는 최소 아군 수. 첫 출격
  +1명 마진과는 별개이며 롤아웃의 성공/실패 판정에만 적용한다.

`response_turns={3,6,9,12}`, `survivor_margin={0,1,2}`의 12조합을 공식
고유 맵 10개(seed 42), LEFT, pool 19모델, worker 16에서 각각 190판씩
평가했다.

| 상대 생산 창 | 생존 마진 | 승 | 무 | 패 | 승점률 |
|---:|---:|---:|---:|---:|---:|
| 3 | 0 | 151 | 35 | 4 | 88.68% |
| 3 | 1 | 150 | 36 | 4 | 88.42% |
| 3 | 2 | 128 | 57 | 5 | 82.37% |
| 6 | 0 | 152 | 34 | 4 | **88.95%** |
| 6 | 1 | 150 | 36 | 4 | 88.42% |
| 6 | 2 | 131 | 54 | 5 | 83.16% |
| 9 | 0 | 147 | 40 | 3 | 87.89% |
| 9 | 1 | 146 | 40 | 4 | 87.37% |
| 9 | 2 | 129 | 54 | 7 | 82.11% |
| 12 | 0 | 141 | 46 | 3 | 86.32% |
| 12 | 1 | 137 | 49 | 4 | 85.00% |
| 12 | 2 | 123 | 61 | 6 | 80.79% |

생존 마진 1은 대부분 소폭 하락했고, 2는 출격 후 롤아웃을 지나치게 자주
실패로 바꿔 크게 악화됐다. 상대 생산 창도 9~12턴까지 늘리면 승리가
무승부로 전환되는 보수화가 커졌다. 미니풀 최적은 6턴/마진 0이었다.

6턴/마진 0을 고유 맵 100개, 총 1,900판으로 확대하자 **1291승 497무
112패, 81.03%**였다. 직전 3턴/마진 0 최고 후보의 **1287승 496무
117패, 80.79%**보다 승리 +4판, 무승부 +1판, 패배 -5판, 승점률
**+0.24%p**로 다시 최고 기록을 갱신했다. 상대별로 `91531` +3.5%p,
`87838`/`93978` +3.0%p, `107981`/`97302` +2.0%p,
`110584` +1.5%p 개선됐고, `70784`/`70833` -3.0%p,
`109203` -2.0%p, `83304` -1.5%p는 악화됐다.

채택 후보 소스와 실행 파일은 각각
`110584_highest_reinforce_resp6_surv0.cpp`,
`110584_highest_reinforce_resp6_surv0_test.exe`다. 스윕 요약은
`adaptive_best_highest_reinforce_hyperparameter_sweep_10maps.csv`, 전체
결과는 `adaptive_best_highest_reinforce_resp6_surv0_vs_full_pool_100maps.csv`다.

동일 고정 10맵 미니풀의 4패를 모두 리플레이와 디버그 로그로 생성했다.
상대는 `79541`, `91531`, `93978`, `109203`이며, 단일 선택형 HTML은
`adaptive_best_highest_resp6_surv0_pool_losses_10maps_replay.html`, 개별
HTML/로그 인덱스는
`adaptive_best_highest_resp6_surv0_pool_losses_10maps/index.html`이다.

### 공통 필수 거점 열세일 때만 필수 탈환 발동

81.03%의 6턴/마진 0 최고 후보를 기준으로, 양쪽 모두에게 필수인 공통
거점에서 내 소유 기지 수가 상대보다 적을 때만 필수 탈환 목표를 새로
잠그도록 변경했다. 현재 필수 조건은 자기 HQ까지의 hop이 상대 HQ까지의
hop보다 작거나 같은 것이므로, 양쪽 필수 집합의 교집합은 두 HQ까지의
hop이 정확히 같은 거점이다. 공통 집합의 내 기지 수와 상대 기지 수만
비교하고 빈 거점은 중립으로 셌다. 열세이면 상대가 건설했거나 점유한 공통
필수 거점 중 내 HQ와 가까운 곳을 목표로 잠그며, 동수이거나 우세하면 필수
탈환은 새로 시작하지 않는다. 빈 거점 확장과 일반 공세는 기존대로 뒀다.

별도 소스와 실행 파일은 각각
`110584_highest_resp6_shared_mandatory_deficit_reclaim.cpp`,
`110584_highest_resp6_shared_mandatory_deficit_reclaim_test.exe`다. 공식 고유
맵 10개(seed 42), LEFT, pool 19모델, worker 16의 190판 결과는 **72승
99무 19패, 63.95%**였다. 기준 최고 후보의 **152승 34무 4패, 88.95%**보다
승리 -80판, 무승부 +65판, 패배 +15판, 승점률 -25.00%p다. 공통 필수
집합이 대부분 중앙선의 소수 거점뿐이라 동수 상태가 길게 유지됐고, 기존에
적극적으로 되찾던 내 쪽 필수 거점에 대한 필수 탈환이 꺼져 일반 공세만으로
넘어가지 못하는 장기 교착이 크게 늘었다. 따라서 1,900판 평가는 생략하고
비채택하며 81.03% 최고판은 유지한다.

결과 원본은
`adaptive_best_highest_resp6_shared_mandatory_deficit_reclaim_vs_full_pool_10maps.csv`다.

### 양측 전체 필수 거점 소유 수에서 밀릴 때만 필수 탈환 발동

공통 필수 거점 교집합 대신, `내가 소유한 내 필수 거점 수`와 `상대가
소유한 상대 필수 거점 수`를 비교했다. 내 수가 상대 수보다 적을 때만 기존
필수 탈환을 새로 발동하며, 발동 후 목표는 원래 로직 그대로 상대가
건설했거나 점유한 내 필수 거점 전체에서 고르게 했다. 이미 잠근 목표는
기존처럼 아군 기지 완공까지 유지했다. 기준은 81.03%의 6턴/마진 0 최고
후보다.

별도 소스와 실행 파일은 각각
`110584_highest_resp6_mandatory_count_deficit_reclaim.cpp`,
`110584_highest_resp6_mandatory_count_deficit_reclaim_test.exe`다. 공식 고유
맵 10개(seed 42), LEFT, pool 19모델, worker 16의 190판 결과는 **79승
65무 46패, 58.68%**였다. 기준 최고 후보의 **152승 34무 4패, 88.95%**보다
승리 -73판, 무승부 +31판, 패배 +42판, 승점률 -30.26%p다.

거점 수가 같은 동안에는 상대가 내 필수 거점에 병력을 보내거나 전투를
시작해도 즉시 필수 탈환이 켜지지 않는다. 실제 아군 기지를 잃어 소유 수가
뒤처진 다음 턴에야 반응하므로 기존 선제 집결·탈환이 사라졌고, 상대가 먼저
자기 쪽 필수 거점을 확장한 것만으로도 수 비교가 흔들려 목표 유무와 발동
조건이 어긋났다. 따라서 1,900판 평가는 생략하고 비채택하며 81.03% 최고판은
유지한다.

결과 원본은
`adaptive_best_highest_resp6_mandatory_count_deficit_reclaim_vs_full_pool_10maps.csv`다.

### 상대 정지 집결군 대응 위치만 전 거점 급습 시뮬레이션으로 선택

6턴/생존 마진 0 최고판에서 상대 비-HQ 건물의 `work_cap` 초과 정지 병력을
집결군으로 감지하는 조건과, 그 머릿수만큼 `staging_defense`를 1:1로
예약하는 방식은 그대로 유지했다. 바꾼 것은 그 방어 예약을 어느 아군
건물에 둘지 결정하는 위치 선택 한 곳뿐이다.

각 아군 건물을 예비대 허브 후보로 놓고, 상대 집결군이 현재 보유한 모든
아군 건물을 각각 급습하는 독립 시나리오를 전투 시뮬레이션한다. 현재 각
건물의 HP·포탑·주둔군과 허브의 실제 잉여 예비대, 상대와 아군의 이동 hop을
반영한다. 모든 거점을 막는 후보끼리는 상대 집결군과 가까운 곳을 고르고,
그런 후보가 없으면 HQ 파괴 여부, 방어 실패 거점 수, 잃는 건물 가치,
대응 지연 순으로 피해가 작은 허브를 고른다.

공격 목표 선택, 공격 집결지, 필수 거점 탈환, 출격 병력 수, 경로,
공격 완공턴 계산은 전부 기존 최고판 그대로다. 최초 구현에서는 이 방어
허브를 공격 집결에도 재사용하고 공격 완공턴 기준 폴백까지 함께 넣어
**130승 44무 16패, 80.00%**로 크게 악화됐으며, 이는 요청 범위를 넘긴
변경이라 전부 제거했다.

범위를 정정한 `ALL_BASES_RALLY_SIM=1` 후보는
`110584_highest_resp6_all_bases_rally_sim.cpp` 및
`110584_highest_resp6_all_bases_rally_sim_test.exe`다. 공식 고유 맵 10개
(seed 42), LEFT, 고정 `pool` 19모델, worker 16의 190판 결과는 **142승
43무 5패, 승점률 86.05%**였다. 동일 소스를 매크로 OFF로 다시 빌드한
기준판은 기존 기록과 정확히 같은 **152승 34무 4패, 88.95%**를 재현했다.
따라서 위치 선택만으로 승리 -10판, 무승부 +9판, 패배 +1판, 승점률
**-2.89%p**다.

`91531`/`110584`에는 각각 +10.0%p, `93978`/`96816`/`97302`/`107981`에는
각각 +5.0%p 개선됐지만, `70784`/`70833`/`87838`에 각각 -15.0%p,
`108830`/`109203`/`110193`/`110496`에 각각 -10.0%p 하락했다. 결과 원본은
`adaptive_best_highest_resp6_all_bases_rally_location_only_vs_full_pool_10maps.csv`다.
전체 1,900판은 진행하지 않고 비채택하며 81.03% 최고판을 유지한다.

### 상대 집결군 최근접 대응에서 가장자리 기지 한 곳 제외

앞의 전 거점 급습 시뮬레이션판은 폐기하고, 6턴/생존 마진 0 최고판의
기존 `상대 정지 집결군에서 가장 가까운 아군 건물` 선택으로 돌아갔다.
여기에만 `EXCLUDE_OUTERMOST_RALLY_BASE`를 추가했다. 매 턴 아군이 소유한
비-HQ 기지 가운데 원점 `(0, 0)`에서 직선거리 제곱이 가장 큰 한 곳을
가장자리 기지로 정하고, 집결군 대응 위치 후보에서 그 기지만 제외한다.
HQ는 가장자리 판정 대상에서는 제외하지만 대응 위치 후보로는 그대로
남는다. 동률이면 region ID가 작은 한 곳만 제외한다.

상대 집결군 감지, 1:1 방어 인원 예약, 실제 이동 위협 시뮬레이션, 공격
목표·병력·집결지·경로와 필수 탈환은 변경하지 않았다. 별도 후보는
`110584_highest_resp6_exclude_outermost_rally_base.cpp` 및
`110584_highest_resp6_exclude_outermost_rally_base_test.exe`다.

공식 고유 맵 10개(seed 42), LEFT, 고정 `pool` 19모델, worker 16의
190판 결과는 **150승 36무 4패, 승점률 88.42%**였다. 최고판의 **152승
34무 4패, 88.95%**와 비교해 승리 2판이 무승부가 되어 **-0.53%p**다.
`84206`에는 +5.0%p 개선됐고 `87838`, `93978`에는 각각 -5.0%p
하락했으며 나머지 16모델은 동일했다. 결과 원본은
`adaptive_best_highest_resp6_exclude_outermost_rally_base_vs_full_pool_10maps.csv`다.

미니풀 차이가 작아 같은 조건을 공식 고유 맵 100개, 총 1,900판으로
확대했다. 결과는 **1303승 483무 114패, 승점률 81.29%**로 기존 최고판의
**1291승 497무 112패, 81.03%**보다 승리 +12판, 무승부 -14판, 패배
+2판, 승점률 **+0.26%p**다. `110193` +1.5%p,
`84206`/`83284`/`97302`/`107981`/`108830` +1.0%p 등이 개선됐고,
`109203` -2.5%p, `79541` -1.5%p, `91531` -0.5%p가 악화됐다.
결과 원본은
`adaptive_best_highest_resp6_exclude_outermost_rally_base_vs_full_pool_100maps.csv`다.
패배는 2판 늘었지만 전체 승점률이 기존 최고를 넘어 이 후보를 새 최고판으로
채택한다.

### 선택 공세 목표를 첫 출격 전까지 매 턴 재선정

새 최고판의 선택 공세 목표를 한 번 고른 뒤 고정하지 않고, 첫 공격 병력이
출발하기 전까지 매 턴 기존 후보 점수로 다시 고르게 했다. 필수 거점 탈환과
첫 출격 이후의 목표 고정은 그대로 두었으며, 목표가 바뀔 때 이미 공세용으로
확보한 병력 수는 유지했다. 별도 후보는
`110584_highest_resp6_exclude_outermost_rally_reselect_push.cpp` 및
`110584_highest_resp6_exclude_outermost_rally_reselect_push_test.exe`다.

문제가 드러났던 `vs 79541 / Pretest #14 / map 1281 / battle 122747`에서는
기존 T45의 R58 목표를 계속 잠그지 않았다. T51에 R64가 더 빠른 후보로
바뀌었고, T56에 4명이 출격했다. 기존에는 200턴 무승부 점수 판정으로
패배했지만 이 후보는 T164에 상대 HQ를 파괴해 승리했다. 진단 로그는
`diagnostic_reselect_push_vs79541_t57/debug_A.txt`다.

그러나 공식 고유 맵 10개(seed 42), LEFT, 고정 `pool` 19모델, worker
16의 190판 결과는 **128승 54무 8패, 승점률 81.58%**였다. 현재 최고판의
**150승 36무 4패, 88.42%**보다 승리 -22판, 무승부 +18판, 패배 +4판,
승점률 **-6.84%p**다. `91531`과 `93978`에는 각각 +5.0%p였지만,
`83304` -25.0%p, `70784`/`70833`/`83284` 각각 -20.0%p,
`84206`/`96816`/`97302`/`107981` 각각 -10.0%p로 크게 악화됐다.

후보 점수의 1턴 차이와 병력 변화가 매 턴 달라지는 상황에서, 이미 집결 중인
병력을 유지한 채 목표만 다시 고르면 집결 방향과 필요 병력이 반복해서
바뀐다. 실제 진단판에서도 첫 공세 뒤 R64→R58→R64→R58 등 여러 차례
재선정됐다. 매 턴 탐색 자체보다 전환 비용·최소 유지 기간이 없는 탐욕적
재선정이 문제이므로 1,900판은 생략하고 비채택한다. 현재 최고판과 그 소스는
변경하지 않는다. 결과 원본은
`adaptive_best_highest_resp6_exclude_outermost_rally_reselect_push_vs_full_pool_10maps.csv`다.

### 선택 공세의 실제 출격 직전에만 목표 재선정

매 턴 재선정판의 목표 진동을 없애기 위해, 집결 중에는 최초 목표를 그대로
유지하고 실제 파동을 출격할 수 있게 된 턴에만 상대 거점 전체를 최신
병력·수비 상태로 다시 비교했다. 같은 목표가 다시 선택되면 즉시 출격하고,
다른 목표가 선택되면 그 턴의 출격을 취소한 뒤 이미 커밋한 병력을 유지한
채 다음 턴부터 새 목표로 재집결한다. 필수 거점 탈환과 출격 후 연속 증원은
기존 그대로다.

별도 후보는
`110584_highest_resp6_exclude_outermost_rally_prelaunch_reselect.cpp` 및
`110584_highest_resp6_exclude_outermost_rally_prelaunch_reselect_test.exe`다.
`vs 79541 / Pretest #14 / map 1281 / battle 122747`에서는 실제 출격 조건이
충족된 T59에 다시 비교했지만, 그 시점에는 R58의 수비가 이미 줄어 R58이
여전히 `ready=3, force=4`의 최선 후보였다. 따라서 목표를 바꾸지 않았고
원래와 같은 200턴 HQ 점수 패배가 유지됐다.

공식 고유 맵 10개(seed 42), LEFT, 고정 `pool` 19모델, worker 16의
190판 결과는 **147승 39무 4패, 승점률 87.63%**였다. 현재 최고판의
**150승 36무 4패, 88.42%**보다 승리 3판이 무승부로 바뀌어 승점률
**-0.79%p**다. `93978`과 `110584`에는 각각 +5.0%p였지만,
`109203` -10.0%p, `79541`/`110193`/`110496` 각각 -5.0%p였다.

매 턴 재선정의 큰 회귀는 막았지만 문제 판을 해결하지 못했다. 요청에 따라
같은 조건을 공식 고유 맵 100개, 총 1,900판으로 확대했으며 결과는 **1266승
518무 116패, 승점률 80.26%**였다. 현재 최고판의 **1303승 483무 114패,
81.29%**보다 승리 -37판, 무승부 +35판, 패배 +2판, 승점률 **-1.03%p**다.
따라서 비채택한다. 결과 원본은 각각
`adaptive_best_highest_resp6_exclude_outermost_rally_prelaunch_reselect_vs_full_pool_10maps.csv`,
`adaptive_best_highest_resp6_exclude_outermost_rally_prelaunch_reselect_vs_full_pool_100maps.csv`다.

### 실제 병력 위치·도착일 기반 선택 공세 출격 목표 재계산

정적 출격 직전판은 목표의 현재 주둔군만 보고 `force=4`를 계산해, 곧바로
합류할 상대 전투 예비대를 놓쳤다. 이를 고쳐 실제 출격 가능한 순간마다 현재
집결지에 서 있는 아군을 실제 위치에서 각 후보로 보내고, 다음 상대 병력을
각자의 실제 도착일에만 전투 시간축에 넣었다.

- 후보에 현재 주둔한 상대 병력
- 관측된 이동 방향이 후보로 이어지는 상대 병력
- 상대 건물의 `work_cap`을 넘는 정지 전투 예비대
- 상대 건물 방향으로 복귀 중인 병력 중 도착 후 `work_cap`을 채우고 남는
  전투 예비대

반대로 다른 아군 건물을 향해 이동 중인 상대 공세 병력은 후보 수비대로
순간이동시키지 않았다. 현재 실제 집결 병력을 1명씩 늘려 전투를 이기는 최소
파동과 함락일을 구하고, 함락일이 가장 빠른 후보를 선택했다. 어느 후보도
현재 파동으로 확보하지 못하면 출격하지 않고 한 명을 더 집결한 뒤 다시
계산한다. 미래 아군 생산은 첫 파동의 승리 전력으로 미리 넣지 않았다.

별도 후보는
`110584_highest_resp6_exclude_outermost_rally_precise_prelaunch_target.cpp` 및
`110584_highest_resp6_exclude_outermost_rally_precise_prelaunch_target_test.exe`다.
`vs 79541 / Pretest #14 / map 1281 / battle 122747`의 T59에서는 기존 R58을
거부하고, 실제 시간축에서 4명으로 T+7에 확보 가능하다고 나온 R32로 목표를
바꿨다. R58에서 4명이 전멸하던 기존 결과가 사라져 **HQ 15:20 패배에서
HQ 20:20 무승부**로 바뀌었다. 재현 파일은
`replay_precise_prelaunch_vs_79541_pretest14_map1281.html`과
`debug_A_precise_prelaunch_vs_79541_pretest14_map1281.txt`다.

공식 고유 맵 100개(seed 42), LEFT, 고정 `pool` 19모델, worker 16의
1,900판 결과는 **1284승 510무 106패, 승점률 81.00%**였다. 현재 최고판의
**1303승 483무 114패, 81.29%**보다 승리 -19판, 무승부 +27판, 패배 -8판,
승점률 **-0.29%p**다. `91531` +7.0%p, `70784`/`70833` 각각 +6.0%p,
`84206` +5.0%p, `87838` +1.5%p, `79541`/`109203` 각각 +1.0%p였고,
`97302`/`107981` 각각 -6.0%p, `93978` -5.0%p, `96816` -4.0%p,
`110584` -3.0%p 등이 악화됐다.

정밀 출격 게이트는 패배를 8판 줄였지만 승리 19판을 줄여 총 승점률은 새
최고판보다 근소하게 낮다. 따라서 실험 후보로 보존하되 현재 최고판은 그대로
유지한다. 결과 원본은
`adaptive_best_highest_resp6_exclude_outermost_rally_precise_prelaunch_target_vs_full_pool_100maps.csv`다.

### 출격 직전 재선정 목표로 같은 턴 직행

정밀 출격판은 현재 집결지 병력이 새 목표로 즉시 직행하는 시간축으로 목표를
선택했지만, 실제 실행에서는 목표만 바꾼 뒤 그 턴을 끝냈다. 다음 턴부터 새
목표 최근접 아군 거점으로 다시 집결했기 때문에 시뮬레이션과 실행 경로가
달랐다. 이를 고쳐 출격 직전 목표가 바뀌면, 정밀 시뮬레이션에 넣었던 최소
파동을 바로 그 턴 현재 집결지에서 새 목표로 직행시켰다.

`vs 93978 / Pretest #16 / map 1248 / battle 138111`의 T96에서 기존 정밀판은
R49→R36으로 재선정한 뒤 아무도 움직이지 않았고, T97~T99에 R20으로 다시
모여 T100에야 R36으로 출격했다. 직행판은 T96에 R34의 5명을 R36으로 바로
보냈다. R36 건설은 T104에서 T101로 3턴 빨라졌고, 결과는 **HQ 25:25
무승부에서 HQ 25:16 승리**로 바뀌었다. 비교 리플레이는
`precise_prelaunch_direct_draw_to_win_vs_93978_pretest16_map1248/paired_draw_to_win_replay.html`다.

별도 실행 파일은
`110584_highest_resp6_exclude_outermost_rally_precise_prelaunch_target_direct_test.exe`다.
공식 고유 맵 100개(seed 42), LEFT, 고정 `pool` 19모델, worker 16의
1,900판 결과는 **1331승 460무 109패, 승점률 82.16%**였다. 직행 전 정밀판의
**1284승 510무 106패, 81.00%**보다 승리 +47판, 무승부 -50판, 패배 +3판,
승점률 **+1.16%p**다. 이전 최고판의 **1303승 483무 114패, 81.29%**와
비교하면 승리 +28판, 무승부 -23판, 패배 -5판, 승점률 **+0.87%p**다.

이전 최고판 대비 `70784`/`70833` 각각 +7.5%p, `91531` +5.5%p,
`87838` +4.5%p, `84206` +3.5%p, `79541` +3.0%p, `109203` +2.5%p,
`110584` +1.0%p가 개선됐다. `97302`/`107981` 각각 -3.5%p,
`93978` -3.0%p, `96816` -2.5%p 등은 악화됐다. 전체 승점률과 패배 수가
모두 이전 최고보다 좋아 이 직행판을 새 최고판으로 채택한다. 결과 원본은
`adaptive_best_highest_resp6_exclude_outermost_rally_precise_prelaunch_direct_vs_full_pool_100maps.csv`다.

### BASE 우위일 때 필수 거점 탈환 필요 병력 마진 +2

기존 필수 거점 탈환의 첫 출격 인원은 전투 시뮬레이션이 계산한 최소 필요
병력에 **1명을 더한 값**이었다. 이 마진을 생존 마진이나 출격 후 증원 마진과
혼동하지 않고, 다음과 같이 첫 출격 필요 병력에만 조건부로 적용했다.

- 내 비본부 `BASE` 수가 상대보다 많지 않으면 기존처럼 `최소 필요 병력 +1`
- 내 비본부 `BASE` 수가 상대보다 많으면 `최소 필요 병력 +2`
- 선택 공세와 빈 거점 확보에는 적용하지 않음

별도 후보는
`110584_highest_precise_prelaunch_direct_mandatory_margin2_if_base_ahead.cpp`이며,
기본 소스에서는 `MANDATORY_MARGIN_TWO_WHEN_BASE_AHEAD` 매크로로 분리했다.

공식 고유 맵 100개(seed 42), LEFT, 고정 `pool` 19모델, worker 16의
1,900판 결과는 **1331승 463무 106패, 승점률 82.24%**였다. 직전 최고판의
**1331승 460무 109패, 82.16%**와 비교하면 승리는 같고, 무승부 +3판,
패배 -3판, 승점률 **+0.08%p**다.

직전 최고판 대비 `79541` +2.0%p, `70784`/`70833`/`84206` 각각
+1.5%p, `83304`/`109203` 각각 +1.0%p가 개선됐다. 반면 `110193`
-2.0%p, `108830` -1.5%p, `87838`/`97302`/`107981`/`110496` 각각
-1.0%p, `110584` -0.5%p가 악화됐다. 전체 개선 폭은 작지만 같은 승리 수에서
패배가 3판 줄었으므로 이 조건부 마진판을 새 최고판으로 채택한다. 결과 원본은
`adaptive_best_highest_precise_prelaunch_direct_mandatory_margin2_if_base_ahead_vs_full_pool_100maps.csv`다.

### 상대 생산으로 상쇄되는 +2 대기 취소

앞 실험의 조건부 `+2`는 집결 계획에서는 최소 필요 병력에 2명을 더했지만,
실제 출격 직전 재검사가 고정 `+1`을 다시 사용하고 있었다. 따라서 실제
출격까지 무조건 `+2`를 유지하는 대조판과, 추가 한 명을 기다리는 가치가
있을 때만 `+2`를 유지하는 판을 분리했다.

새 출격 직전 판단은 내 비본부 `BASE` 수가 더 많고 `+1` 파동은 지금 바로
출격할 수 있지만 `+2` 파동은 아직 준비되지 않았을 때만 작동한다.

1. 집결지 밖의 실제 공격 후보, 작전에 커밋되어 이동 중인 병력, 현재
   골드·순수입으로 새로 훈련할 병력 중 추가 한 명의 가장 빠른 집결일을
   계산한다.
2. `+1` 즉시 출격과 `+2` 지연 출격의 이동 및 전투 시간축을 각각 돌려
   예상 함락일을 구한다.
3. 상대의 현재 추정 골드, 순수입, HQ 훈련 한도와 상대 HQ→목표 이동 시간을
   사용해 각 함락일까지 새로 생산하여 목표에 도착시킬 수 있는 수비병 수를
   계산한다.
4. `+2` 쪽에서 상대 도착 병력도 한 명 이상 늘면 기다림이 상쇄되므로
   기존 `+1` 파동을 즉시 출격시킨다. 상대 병력이 늘지 않거나 추가 한 명이
   이미 집결해 지연이 없을 때만 `+2`를 유지한다.

디버그 로그에는 `RECLAIM_MARGIN2_TIMING`으로 `MARGIN2_NOW`, `WAIT_MARGIN2`,
`PLUS1_NOW`와 양쪽 예상 함락일·상대 생산 도착 수를 남긴다.

공식 고유 맵 100개(seed 42), LEFT, 고정 `pool` 19모델, worker 16의 대조
결과는 다음과 같다.

- 출격 직전에도 무조건 `+2`: **1329승 466무 105패, 82.21%**
- 상대 생산으로 상쇄될 때 `+1` 즉시 출격: **1335승 460무 105패, 82.37%**

상쇄 판은 무조건 `+2`보다 승리 +6판, 무승부 -6판, 패배 동일,
승점률 **+0.16%p**다. 직전 최고였던 집결 계획만 조건부 `+2`인 판의
**1331승 463무 106패, 82.24%**와 비교해도 승리 +4판, 무승부 -3판,
패배 -1판, **+0.13%p**다. 원래 `+1` 직행 최고판의 **1331승 460무
109패, 82.16%**보다는 승리 +4판, 패배 -4판, **+0.21%p**다.

무조건 `+2` 대비 `84206`/`110496` 각각 +1.0%p,
`91531`/`93978`/`110584` 각각 +0.5%p가 좋아졌고, `83284`만 -0.5%p였다.
나머지는 동일했다. 상대도 같은 시간에 한 명을 늘릴 수 있는 대기를 취소하는
조건 자체가 순이득으로 확인됐으므로 이 판을 새 최고판으로 채택한다.

후보와 대조 실행 파일은 각각
`110584_highest_precise_prelaunch_direct_mandatory_margin2_unmatched_test.exe`,
`110584_highest_precise_prelaunch_direct_mandatory_margin2_always_launch_test.exe`다.
결과 원본은 각각
`adaptive_best_highest_precise_prelaunch_direct_mandatory_margin2_unmatched_vs_full_pool_100maps.csv`,
`adaptive_best_highest_precise_prelaunch_direct_mandatory_margin2_always_launch_vs_full_pool_100maps.csv`다.

### 63,000칸 BASE 전투표로 상대 증원 전 파괴 판단

현재 최고판의 필수 거점 탈환은 현재 주둔군을 실제 `combatDay`로 이기는
최소 병력에 +1 또는 조건부 +2를 붙이지만, 그 병력이 BASE를 파괴하는 데
몇 턴이 걸리는지는 첫 출격 인원 결정에 직접 쓰지 않았다. 이를 보완하기
위해 다음 5차원 전투표를 사전 계산했다.

- 내 HQ 1~5 × 상대 HQ 1~5
- 상대 BASE 1~3
- 상대 상주 병력 0~20명
- 내 출격 병력 1~40명
- 총 63,000칸, 각 칸에 BASE 파괴 턴과 아군 생존자 수 저장

각 칸은 상위 8비트에 파괴 턴, 하위 8비트에 생존자 수를 넣은
`uint16_t`이며, 실패는 `0xffff`다. 실행 파일에서는 126,000바이트의 읽기
전용 정적 데이터가 되고 런타임 파일 입출력 없이 O(1)로 조회한다. 생성된
헤더는 `base_combat_lookup_table.inc`, 생성기는
`combat_lookup_table/generate_base_combat_lookup.cpp`다.

실험 후보는 현재 최고판에 `BASE_REINFORCEMENT_RACE_LOOKUP=1`을 추가한
`110584_highest_precise_prelaunch_direct_mandatory_margin2_unmatched_base_race_lookup.cpp`다.
필수 BASE의 첫 출격 직전에 다음 조건이 모두 맞을 때만 표를 사용한다.

1. BASE와 양측 병력이 모두 해당 레벨의 완전한 HP이고, 현재 교전 중인
   아군이 없으며 상주군/출격군이 표 범위 안이다.
2. 현재 목표 방향으로 이동 중인 상대 병력, 다른 상대 건물의 노동 초과
   정지 예비대, 빈 지역의 정지 야전 병력 중 가장 빠른 실제 합류일을 구한다.
3. `내 도착일 + 표의 파괴 턴 - 1 < 상대 증원 도착일`이고 아군이 최소
   1명 생존하는 가장 작은 파동을 출격시킨다.
4. 표의 전제와 실제 상태가 다르거나 증원 전 파괴 답이 없으면 기존
   `plan_attack_force`의 +1/조건부 +2 판단으로 폴백한다.

디버그 로그는 `RECLAIM_BASE_RACE`로 출격 인원, 전투 턴, 양쪽 도착일,
생존자와 즉시 골드 순가치를 남긴다. 공통 코드 272,830바이트, 표 헤더
391,469바이트, 래퍼 382바이트로 합계 **664,681바이트**라 1MiB 소스 제한
아래다. `-O2` Windows 실행 파일은 **552,900바이트**로 10MiB 제한에도
충분히 들어온다. 아직 1,900판 성적 검증은 수행하지 않았다.

110584 원본과 공식 고유 맵 100개(seed 42)를 양쪽 진영으로 번갈아 총
200판 대전한 결과는 **131승 57무 12패, 승점률 79.75%**였다. 동일 조건의
직전 최고판은 **132승 56무 12패, 80.00%**이므로 승리 1판이 무승부로
바뀌어 **-0.25%p**다. LEFT 100판만 보면 수정판은 **68승 26무 6패,
81.00%**, 직전 최고판은 **69승 25무 6패, 81.50%**였고, RIGHT 100판은
양쪽 모두 **63승 31무 6패, 78.50%**로 같았다.

결과가 바뀐 것은 `Pretest #16 / map 333 / battle-136186` 한 판뿐이다.
직전 최고판은 200턴 종료 시 HQ 25:20 승리였지만 전투표판은 HQ 25:25
무승부였다. 결과 원본은
`base_race_lookup_vs_110584_100maps_both_sides.csv`,
`margin2_unmatched_baseline_vs_110584_100maps_both_sides.csv` 및 양쪽의
`*_left_100maps_games.csv`, `*_left_100maps_summary.json`이다. 아직 전체 풀
1,900판은 수행하지 않았다.

#### BASE 파괴 후 RAID 철수 / 불가피한 야전 HOLD

첫 전투표판은 BASE HP가 0이 되는 즉시 `CAPTURE`를 반환해, 이미 목표로
이동 중인 상대 증원과의 파괴 후 야전 전투를 보지 않았다. 이를 다음 두
경로로 분리했다.

- 파괴 다음 판단 시점에 상대가 아직 목표에 없고 실제 이동 병력이 오고
  있으며, 표의 즉시 순가치가 양수라면 생존자 전원을 가장 가까운 아군
  건물로 이동시킨다. 아군 건물이 최종 목적지인 이동은 무료이며, 동시
  이동이므로 같은 턴 목표로 들어오는 상대와 엇갈려 전투를 피한다.
- 현재 목표에서 이미 적과 맞물려 이동이 봉쇄됐거나 철수 조건이 안 되면
  `postBreachHold`를 유지한다. 이때 야전 롤아웃은 예약된 마지막 상대
  증원이 도착할 때까지 성공을 반환하지 않으며, 새 증원은 경유지 없이
  실제 전투 지역으로 직행한다. 현재 교전군과 관측 이동군을 모두 제거한
  뒤에만 `SECURED`로 바꾸고 건설 단계로 넘어간다.

RAID 철수 뒤 바로 같은 필수 거점을 재공격하면 철수 의미가 없어지므로,
관측된 마지막 이동 증원이 목표에 도착한 다음 턴까지 재교전과 건설을
유예하고 그 뒤 최신 상주군으로 다시 판단한다. 로그는
`RECLAIM_BASE_RAID action=EVACUATE/HOLD/SECURED/REENGAGE`로 남긴다.

`Pretest #16 / map 333 / battle-136186`에서는 T108에 R51 BASE를 파괴한
뒤, T109에 생존 A9/A13/A20 3명을 R36으로 전원 철수시켰다. 이동 중인
상대 5명의 최초 도착은 당일, 마지막 도착은 2턴 뒤였고 서로 엇갈려 실제
전투를 피했다. 재현 파일은
`base_race_raid_evac_cooldown_vs_110584_pretest16_map333/raid_evac_cooldown.html`과
동일 경로의 `.debug.txt`다. 최종 결과는 HQ 25:25 무승부로 전투표 첫판과
같다.

110584 원본과 공식 고유 맵 100개(seed 42), 양쪽 진영 총 200판 결과는
**129승 59무 12패, 승점률 79.25%**였다. RAID/HOLD 전 전투표판의
**131승 57무 12패, 79.75%**보다 승리 2판이 무승부가 되어 **-0.50%p**,
직전 최고판의 **132승 56무 12패, 80.00%**보다 **-0.75%p**다. 요청한
철수/HOLD 동작은 재현됐지만 110584 단독 성적은 개선되지 않았다. 결과는
`base_race_raid_hold_cooldown_vs_110584_100maps_both_sides.csv`다. 전체 풀
1,900판은 아직 수행하지 않았다.

### 상대 증원 전 양수 교환을 독립 수익 공세로 적용 (2026-08-28)

HQ 레벨 우위나 상대의 다음 HQ 업그레이드 시점에만 묶지 않고, 선택 공세의
실제 출격 판단 때 다음 조건을 만족하는 BASE 타격은 항상 실행하도록 했다.

1. 집결지에 실제로 도착한 병력을 체력 내림차순으로 1명씩 늘려, 현재 HP의
   적 주둔군과 손상 상태를 포함한 BASE를 `combatDay`로 직접 시뮬레이션한다.
2. 목표 방향으로 이미 이동 중인 적, 다른 적 건물의 `work_cap` 초과 정지
   예비대, 건물 밖 정지 야전 병력이 합류하는 가장 빠른 실제 도착일을 구한다.
3. `내 도착일 + 전투 턴 - 1 < 적 증원 도착일`이고 최소 1명이 생존해야 한다.
   증원이 없으면 200턴까지 남은 시간 안에 파괴되는지만 본다.
4. 즉시 순가치는
   `적 주둔군 수×120 + BASE 누적 투자비 - 내 전사자 수×120 - 출격 인원×10`
   으로 계산하며, 이 값이 0보다 큰 경우만 허용한다.
5. 모든 증원까지 이기는 완전 점령 해가 있으면 기존 완전 점령을 우선한다.
   그런 해가 없을 때만 양수 교환 후보 중 즉시 순가치가 가장 큰 곳으로
   같은 턴 직행한다. 파괴 뒤에는 기존 RAID 철수/불가피한 야전 HOLD 상태를
   그대로 사용한다.

공통 코드는 `PROFITABLE_PRE_REINFORCEMENT_RAID` 매크로로 분리했고, 현재
82.37% 최고판에 이 변경만 더한 래퍼는
`110584_highest_precise_prelaunch_direct_mandatory_margin2_unmatched_profitable_raid.cpp`다.
공통 소스와 래퍼 합계는 289,386바이트, `-O2 -static -s` 실행 파일은
1,345,536바이트로 각각 1MiB/10MiB 제한 안이다.

공식 고유 맵 100개(seed 42), LEFT, **고정 `pool`의 19개 실행 파일**,
worker 16으로 1,900판을 평가한 결과는 다음과 같다.

| 판 | 승 | 무 | 패 | 승점률 |
|---|---:|---:|---:|---:|
| 직전 최고 | 1335 | 460 | 105 | 82.37% |
| 증원 전 양수 교환 | **1391** | **406** | **103** | **83.89%** |

승리 +56판, 무승부 -54판, 패배 -2판, 승점률 **+1.53%p**다. 상대별로
`110193` +5.5%p, `108830` +5.0%p, `97302`/`107981` +3.0%p,
`109203`/`110584`/`84206` 각각 +2.5%p가 좋아졌다. `91531`과 `79541`만
각각 -1.5%p였다. 특히 `110584`는 **69승 25무 6패, 81.5%**에서
**74승 20무 6패, 84.0%**로 바뀌었다. 같은 100맵의 paired 변경은
무승부→승리 8판, 승리→무승부 3판이다.

결과 원본은
`adaptive_best_highest_profitable_pre_reinforcement_raid_vs_fixed_pool_100maps.csv`,
paired 비교 리플레이는
`highest_profitable_raid_vs_110584_changed_replays_100maps/paired_changed_replays.html`이다.
`Pretest #16 / map 333 / battle-136186`은 양수 교환판에서도 기존 최고와
같이 HQ 25:20 승리를 유지한다.

검증 중 `evaluate_all_models.py`가 `pool/110584.exe` 대신 제출 소스를 새로
컴파일한 해시가 다른 실행 파일을 상대하게 해 45.5%로 표시된 결과가 한 번
나왔으나, 이는 고정 pool 평가가 아니므로 채택 비교에서 제외했다. 위 표와
상대별 수치는 모두 `pool/*.exe`를 직접 사용해 다시 계산한 값이다.

### 파괴 직후 빈 거점의 실제 이동 증원 대응 (2026-08-28)

`vs_70784 / Pretest #21 / map 1558 / battle-167791`의 T57에는 R37에
집결한 5명이 R54로 직행했다. 출격 직전 로그는
`capture=4 send=5 defenders=12 survivors=5`였고, 실제로도 T61에 R54
BASE를 파괴할 때 5명 전원이 생존했다. 따라서 이 판의 5명 산정은 BASE
파괴 기준으로 부족하지 않았다.

패배 원인은 파괴 뒤였다. 기존 전투 시간축은 BASE HP가 0이 되는 즉시
성공을 반환했고, 다음 턴부터 생존자들은 일반 `best_help`와 후속 공세의
유휴 병력으로 다시 분류됐다. 그 결과 원본은 T63~64에 R54의 5명을 모두
R37/R29 방면으로 빼 버렸고, 이후 도착한 B17이 빈 R54에 남아 T70에 BASE를
재건했다. 즉 전역 병력을 공격용/방어용으로 고정한 문제가 아니라, 방금
파괴한 빈 거점의 실제 접근 병력에 대한 최소 점유 예약이 없던 문제였다.

이를 `HOLD_EMPTY_CAPTURE_AGAINST_INCOMING`으로 다음과 같이 보완했다.

1. 매 턴 건물이 없고 아군만 서 있는 모든 거점에 대해, 직전 지역→현재
   지역의 이동 방향이 그 거점의 최단 경로와 일치하는 상대 이동 병력 HP를
   합산한다. 기존 `threat_hps`는 아군 건물만 목표로 삼으므로 별도 계산한다.
2. 현재 아군 병력의 실제 HP와 접근 중인 상대의 실제 HP로 포탑 없는 야전
   전투를 반복해, 상대를 전멸시키고 아군이 최소 1명 생존하는 최소 잔류
   인원을 계산한다.
3. 체력이 높은 순서로 그 인원만 `best_help`와 이후 유휴 공격/확장 후보에서
   제외한다. 적 이동 방향이 바뀌거나 전투가 끝나면 다음 턴 즉시 예약을
   다시 계산하므로 불필요한 고정 수비대는 만들지 않는다.

수정 재현에서는 T62~63에 `incoming=3 keep=4`, T64에
`incoming=1 keep=2`가 계산됐다. A10/A11 두 명이 R54에 남아 B17을
제거한 뒤 예약이 풀렸고, 상대의 R54 재건도 막았다. 같은 판은 기존의
T190 HQ 파괴 패배에서 **200턴 종료 HQ 25:5 승리**로 바뀌었다. 재현 파일은
`empty_capture_hold_vs_70784_pretest21_map1558/empty_capture_hold.html`과
동일 경로의 `.debug.txt`다.

공식 고유 맵 100개(seed 42), LEFT, 고정 `pool` 19모델, worker 16의
1,900판 결과는 다음과 같다.

| 판 | 승 | 무 | 패 | 승점률 |
|---|---:|---:|---:|---:|
| 직전 양수 교환 최고 | 1391 | 406 | 103 | 83.89% |
| 빈 거점 실제 증원 잔류 | **1395** | **412** | **93** | **84.26%** |

승리 +4판, 무승부 +6판, 패배 -10판, 승점률 **+0.37%p**다. `70784`는
86승 11무 3패에서 **88승 10무 2패**로 승점 +1.5점 개선됐다. 상대별
최대 개선은 `83304` +3.0점, `87838` +2.5점이며, `108830`은 -2.5점,
`110584`/`110193`은 각각 -1.5점 악화됐다. 전체 승점과 패배 수가 모두
좋아 이 판을 새 최고판으로 채택한다. 결과 원본은
`adaptive_best_highest_profitable_raid_empty_capture_hold_vs_fixed_pool_100maps.csv`다.

### 필수 거점 강제 탈환 제거 실험 (2026-08-28)

`vs_70784 / Pretest #21 / map 1558`의 T34 R68 출격처럼, 필수 거점
분기가 일반 공세보다 먼저 목표를 고르고 현재 상주군 위주로 필요 병력을
산정하는 문제가 보여 필수 거점 관련 로직을 세 단계로 분리해 시험했다.

먼저 필수 거점 출격 직전마다 관측 이동 증원까지 끝까지 기다리는 정밀
시뮬레이션을 붙였다. 문제의 R68 판은 필요 병력이 4명에서 6명, 다시 7명으로
갱신되어 HQ 25:20 승리로 바뀌었지만, 계속 생산·이동하는 증원을 매 턴 새로
포함하면서 출격이 지나치게 지연됐다. 고정 pool 1,900판 결과는
**1138승 376무 386패, 승점률 69.79%**였으므로 폐기했다. 재현 파일은
`mandatory_precise_gate_vs_70784_pretest21_map1558/mandatory_precise_gate.html`,
전체 결과는
`adaptive_best_highest_profitable_raid_empty_hold_mandatory_precise_gate_vs_fixed_pool_100maps.csv`다.

다음으로 필수 거점 표시 자체를 전부 제거했다. 이 표시는 강제 탈환뿐 아니라
초반 건설·확장 우선순위에도 사용되므로 경제가 붕괴했고, 결과는
**0승 2무 1898패, 승점률 0.05%**였다. 문제의 R68 판도 156일에 패배했다.
전체 결과는
`adaptive_best_highest_profitable_raid_empty_hold_no_mandatory_vs_fixed_pool_100maps.csv`다.

마지막으로 전략적 필수 거점 표시와 확장 판단은 유지하되, 그 거점을 일반
공세보다 먼저 `territoryReclaimTarget`으로 강제 승격시키는 분기만 껐다.
문제의 R68 판은 183일 HQ 파괴에서 200턴 종료 HQ 15:20 패배로 완화됐지만,
고정 pool 1,900판 전체 결과는 다음처럼 크게 악화됐다.

| 판 | 승 | 무 | 패 | 승점률 |
|---|---:|---:|---:|---:|
| 현재 최고 | 1395 | 412 | 93 | 84.26% |
| 강제 탈환만 제거 | 704 | 709 | 487 | 55.71% |

특히 `110584` 상대 승점이 82.5점에서 31.0점, `110193`은 84.0점에서
26.0점으로 떨어졌다. 강제 탈환은 일부 과소 병력 공격을 만들지만, 이를
없애면 일반 공세가 중요한 축을 제때 되찾지 못해 무승부와 패배가 훨씬 많이
발생한다. 따라서 세 변형 모두 폐기하고 **84.26% 최고판을 유지**한다.
강제 탈환만 제거한 래퍼는
`110584_highest_profitable_raid_empty_hold_no_forced_mandatory_reclaim.cpp`,
문제 판 재현은
`no_forced_mandatory_reclaim_vs_70784_pretest21_map1558/no_forced_mandatory_reclaim.html`,
결과는
`adaptive_best_highest_profitable_raid_empty_hold_no_forced_mandatory_reclaim_vs_fixed_pool_100maps.csv`다.

### 최고판 필수 거점 탈환 실패-경기 패배 상관 분석 (2026-08-28)

현재 채택 최고판을 공식 고유 맵 100개(seed 42), LEFT, 고정 `pool`
19모델에 다시 실행했다. 진단 로그만 추가했으며 결과는 기존과 정확히 같은
**1395승 412무 93패, 승점률 84.26%**여서 전략 동작이 바뀌지 않았음을
확인했다.

여기서 "필수 탈환 목표 전투 패배"는 필수 탈환 파동이 실제 목표 지역에
도착해 적과 교전했고, 거점을 확보하기 전에 해당 작전에 묶인 공격 병력이
전멸한 경우로 정의했다. 단순 목표 선정, 집결 중 경기 종료, 출격하지 않은
경우는 전투 패배로 세지 않았다.

- 필수 탈환 조건은 1,900판 중 **1,555판(81.84%)**에서 발동했다. 발동판은
  1084승 378무 93패로 패배율 5.98%였고, 미발동 345판은 311승 34무
  0패였다. 그러나 발동 자체가 워낙 흔해 패배 원인을 특정하는 조건으로는
  정밀도가 낮다.
- 실제 필수 탈환 출격은 **1,349판**에서 있었다.
- 목표 전투 패배는 그중 **11판(0.82%)**뿐이었고 최종 결과는
  **1승 2무 8패**, 경기 패배율 **72.73%**였다.
- 출격은 했지만 목표 전투 패배가 없었던 1,338판은 83패, 패배율
  **6.20%**였다. 따라서 출격판끼리 비교한 패배 위험비는 **11.72배**,
  오즈비는 **40.32**, 이진 상관계수 phi는 **0.239**다.
- 전체 93패 중 이 패턴이 있었던 것은 **8패(8.60%)**다. 즉 발생하면 매우
  치명적이지만, 현재 전체 패배의 대부분을 설명하는 일반 원인은 아니다.

전투 패배 11판은 `84206` 2패, `91531` 3패, `93978` 2패,
`110496` 1패, `97302`/`107981` 각각 1무, `109203` 1승이었다.
따라서 데이터상 필수 탈환 조건 자체를 제거할 근거는 약하고, 실제 출격
직전 필요 병력 산정 또는 첫 파동 전멸 후 대응을 이 11개 사례에 한정해
고치는 편이 맞다. 이 수치는 상관 분석이므로, 이미 불리한 판에서 탈환
전투도 함께 실패했을 가능성을 배제하는 인과 증명은 아니다.

경기별 원본은
`mandatory_reclaim_correlation_highest_pool_100maps.csv`, 요약은
`mandatory_reclaim_correlation_highest_pool_100maps.json`, 전투 패배
11판만 추린 표는
`mandatory_reclaim_battle_failed_cases_highest_pool_100maps.csv`다. 재현
스크립트는 `analyze_mandatory_reclaim_correlation.py`다.

위 11판은 개별 리플레이와 디버그 로그로 다시 기록했으며, 한 파일에서
선택해 볼 수 있는 묶음은
`mandatory_reclaim_battle_failed_replays_highest_pool_100maps_replay.html`이다.
개별 파일과 매니페스트는
`mandatory_reclaim_battle_failed_replays_highest_pool_100maps/`에 있다.

### 필수 탈환 최초 출격의 이동 수비군 1회 스냅샷 (2026-08-28)

`vs_84206 / Pretest #15 / map 353`의 R46 탈환에서는 T54에 B10이 이미
R46 방향으로 이동 중이었고 아군 파동보다 2턴 먼저 도착하는데도, 최초
출격의 정적 `plan_attack_force`는 R46 상주 B8 한 명만 세어 4명을
출격시켰다. 이동 수비군을 보는 롤아웃은 출격 다음 턴부터 시작하므로 이미
명령한 첫 파동을 되돌릴 수 없었다.

이를 다음처럼 수정했다.

1. 정적 계획으로 첫 파동이 실제 출격 가능한 최초 순간에만 스냅샷을
   만든다.
2. 목표 상주군은 0일, 현재 이동 방향이 목표 최단 경로와 일치하는 관측
   이동군은 실제 남은 이동일에 합류시켜 `simulate_reinforcement_stream`을
   실행한다. 미래 생산과 정지 예비대는 넣지 않는다.
3. 관측 이동군까지 제거하고 점령하는 최소 인원에 기존 마진을 더한 필요
   인원을 해당 파동 동안 고정한다. 매 턴 새 증원을 더해 출격을 끝없이
   미루지 않는다.
4. 한 명을 더 기다리는 동안 상대도 증원하는 문제를 피하기 위해, 집결지
   건물의 노동 예약 인원만 필요한 만큼 즉시 빌린다. 다른 거점 노동자는
   건드리지 않는다.

첫 구현은 노동자를 빌리지 않아 더 이른 R29에서 4명 준비 후 5번째 병력을
3턴 기다렸고, 문제 판이 T139 HQ 파괴로 더 악화됐다. 즉 이동군 반영보다
출격 지연의 손해가 더 컸다. 노동자 즉시 동원까지 넣은 최종판은 R29에서
T20에 5명을 바로 출격했고, R46에서도 `target_def=1 moving_def=1`,
`minimum=4 margin=1 required=5 borrowed=1`로 5명을 지연 없이 출격했다.
문제 판은 기존 **200턴 HQ 10:20 패배**에서 **200턴 HQ 15:10 승리**로
바뀌었다. 재현 파일은
`mandatory_mover_snapshot_vs_84206_pretest15_map353/mandatory_mover_snapshot.html`과
동일 경로의 `.debug.txt`다.

공식 고유 맵 100개(seed 42), LEFT, 고정 `pool` 19모델, worker 16의
1,900판 결과는 다음과 같다.

| 판 | 승 | 무 | 패 | 승점률 |
|---|---:|---:|---:|---:|
| 직전 최고 | 1395 | 412 | 93 | 84.26% |
| 이동 수비군 스냅샷 | **1365** | **474** | **61** | **84.32%** |

승리는 30판 줄고 무승부는 62판 늘었지만 패배가 32판 줄어 총 승점은
1601점에서 **1602점으로 1점 증가**했다. 상대별로 `91531` +8.0점,
`93978` +4.5점, `109203` +3.5점이 좋아졌고, `110584` -3.5점,
`110496` -3.0점이 악화됐다. 총 승점이 소폭이나마 좋아지고 필수 탈환
전멸 위험이 크게 줄어 이 판을 새 최고판으로 채택한다. 결과 원본은
`adaptive_best_highest_mandatory_mover_snapshot_vs_fixed_pool_100maps.csv`다.

새 최고판의 패배 표본 5판은 `110584` 상대의 전체 2패와 서로 다른 상대
3판(`70784`, `83284`, `84206`)으로 구성했다. 통합 리플레이는
`mandatory_mover_snapshot_loss_samples_5_replay.html`, 개별 HTML과 디버그
로그는 `mandatory_mover_snapshot_loss_samples_5/`에 있다.

### 선택 공세 출격 경로의 이동군 충돌 차단 (2026-08-28)

`vs_70784 / Pretest #23 / map 1558 / battle 187238`에서는 T50에 R33의
4명을 R50으로 보냈지만, B7~B13 7명이 R44에서 R33으로 이미 이동하고
있었다. 두 파동은 다음 이동일에 중간 지역 R38에서 만났고 4명이 전멸했다.
기존 정밀 출격 시뮬레이션은 목표 R50에 합류할 수비군만 계산했으므로,
다른 목적지로 이동하는 적과의 **중간 경로 충돌**은 정상적으로 보지
못했다.

선택 공세의 출격 직전 목표 비교에 다음 검사를 추가했다.

1. 아군 파동은 집결지에서 후보 목표까지, 관측된 적 이동군은 현재 위치에서
   추정 목적지까지 하루씩 최단 경로로 전진시킨다.
2. 아군이 목표에 도달하기 전에 같은 날 같은 중간 지역에 도착하면 해당
   후보를 그 턴에만 제외한다. 목표 지역 전투는 기존 정밀 전투
   시뮬레이션이 계속 담당한다.
3. 전체 공세를 취소하지 않고 남은 후보를 다시 비교한다. 다음 턴에는 최신
   위치로 전부 재판단한다.

문제 판 T50에는 R50과 R64가 `day=0, region=R38, enemies=7`로 제외됐고,
충돌하지 않는 R65가 선택됐다. 결과는 기존 **150일 아군 HQ 파괴 패배**에서
**200턴 HQ 20:18 승리**로 바뀌었다. 재현 파일은
`optional_push_route_guard_vs_70784_pretest23_map1558/route_guard.html`과
동일 경로의 `.debug.txt`다.

공식 고유 맵 100개(seed 42), LEFT, 고정 `pool` 19모델, worker 16의
1,900판 결과는 다음과 같다.

| 판 | 승 | 무 | 패 | 승점 | 승점률 |
|---|---:|---:|---:|---:|---:|
| 직전 최고 | 1365 | 474 | 61 | 1602 | 84.3158% |
| 경로 충돌 차단 | **1371** | **470** | **59** | **1606** | **84.5263%** |

총 승점은 **+4점**, 패배는 **-2판**이다. `70784`와 동일 계열 `70833`은
각각 +1점이며, `110584`도 60승 38무 2패에서 61승 37무 2패로 +0.5점
개선됐다. 전체 승점과 패배 수가 모두 좋아 이 로직을 새 최고판에
채택했다. 결과 원본은
`adaptive_best_highest_mandatory_mover_snapshot_route_guard_vs_fixed_pool_100maps.csv`다.

### 불가능한 필수 탈환 목표 잠금 해제 축소 (2026-08-28)

가장 가까운 상대 정지 예비대 한 곳과 목표로 이동 중인 적을 포함한
낙관적 시뮬레이션에서도 점령 후 생존자가 없으면, 필수 탈환 목표 잠금을
풀 수 있게 실험했다. 무조건 해제하면 기존 행동을 지나치게 많이 바꾸므로
최종적으로 다음 세 조건을 동시에 만족할 때만 해제했다.

1. 예비대 포함 시뮬레이션 결과가 `FAIL`이다.
2. 현재 병력만으로는 부족하여 추가 생산량 `train`이 0이 아니다.
3. 적이 없는 중립 필수 거점이라는 대체 행동이 존재한다.

공식 고유 맵 100개, 고정 pool 19모델의 1,900판에서 최종 축소안은
**1380승 466무 54패, 1613점(84.8947%)**으로 경로 충돌 차단판의
1606점보다 **+7점**, 패배는 **-5판**이었다. 따라서 이 축소안을 현재
최고판으로 채택했다. 결과는
`adaptive_best_highest_route_guard_mandatory_reserve_release_neutral_train_vs_fixed_pool_100maps.csv`다.

대조군은 무조건 해제안이 1593.5점, 중립 대안만 요구한 안이 1588.5점이었다.
공격 예비대 사전 생산을 추가한 두 실험도 각각 1607점과 1601.5점으로
최고판보다 낮아 모두 폐기했다.

### 필수 탈환 출격 직전 정지 예비대 포함 실험 (2026-08-28)

현재 최고판의 `vs 110584 / Pretest #15 / map 353`에서는 T55 예비대
재검사가 `planned=4 reserve=2 train=0 survivors=0`, 즉 `FAIL`을 정확히
냈다. 하지만 잠금 해제 조건이 `train != 0`을 요구해 실패를 무시했고,
T56 출격 스냅샷은 R46 상주군과 이동 중인 적만 세어
`minimum=3 + margin=1 = 4`명으로 출격했다. R57의 정지 예비대 2명은
다음 턴 출발해 아군과 같은 날 R46에 도착했다.

실험판은 출격 스냅샷에 목표까지 가장 가까운 상대 건물 한 곳의 정지
예비대를 실제 hop만큼 늦게 합류시켰다. 또한 이전 스냅샷의 요구 인원이
모인 순간, 실제 이동 명령 전에 최신 예비대로 다시 계산하도록 했다.
문제 판에서는 T56 필요량이 4명에서 7명으로 올랐고, 7명이 모인 T63에는
늘어난 예비대를 반영해 다시 9명으로 올려 오출격을 막았다. 해당 판은
**패배에서 무승부**로 바뀌었다.

그러나 110584 상대 동일 100맵 결과는 다음처럼 악화됐다.

| 판 | 승 | 무 | 패 | 승점률 |
|---|---:|---:|---:|---:|
| 현재 최고판 | 66 | 33 | 1 | 82.5% |
| 정지 예비대 출격 포함 | 51 | 48 | 1 | 75.0% |

승패 전환은 `L→D` 1판, `D→L` 1판, `D→W` 10판, `W→D` 25판이었다.
예비대가 계속 생산되는 경우 필요 병력도 계속 증가하여 공격을 지나치게
억제하는 부작용이 더 컸다. 1,900판은 진행하지 않았고 이 실험은
**비채택**이다. 결과는 `nearest_launch_reserve_vs_110584_100_games.csv`와
`nearest_launch_reserve_vs_110584_100_summary.json`에 있다.

### BASE 수 우위 시 강제 필수 탈환 미발동 (2026-08-28)

필수 탈환이 일반 공세와 다른 목표 잠금·병력 산정 경로를 사용한다는
불일치를 줄이기 위해, 신규 목표를 고르는 순간 `내 BASE 수 > 상대 BASE
수`이면 강제 필수 탈환을 시작하지 않고 이후 일반 공세 판단에 맡기는
조건을 최고판에 시험했다. 이미 시작한 탈환은 중간에 해제하지 않았다.

110584 상대 동일 100맵 결과는 기존 **66승 33무 1패, 82.5%**에서
**65승 33무 2패, 81.5%**로 1점 악화됐다. 전환은 `W→D` 5판,
`D→W` 4판, `D→L` 1판이었다. 또한 문제의 Pretest #15 / map 353은
필수 탈환이 시작된 T48에 BASE 수가 2:2였으므로 이 조건의 적용 대상이
아니며 그대로 패배한다.

공식 고유 맵 100개, 고정 pool 19모델의 1,900판 결과는 다음과 같다.

| 판 | 승 | 무 | 패 | 승점 | 승점률 |
|---|---:|---:|---:|---:|---:|
| 현재 최고판 | 1380 | 466 | 54 | 1613 | 84.8947% |
| BASE 우위 시 필수 탈환 생략 | 1314 | 532 | 54 | 1580 | 83.1579% |

총 승점은 **-33점**으로 크게 악화됐고 패배 수는 54판으로 같았다.
`70784`/`70833`은 각각 -6.5점, `84206` -4.5점, `91531` -4점이었다.
반대로 `65205`/`65257`/`109203`은 각각 +1.5점, `87838`은 +1점이었다.
필수 탈환을 일반 공세에 넘기는 방향은 일부 상대에는 효과가 있었지만,
BASE 수 우위 하나로 생략하는 현재 조건은 승리를 대량으로 무승부로
바꾸므로 **비채택**한다. 결과는
`adaptive_best_highest_skip_mandatory_when_base_ahead_vs_fixed_pool_100maps.csv`,
110584 단독 결과는 `skip_mandatory_when_base_ahead_vs_110584_100_games.csv`와
동일 접두사의 요약 JSON에 있다.

### 선택 공세 목표 매턴 재선정 + 첫 목표 최근접 집결지 고정 (2026-08-28)

일반 선택 공세의 목표를 병력 준비 중에도 매턴 다시 고르되, 병력이 목표를
따라 왕복하지 않도록 첫 목표에 가장 가까운 아군 건물을 공통 집결지로 한 번
고정했다. 후보 목표의 준비 시간과 출격 직전 정밀 시뮬레이션도 이 고정
집결지를 출발점으로 계산했다.

`vs 79541 / Pretest #14 / map 1281`에서는 목표가 R58에서 R64로 바뀌어도
집결지는 R38로 유지되어 과거의 목표-집결지 왕복은 사라졌다. 110584 상대
100맵은 현재 최고판의 66승 33무 1패에서 **68승 31무 1패**로 1점
좋아졌다. 그러나 고정 pool 19모델 1,900판에서는 다음처럼 악화됐다.

| 판 | 승 | 무 | 패 | 승점 | 승점률 |
|---|---:|---:|---:|---:|---:|
| 현재 최고판 | 1380 | 466 | 54 | 1613 | 84.8947% |
| 첫 목표 최근접 집결지 고정 | 1282 | 554 | 64 | 1559 | 82.0526% |

총 승점 **-54점**, 패배 **+10판**이다. 특히 `87838` -8점,
`108830`/`110193` 각각 -6점, `110496` -5.5점으로 악화됐다. 첫 목표가
변두리일 때 그 목표의 최근접 건물을 전체 공세의 출발점으로 고정하는 것이
이후 다른 후보까지의 거리와 대응력을 망가뜨렸다. **비채택**이며 결과는
`adaptive_best_highest_dynamic_optional_push_fixed_rally_vs_fixed_pool_100maps.csv`,
110584 단독 결과는 `dynamic_optional_push_fixed_rally_vs_110584_100_games.csv`다.

### 선택 공세 HQ-중앙 균형 집결지 실험 (2026-08-28)

위 실험의 변두리 고정을 피하기 위해 집결지를 공격 목표와 완전히 분리했다.
아군 건물마다 `HQ까지 hop`과 `중앙 거점까지 hop`을 구해 두 값의 최댓값을
최소화하고, hop 합과 실제 거리 합으로 타이브레이크했다. 선택한 집결지는
작전 동안 고정하며, 매턴 목표 비교와 실제 출격 직전 시뮬레이션이 모두 같은
집결지를 사용한다.

기존 고정 집결지 실험이 패배한 `vs 87838 / Pretest #25 / map 97`에서는
집결지가 변두리 R20에서 HQ와 중앙 R37 사이의 R10으로 바뀌었고 결과도
**패배에서 무승부**로 회복됐다. 하지만 집결지가 대부분의 전선에서 너무
뒤쪽이 되어 전반적인 출격 속도가 크게 떨어졌다.

110584 상대 100맵은 현재 최고판의 66승 33무 1패에서
**47승 52무 1패**로 9.5점 악화됐다. 고정 pool 19모델 1,900판 결과는
다음과 같다.

| 판 | 승 | 무 | 패 | 승점 | 승점률 |
|---|---:|---:|---:|---:|---:|
| 현재 최고판 | 1380 | 466 | 54 | 1613 | 84.8947% |
| HQ-중앙 균형 집결지 | 913 | 890 | 97 | 1358 | 71.4737% |

총 승점 **-255점**, 패배 **+43판**으로 명확한 퇴행이다. `79541`
-27.5점, `70784`/`70833` 각각 -27점, `83284` -20.5점이었다. 집결지를
목표에서 완전히 분리해 HQ-중앙 중간에 두는 방식은 **비채택**한다. 최고판
래퍼와 실행 파일은 변경하지 않았다. 실험 결과는
`adaptive_best_highest_dynamic_optional_push_hq_center_rally_vs_fixed_pool_100maps.csv`,
110584 단독 결과는 `dynamic_optional_push_hq_center_rally_vs_110584_100_games.csv`,
대표 재현은 `hq_center_rally_vs_87838_pretest25_map97/replay.html`에 있다.

### 선택 공세 중앙 최근접 집결지 실험 (2026-08-28)

HQ-중앙 균형안에서 HQ 거리를 완전히 제거하고, 일반 선택 공세의 공통
집결지를 현재 아군 건물 중 `중앙 거점까지 hop`이 가장 짧은 곳으로 골랐다.
hop이 같을 때만 중앙까지의 실제 거리로 타이브레이크했다. 목표는 준비 중
매턴 재선정하며, 목표 비교와 출격 직전 시뮬레이션은 모두 이 집결지를
출발점으로 사용한다.

110584 상대 동일 100맵은 현재 최고판의 66승 33무 1패, 82.5점에서
**74승 25무 1패, 86.5점**으로 4점 좋아졌다. 그러나 고정 pool 19모델
1,900판 결과는 다음과 같다.

| 판 | 승 | 무 | 패 | 승점 | 승점률 |
|---|---:|---:|---:|---:|---:|
| 현재 최고판 | 1380 | 466 | 54 | 1613 | 84.8947% |
| 중앙 최근접 집결지 | 1318 | 526 | 56 | 1581 | 83.2105% |

HQ-중앙 균형안의 1358점보다는 223점 회복했고, 첫 목표 최근접 고정안의
1559점보다도 22점 높다. 하지만 현 최고판에는 **-32점**, 패배는 +2판이다.
상대별로 `110584` +4점, `65205`/`65257` 각각 +1점,
`97302`/`107981` 각각 +0.5점이지만, `87838` -8.5점, `84206` -5점,
`93978` -4점, `79541` -3.5점이었다.

`vs 87838 / Pretest #25 / map 97`에서는 중앙 R37 자체가 아군 건물이어서
집결지가 R37로 잡혔고, HQ-중앙 균형안의 R10 집결 때 무승부였던 판이 다시
**HQ 15:20 패배**가 됐다. 중앙에만 최대한 붙이는 것도 상대별 균형을
해치므로 **비채택**한다. 최고판은 변경하지 않았다. 전체 결과는
`adaptive_best_highest_dynamic_optional_push_center_rally_vs_fixed_pool_100maps.csv`,
110584 단독 결과는 `dynamic_optional_push_center_rally_vs_110584_100_games.csv`,
대표 재현은 `center_rally_vs_87838_pretest25_map97/replay.html`에 있다.

### HQ 노동 보충 신규 생산 전환 (2026-08-28)

`vs 93978 / Pretest #24 / map 1255`에서 T61 HQ를 L2로 업그레이드한
직후, R41에 도착한 공격 집결 병력 A5·A6 중 A5를 부족한 HQ 노동력
보충용으로 회군시켰다. A10과 B10이 빈 R44에서 교전 중이었는데도 A5는
HQ로, A6은 R20 집결지로 향해 상대 B11의 R44 재선점을 막지 못했다.

평시 HQ 노동력 부족은 다른 거점의 병력을 이동시켜 채우지 않고,
`missing_workers`의 HQ 신규 생산 수요로 남기도록 바꿨다. 다만 실제 HQ
위협이 감지된 경우에는 기존 병력의 방어 증원 이동을 계속 허용한다.

문제 판에서는 A5를 HQ로 빼지 않고 A5·A6 모두 공격 후보로 유지해 기존
**HQ 20:25 패배**가 **HQ 20:20 무승부**로 바뀌었다. 진행 중인 야전
교전을 별도로 감지해 R44로 직행시키는 실험까지 함께 넣으면 이 판은
HQ 25:20 승리였지만, 결합안의 전체 성적은 1607.5점으로 낮아 직행안은
채택하지 않았다.

공식 고유 맵 100개(seed 42), LEFT, 고정 pool 19모델, worker 16의
1,900판 결과는 다음과 같다.

| 판 | 승 | 무 | 패 | 승점 | 승점률 |
|---|---:|---:|---:|---:|---:|
| 직전 최고 | 1380 | 466 | 54 | 1613 | 84.8947% |
| HQ 노동 신규 생산 | **1401** | **446** | **53** | **1624** | **85.4737%** |

총 승점은 **+11점**, 패배는 **-1판**이다. `91531` +4.5점,
`96816` +3.5점, `97302`/`107981` 각각 +2점, `110584` +1.5점이
개선됐다. 반대로 `110496` -2.5점, `93978`/`87838` 각각 -2점이었다.
전체 승점과 패배 수가 모두 좋아 이 규칙을 새 최고판에 **채택**했다.
결과는 `adaptive_best_highest_train_hq_labor_only_vs_fixed_pool_100maps.csv`,
문제 판 재현은 `hq_labor_train_vs_93978_pretest24_map1255/replay.html`에 있다.

### 일반 총공세 출격 전 정밀 시뮬레이션 통일 (2026-08-28)

기존 선택 PUSH는 실제 출격 직전에 `PUSH_PRECISE_PRELAUNCH`로 현재 병력
위치, 이동 중인 적, 각 상대 거점의 정지 예비대와 도착일을 넣은 시간축
전투를 다시 계산했다. 반면 일반 총공세는 `ATTACK_TARGET_RESPONSE`의
대응 시간 점수와 현재 주둔군 정적 전투만 통과하면 바로 출격해, 정밀
시뮬레이션이 막았을 공격을 같은 턴의 다른 경로가 다시 내보낼 수 있었다.

일반 총공세도 집결 병력이 실제 출발 가능한 순간에는 동일한 시간축 계산으로
모든 상대 거점을 비교하도록 `PRECISE_ALL_OFFENSE_AT_LAUNCH`를 추가했다.
확보 가능한 목표가 없으면 `OFFENSE_PRECISE_PRELAUNCH best=R-1`로 출격을
막고, 작은 파동을 버리지 않은 채 한 명을 더 모아 다음 턴 다시 계산한다.
상대 HQ 직접 공격은 기존 `DIRECT_HQ_FEASIBLE` 검증을 계속 사용한다.

`vs 93978 / Pretest #24 / map 1255`에서는 T115의 단순 목표 점수가 R63을
`capture=6, response=5, slack=-1`로 골랐지만, 새 정밀 게이트는 실제 적
증원 시간축으로 `best=R-1`을 반환해 R41의 7명 출격을 차단했다. 기존
**HQ 20:20 무승부**가 **HQ 25:20 승리**로 바뀌었다. 재현 파일은
`unified_prelaunch_vs_93978_pretest24_map1255/replay.html`과 같은 폴더의
`debug_A.txt`다.

공식 고유 맵 100개(seed 42), LEFT, 고정 pool 19모델, worker 16의
1,900판 결과는 다음과 같다.

| 판 | 승 | 무 | 패 | 승점 | 승점률 |
|---|---:|---:|---:|---:|---:|
| 직전 HQ 노동 신규 생산 | 1401 | 446 | 53 | 1624 | 85.4737% |
| 일반 공세 정밀 출격 통일 | 1400 | 447 | 53 | 1623.5 | 85.4474% |
| 정밀 실패 시 완전 취소 실험 | 1401 | 445 | 54 | 1623.5 | 85.4474% |

통일안은 직전보다 **-0.5점**이지만 패배 수는 53판으로 동일하다. 실패 시
공격 훈련까지 완전히 끄는 실험은 같은 승점이면서 패배가 54판으로 늘고
문제 판도 다시 무승부가 되어 폐기했다. 출격 검증의 불일치를 제거하고 문제
판의 확정 손실을 막는 첫 통일안을 적용했다. 전체 결과는
`adaptive_best_highest_unified_prelaunch_vs_fixed_pool_100maps.csv`, 폐기한
완전 취소 대조군은
`adaptive_best_highest_unified_prelaunch_block_vs_fixed_pool_100maps.csv`다.

### 빈 거점 미래 교전 선제 증원 (2026-08-28)

`vs 93978 / Pretest #24 / map 1255`에서는 T55 아군 A10이 빈 필수 거점
R44를 건설하러 출발했고, 상대 B10도 같은 거점 방향으로 이동 중이었다.
하지만 기존 위협 감지는 이미 존재하는 아군 건물만 대상으로 삼았고, 빈
거점 HOLD도 아군이 먼저 도착해 점거한 뒤에만 작동했다. 그 결과 T58에
R28의 A5·A6이 유휴 상태였는데도 둘 다 R41 공세 집결로 빠지고, A10과
B10은 T60 R44에 동시에 도착해 상호 전멸했다.

최고판에 `REINFORCE_FUTURE_EMPTY_STRONGHOLD_BATTLE`을 추가해 다음처럼
처리했다.

1. 빈 거점을 직접 목표로 이동 중인 아군 `BUILD` 병력과, 직전 지역에서
   현재 지역으로 이동한 방향이 그 거점의 최단 경로와 일치하는 상대 이동
   병력을 찾는다.
2. 양쪽의 현재 HP와 실제 남은 hop을 도착일별 야전 시뮬레이션에 넣는다.
   같은 턴 또는 상대가 한 턴 뒤 도착하는 임박 충돌만 대상으로 하며, 먼
   후속 병력은 기존 `REINFORCE_RESPONSE_LOOKAHEAD=6` 안에서만 포함한다.
3. 기존 파동만으로 이기면 보내지 않는다. 패배면 비김 이상을 만드는 최소
   인원만 보내며, 이미 상호 전멸 무승부라면 그 결과도 허용한다. 다만
   현 거점의 `need`를 모두 남긴 뒤에도 제때 도착할 잉여가 있으면 신규
   생산 없이 한 명을 보내 승리 쪽으로 기울인다. 별도 `+1` 안전 마진은
   붙이지 않는다.
4. 기존 공세용 `ATTACK` 병력을 먼저 재활용하고, 선택된 병력은 경유지 없이
   미래 전장으로 직행한다. 다른 빈 거점의 건설 담당자와 상대 HQ 직접
   공격 파동은 빼지 않는다.

문제 판에서는 T58에
`FUTURE_EMPTY_BATTLE target=R44 own_eta=3 enemy_eta=3 min=1 send=1
draw_upgrade=1`이 기록됐다. A5 한 명만 R28에서 R44로 직행하고 A6은 기존
R41 공세에 남았다. R44 전투에서 아군이 살아남아 T65 BASE를 건설했으며,
최종 결과는 **HQ 25:20 승리**였다. 재현은
`future_empty_battle_vs_93978_pretest24_map1255/replay.html`과 같은 폴더의
`debug_A.txt`다.

공식 고유 맵 100개(seed 42), LEFT, 고정 `pool` 19모델, worker 16의
1,900판 결과는 다음과 같다.

| 판 | 승 | 무 | 패 | 승점 | 승점률 |
|---|---:|---:|---:|---:|---:|
| 직전 일반 공세 정밀 출격 통일 | 1400 | 447 | 53 | 1623.5 | 85.4474% |
| 빈 거점 미래 교전 선제 증원 | **1426** | **431** | **43** | **1641.5** | **86.3947%** |

총 승점은 **+18점**, 패배는 **-10판**이다. `93978` +4점,
`110496` +3.5점, `83284`/`83304` 각각 +2.5점, `84206`/`109203`
각각 +2점, `110584` +1.5점으로 개선됐다. 반대로 `91531`과 `87838`은
각각 -1.5점, `70784`/`70833`은 각각 -0.5점이었다. 전체 승점과 패배 수가
모두 크게 좋아 이 규칙을 새 최고판에 **채택**했다. 결과 원본은
`adaptive_best_highest_future_empty_battle_vs_fixed_pool_100maps.csv`다.

### 승리한 빈 거점 유지와 후속파 대응 (2026-08-28)

`vs 65205 / Pretest #19 / map 184`의 T55를 다시 계측했다. 로그의
`PASSIVE_HQ me=L5`는 현재 HQ 레벨이 아니라 남은 턴 동안 경제 행동만 했을
때의 **예상 최종 레벨**이다. 실제 T55 HQ는 L1이며, 당시 병사 HP 상한은
4, HQ 일자리와 턴당 훈련 상한은 각각 1이다.

기존 방어 계산은 R37의 HP 4 병사와 HP 1 병사를 모두 만피 신병 두 명으로
치환했다. 그래서 T55에 방어 수요 2명, 현지 병력 2명으로 닫혀 HQ 증원을
보내지 않았다. 별도로, 빈 거점 야전에서 승리한 생존자는 다음 턴부터 즉시
일반 유휴 병력으로 풀렸기 때문에 관측된 상대 후속파보다 건설/증원이 먼저
가능한 상황도 유지하지 못했다.

`HOLD_WON_EMPTY_STRONGHOLD`은 다음처럼 동작한다.

1. 직전 턴에 양측 병력이 있던 빈 거점에서 현재 아군만 남으면 야전 승리
   상태로 기록한다. 건설에 성공하거나 아군 점유가 끝날 때까지 유지한다.
2. 상대 이동 방향이 그 빈 거점을 향할 때 병사별 현재 HP와 실제 남은 hop을
   후속파 도착일로 넣는다. 이미 그 거점으로 이동 중인 아군 증원도 실제 HP와
   ETA로 넣는다.
3. 현재 골드와 순수입으로 계산한 BASE 건설 가능일을 같은 시간축에 넣고,
   건설 턴에는 이동/전투보다 먼저 L1 BASE와 포탑을 적용한다.
4. 현재 생존자만으로 후속파를 막으면 전원을 그대로 둔다. 못 막으면 다른
   거점의 노동/방어 수요를 제외한 정지 예비대를 ETA와 HP 순으로 추가하여
   승리 가능한 최소 인원만 빈 거점으로 직행시킨다. 끝까지 못 막으면 최소
   한 명을 남겨 상대 한 명의 공짜 점유를 막고, 이후 실제 교전 방어 판단에
   넘긴다.

공격 시뮬레이션 호출부도 확인했다. 이미 존재하는 공격 병력과 수비 병력은
각 `Warrior.hp`를 그대로 시간축 입력에 넣고 있었고, 아직 존재하지 않는
미래 생산 병력만 해당 HQ 레벨의 신병 HP를 사용한다. 이 부분은 정확해서
변경하지 않았다.

기존 건물 방어의 주둔군도 손상 HP를 그대로 넣는
`PRECISE_DEFENSE_CURRENT_HP`를 구현해 분리 시험했다. 단, 성공 조건은 기존과
같이 건물 생존으로 두었으며 수비병 생존 마진은 추가하지 않았다. 공식 고유
맵 100개(seed 42), LEFT, 고정 pool 19모델, worker 16 결과는 다음과 같다.

| 판 | 승 | 무 | 패 | 승점 | 승점률 | 판정 |
|---|---:|---:|---:|---:|---:|---|
| 직전 최고 | 1426 | 431 | 43 | 1641.5 | 86.3947% | 기준 |
| 실제 방어 HP만 | 1391 | 457 | 52 | 1619.5 | 85.2368% | 비채택 |
| 승리 빈 거점 유지/후속파만 | **1437** | **419** | 44 | **1646.5** | **86.6579%** | 채택 |
| 두 변경 결합 | 1394 | 455 | 51 | 1621.5 | 85.3421% | 비채택 |

정확한 방어 HP는 규칙상 더 정확하지만 현재의 도착 시간·재배치 정책과
결합했을 때 직전 최고보다 22점 낮아 기본 최고판에서는 플래그를 켜지 않았다.
반면 승리 빈 거점 유지안은 **+5점**으로 새 최고다. 110584 상대는
기존 69승 30무 1패(84점)에서 **74승 25무 1패(86.5점)**로 +2.5점이다.

문제 맵을 `pool/65205.exe`와 라이브로 다시 대전했을 때 기존판은
200턴 HQ 10:15 패배였고, 채택판은 **200턴 HQ 20:10 승리**로 바뀌었다.
재현은 `won_empty_hold_vs_65205_pretest19_map184/replay.html`, 전체 결과는
`adaptive_best_highest_won_empty_hold_vs_fixed_pool_100maps.csv`에 있다.

### 상대의 빈 필수 거점 선점 예측 (2026-08-28)

상대 이동 병력이 현재 비어 있는 아군 필수 거점을 향하고 있고, 상대가
도착 즉시 BASE를 지을 수 있으며 아군은 그 건설 시점까지 도달할 수 없을
때 필수 거점 탈환 준비를 미리 시작하는 `ANTICIPATE_MANDATORY_PRECLAIM`을
구현했다. 예측 상태에서는 병력을 생산ㆍ집결하되, 상대가 실제로 도착하여
점유하기 전에는 출격하지 않는다. 상대가 경로를 바꾸거나 조건이 깨지면
예측 목표를 즉시 해제한다. 실제 점유가 확인되면 기존 필수 거점 탈환으로
승격하며, 공격 시뮬레이션은 상대가 지을 L1 BASE와 이동 병력의 현재 HP를
포함한다.

감지 범위를 세 단계로 시험했다.

| 조건 | 승 | 무 | 패 | 승점 | 승점률 | 최고판 대비 |
|---|---:|---:|---:|---:|---:|---:|
| 넓은 후보 감지 | 1371 | 460 | 69 | 1601 | 84.2632% | -45.5 |
| 다음 거점ㆍ도착 즉시 건설로 제한 | 1411 | 424 | 65 | 1623 | 85.4211% | -23.5 |
| 연속 이동 경로상 목표가 하나로 확정될 때 | 1411 | 446 | **43** | 1634 | 86.0000% | -12.5 |
| 현재 최고판 | **1437** | 419 | 44 | **1646.5** | **86.6579%** | 기준 |

경로 확정판은 패배를 한 판 줄였지만 승리가 26판 줄고 무승부가 27판
늘어 총 승점이 12.5점 하락했다. 필수 거점 선점을 일찍 막는 효과보다
다른 확장ㆍ공세에 쓸 병력을 미리 묶는 비용이 더 컸으므로 세 버전 모두
**비채택**했다. 코드는 실험 플래그로 남기되 최고판 wrapper에서는 끈다.
각 결과 원본은 `anticipated_mandatory_preclaim_vs_fixed_pool_100maps.csv`,
`anticipated_mandatory_preclaim_strict_vs_fixed_pool_100maps.csv`,
`anticipated_mandatory_path_confirmed_vs_fixed_pool_100maps.csv`다.

### 필수 거점 탈환 중 신규 거점 병행 확보 (2026-08-28)

기존 최고판은 적이 점유한 필수 거점의 탈환 상태가 활성화되면 빈 거점
확보 패스 전체를 중단한다. `PARALLEL_EXPANSION_DURING_MANDATORY_RECLAIM`은
이 차단을 풀어 같은 턴에도 신규 거점 후보 선정, 빌더 파견 및 필요 신병
생산을 허용한다. 다만 이미 탈환 작전에 커밋됐거나 `ATTACK` 목적인 병력은
빌더 후보에서 제외해 탈환 선발대를 직접 빼가지는 않게 했다.

공식 고유 맵 100개(seed 42), LEFT, 고정 pool 19모델, worker 16의
1,900판 결과는 다음과 같다.

| 판 | 승 | 무 | 패 | 승점 | 승점률 |
|---|---:|---:|---:|---:|---:|
| 현재 최고판 | **1437** | 419 | **44** | **1646.5** | **86.6579%** |
| 탈환ㆍ신규 확보 병행 | 1425 | 428 | 47 | 1639 | 86.2632% |

전체 승점은 **-7.5점**, 패배는 **+3판**이다. `87838` +1.5점,
`109203` +1점으로 좋아졌지만 `91531` -2.5점, `84206` -2점 및 여러
모델에서 각 -1점가량 악화됐다. 동시 확장 자체는 실행되지만 신규 빌더
생산과 건설비 예약이 필수 탈환의 훈련ㆍ이동 예산을 먼저 잠그는 비용이
더 컸으므로 **비채택**했다. 코드는 실험 플래그와 별도 wrapper
`110584_highest_parallel_expansion_during_mandatory.cpp`로 남겼고, 결과는
`highest_parallel_expansion_mandatory_vs_fixed_pool_100maps.csv`다.

### 빈 필수 거점이 남으면 강제 탈환 발동 연기 (2026-08-28)

`DEFER_MANDATORY_RECLAIM_WHILE_NEUTRAL_MANDATORY`는 내 필수 권역 중
건물이 없고 상대 상주 병력도 없는 거점이 하나라도 있으면 새로운 강제
필수 탈환을 발동하지 않는다. 이미 집결ㆍ출격 중인 탈환은 취소하지 않고,
빈 필수 거점이 모두 사라진 뒤부터 기존 탈환 발동을 다시 허용한다.

공식 고유 맵 100개(seed 42), LEFT, 고정 pool 19모델, worker 16의
1,900판 결과는 다음과 같다.

| 판 | 승 | 무 | 패 | 승점 | 승점률 |
|---|---:|---:|---:|---:|---:|
| 현재 최고판 | **1437** | **419** | **44** | **1646.5** | **86.6579%** |
| 빈 필수 거점 우선 | 1167 | 383 | 350 | 1358.5 | 71.5000% |

총 승점 **-288점**, 패배 **+306판**으로 크게 악화됐다. 빌더가 이미
이동 중이거나 건설비를 기다리는 거점도 건물이 완공될 때까지 계속 빈
거점으로 계산되므로, 그 긴 시간 동안 상대가 점유한 필수 거점의 탈환이
통째로 봉쇄된 것이 원인이다. 따라서 이 조건은 **비채택**했으며 결과는
`highest_defer_mandatory_for_neutral_vs_fixed_pool_100maps.csv`다.

### 필수 탈환 발동을 상대 BASE 3개 이하로 제한 (2026-08-28)

사용자 정정에 따라 빈 필수 거점 존재 여부는 조건에서 완전히 제거했다.
`FORCED_MANDATORY_RECLAIM_OPP_BASE_MAX=3`은 기존 강제 필수 탈환 발동
조건에 `상대가 소유한 BASE 수 <= 3`을 AND로 추가한다. HQ는 개수에서
제외하며, 이미 시작한 작전은 상대의 네 번째 BASE가 완공돼도 중간 취소하지
않는다.

공식 고유 맵 100개(seed 42), LEFT, 고정 pool 19모델, worker 16의
1,900판 결과는 다음과 같다.

| 판 | 승 | 무 | 패 | 승점 | 승점률 |
|---|---:|---:|---:|---:|---:|
| 현재 최고판 | **1437** | 419 | 44 | **1646.5** | **86.6579%** |
| 상대 BASE 3개 이하에서만 탈환 | 1279 | **585** | **36** | 1571.5 | 82.7105% |

패배는 8판 줄었지만 승리가 158판 줄고 무승부가 166판 늘어 총 승점은
**-75점**이다. 후반 필수 탈환을 막으면 패배를 회피하는 대신 승리로
이어지는 영토 공세도 사라지는 결과라 **비채택**했다. 결과는
`highest_mandatory_only_oppbase3_vs_fixed_pool_100maps.csv`다.

### 상대 BASE 3개 제한판의 일반 공세 계획 마진 +1 (2026-08-28)

직전 `FORCED_MANDATORY_RECLAIM_OPP_BASE_MAX=3` 실험판 위에서 일반 공세
계획 단계의 `ATTACK_SAFETY_MARGIN`만 기본 +5명에서 +1명으로 낮췄다.
실제 출격 직전 정밀 시뮬레이션, 증원 생존 마진 0 및 필수 탈환의 +1/+2
마진은 변경하지 않았다.

공식 고유 맵 100개(seed 42), LEFT, 고정 pool 19모델, worker 16의
1,900판 결과는 다음과 같다.

| 판 | 승 | 무 | 패 | 승점 | 승점률 |
|---|---:|---:|---:|---:|---:|
| 현재 최고판 | **1437** | 419 | 44 | **1646.5** | **86.6579%** |
| 상대 BASE<=3, 계획 마진 +5 | 1279 | 585 | 36 | 1571.5 | 82.7105% |
| 상대 BASE<=3, 계획 마진 +1 | 1328 | 546 | **26** | 1601 | 84.2632% |

같은 제한판 안에서는 마진 축소로 승리 +49판, 무승부 -39판, 패배 -10판,
승점 **+29.5점**으로 크게 개선됐다. 그러나 현재 최고판보다는 승점이
45.5점 낮아 최고판에는 **비채택**했다. 반면 패배는 최고판보다 18판
적어 저패배 성향의 대조 후보로 보존한다. 결과는
`highest_mandatory_only_oppbase3_attack_margin1_vs_fixed_pool_100maps.csv`다.

### 현재 최고판의 일반 공세 계획 마진 제거 (2026-08-28)

상대 BASE 수에 따른 필수 탈환 제한은 넣지 않고, 현재 최고판에서 일반
공세 계획 단계의 `ATTACK_SAFETY_MARGIN`만 +5명에서 0명으로 제거했다.
실제 출격 직전 정밀 시뮬레이션과 필수 탈환 마진은 그대로 유지했다.

공식 고유 맵 100개(seed 42), LEFT, 고정 pool 19모델, worker 16의
1,900판 결과는 다음과 같다.

| 판 | 승 | 무 | 패 | 승점 | 승점률 |
|---|---:|---:|---:|---:|---:|
| 현재 최고판, 계획 마진 +5 | **1437** | **419** | **44** | **1646.5** | **86.6579%** |
| 계획 마진 0 | **1437** | 415 | 48 | 1644.5 | 86.5526% |

승리 수는 같지만 무승부 4판이 패배로 바뀌어 총 승점은 **-2점**이다.
`109203` +2.5점, `96816`/`91531`/`87838` 각 +1.5점 개선됐으나
`110584` -2.5점, `108830` -2점 등의 손해가 더 컸다. 근소하게
**비채택**하여 최고판은 계획 마진 +5를 유지한다. 결과는
`highest_attack_plan_margin0_vs_fixed_pool_100maps.csv`다.

### 현재 최고판의 일반 공세 계획 마진 0~5 전수 비교 (2026-08-28)

현재 최고판의 다른 조건을 전부 고정하고 일반 공세 계획 단계의
`ATTACK_SAFETY_MARGIN`만 0부터 5까지 비교했다. 실제 출격 직전 정밀
시뮬레이션, 필수 탈환 마진 및 증원 생존 마진은 바꾸지 않았다.

공식 고유 맵 100개(seed 42), LEFT, 고정 pool 19모델, 총 worker 16의
각 1,900판 결과는 다음과 같다.

| 계획 마진 | 승 | 무 | 패 | 승점 | 승점률 |
|---:|---:|---:|---:|---:|---:|
| 0 | 1437 | 415 | 48 | 1644.5 | 86.5526% |
| 1 | 1446 | 408 | 46 | 1650 | 86.8421% |
| 2 | 1428 | 427 | 45 | 1641.5 | 86.3947% |
| **3** | **1462** | 395 | **43** | **1659.5** | **87.3421%** |
| 4 | 1426 | 431 | **43** | 1641.5 | 86.3947% |
| 기존 5 | 1437 | **419** | 44 | 1646.5 | 86.6579% |

마진 3은 기존 마진 5보다 승리 +25판, 무승부 -24판, 패배 -1판으로
총 승점이 **+13점** 올랐다. `83304` +3.5점, `109203` +3점,
`110193`/`96816` 각 +2점 등이 개선됐으며 `93978` -3점,
`91531` -1.5점이 주요 악화다. 110584 상대도 74승 25무 1패에서
75승 24무 1패로 +0.5점이다. 전체 승점과 패배 수가 모두 개선되어
계획 마진 **3을 새 최고판에 채택**했다. 전체 결과는
`highest_attack_plan_margin{0,1,2,3,4}_vs_fixed_pool_100maps.csv`와
기존 마진 5의 `adaptive_best_highest_won_empty_hold_vs_fixed_pool_100maps.csv`다.

### 필수 탈환 마진을 기본 0ㆍBASE 우위 1로 축소 (2026-08-28)

새 최고판인 일반 공세 계획 마진 3을 기준으로 필수 탈환 마진을 각각 한
명씩 낮췄다. `MANDATORY_RECLAIM_INITIAL_MARGIN`은 +1에서 0으로,
내 BASE 수가 더 많을 때 적용하는 `MANDATORY_RECLAIM_AHEAD_MARGIN`은
+2에서 +1로 바꿨다. 상대도 추가 병력을 생산할 수 있으면 조건부 한 명을
기다리지 않는 기존 시간 비교는 유지했다.

공식 고유 맵 100개(seed 42), LEFT, 고정 pool 19모델, worker 16의
1,900판 결과는 다음과 같다.

| 판 | 승 | 무 | 패 | 승점 | 승점률 |
|---|---:|---:|---:|---:|---:|
| 현재 최고: 필수 +1/+2 | **1462** | 395 | **43** | **1659.5** | **87.3421%** |
| 필수 0/+1 | 1060 | **528** | 312 | 1324 | 69.6842% |

승리 -402판, 무승부 +133판, 패배 +269판으로 총 승점이 **-335.5점**
급락했다. 기본 마진 0은 관측된 현재 병력을 상대로 시뮬레이션상 딱 이기는
최소 파동을 허용해, 상대 증원과 도착 시점 오차를 전혀 흡수하지 못한다.
따라서 **비채택**하고 최고판은 필수 탈환 기본 +1, BASE 우위 조건부 +2를
유지한다. 결과는
`highest_attack_margin3_mandatory_margin0_ahead1_vs_fixed_pool_100maps.csv`다.

### BASE 파괴 불가 시 양수 주둔군 교환 폴백 (2026-08-28)

63,000칸 전투표 원본에는 BASE 파괴에 실패해도 즉시 골드 순가치가 양수인
조합이 80개 있다. 압축 표는 실패를 모두 `0xffff`로 저장해 이 정보를
잃으므로, 실제 출격 직전의 현재 HP 기반 `simulate_isolated_base_combat`이
상대 생존자와 남은 건물 HP도 반환하도록 확장했다.

`PROFITABLE_FAILED_GARRISON_TRADE`의 우선순위와 조건은 다음과 같다.

1. 정밀 시뮬레이션으로 파괴ㆍ점령 가능한 공세가 있으면 항상 기존 공세를
   선택한다.
2. BASE를 파괴하고 아군이 생존하는 양수 수익 RAID가 있으면 이를 선택한다.
3. 위 두 공격이 모두 없을 때만, BASE는 살아남지만 공격군 전멸로 전투가
   실제 종료되고 `상대 병력 손실×120 - 아군 병력 손실×120 - 이동비`가
   양수인 공격을 폴백으로 허용한다. 파괴 실패이므로 건물 투자비와 수입
   차단 가치는 0이다.
4. 외부 증원 도착으로 시뮬레이션 창만 끝났고 공격군이 남은 경우는 확정
   교환이 아니므로 제외한다. 실패 교환군이 전멸한 뒤에는 같은 BASE에
   두 번째 파동을 자동 재시도하지 않고 작전을 종료한다.

공식 고유 맵 100개(seed 42), LEFT, 고정 pool 19모델, worker 16의
1,900판 결과는 기존 최고와 행 단위까지 완전히 동일한 **1462승 395무
43패, 1659.5점(87.3421%)**이었다. 고정 풀에서 승패를 바꾼 사례가 전혀
없어 복잡도만 늘리고 실질적인 효용은 확인되지 않았다. 따라서 최고판에는
**비채택**하고 구현은 기본 비활성 실험 플래그로만 남겼다. 결과는
`highest_attack_margin3_profitable_failed_trade_vs_fixed_pool_100maps.csv`다.

### 첫 필수 탈환 발동 시점의 거점 확보 전용 반사실 분석 (2026-08-28)

현재 최고판에서 최초 `RECLAIM_MANDATORY_TRIGGER`가 발생한 순간에 필수
탈환을 하지 않았다면 BASE 수가 실제로 밀렸을지를 진단했다. 공식 고유 맵
100개(seed 42), LEFT, 고정 pool 19모델, worker 16의 1,900판을 다시
실행했으며 실제 결과는 **1462승 395무 43패**로 기존 최고 성적과 정확히
일치했다.

반사실 시뮬레이션은 발동 턴 시작 직전의 실제 골드, HQ/BASE 레벨, 병력
HPㆍ위치 및 중립 거점으로 이미 이동 중인 건설병을 복원한다. 이후에는
양쪽에 같은 결정 규칙을 적용해 중립 거점 건설만 허용했다. 공격, 방어
증원, 전투, 건물 업그레이드ㆍ수리는 금지하고, 실제 이동 턴 수와 골드,
현재 노동 수입ㆍ유지비, HQ 훈련 비용을 반영해 더 빨리 완공 가능한 쪽이
한 명의 건설병을 보낸다. 완공 예상 턴이 같으면 어느 쪽도 소비하지 않고
그 거점을 미점령으로 남겼다. 따라서 이 값은 모든 가능한 확보 전략의
최적해가 아니라, 양쪽을 동일하게 통제한 결정론적 진단 기준이다.

최초 발동은 1,900판 중 **1,556판**에서 발생했다. 그 시점부터 확보전만
했을 때 최종 BASE 수 관계는 다음과 같다(HQ 제외).

| 확보전만 한 최종 관계 | 판 | 최초 발동판 대비 |
|---|---:|---:|
| 내 BASE 열세 | **251** | **16.13%** |
| 동률 | **1,058** | **67.99%** |
| 내 BASE 우세 | **247** | **15.87%** |

즉 최초 필수 탈환이 켜진 판의 **83.87%는 공격ㆍ방어를 모두 빼도 BASE가
밀리지 않았다**. 필수 탈환 발동 자체를 곧바로 “확보전만 하면 반드시
열세가 된다”는 신호로 해석하면 과잉 대응이다. 반대로 251판은 실제로
확보전만으로도 열세가 되므로 탈환 필요성이 분명한 집단이다.

최초 발동 목표의 작전 진행은 다음과 같았다.

| 최초 작전 결과 | 판 |
|---|---:|
| 출격 후 목표 확보 | **876** |
| 출격 후 정밀 재판정에서 목표 잠금 해제 | **106** |
| 출격했으나 경기 종료까지 미확보 | **3** |
| 출격 없이 다른 병력으로 확보 | 432 |
| 출격 전 목표 잠금 해제 | 139 |

첫 출격 뒤 성공/해제 판정이 난 982판만 보면 성공률은 **89.21%**다. 최초
작전에서는 `RECLAIM_MANDATORY_WAVE_LOST`로 직접 끝난 사례는 없었고,
실패 106판은 출격 뒤 재검사에서 목표 잠금이 풀린 경우다. 성공군의 최초
출격은 평균 **4.39명(중앙값 5명)**, 실패ㆍ해제군은 평균 **3.02명
(중앙값 3명)**이었다. 최초 발동 턴도 성공군 평균 T19.80, 실패군 평균
T9.02로, 실패는 이른 시기의 작은 출격에 집중됐다.

확보전 관계별로 미결 3판을 제외한 첫 출격 성공률은 열세 **97.30%**
(216/222), 동률 **84.86%**(471/555), 우세 **92.20%**(189/205)였다.
따라서 단순히 “확보전에서 밀리는가”만으로 첫 공격 성공 여부를 예측할
수는 없다. 이 반사실은 필수 탈환을 아예 삭제하기보다는, 최초 발동 시
`확보전 열세 여부`와 `출격 필요 인원ㆍ시점`을 별도 게이트로 삼을 근거에
가깝다.

전체 행별 결과와 요약은
`first_mandatory_capture_only_highest_pool_100maps.csv` 및 `.json`, 성공
3판ㆍ실패 3판의 실제 리플레이와 디버그 로그는
`first_mandatory_first_attack_outcome_replays_6/`에 저장했다.

#### 확보전 반사실 결과별 최초 탈환 성공률

`발동 목표 확보율`은 출격 없이 다른 병력이 먼저 확보한 경우도 성공으로
포함한다. `출격 성공률`은 실제로 첫 필수 탈환 병력을 보낸 판만 분모로
삼고, 출격 뒤 해제 및 경기 종료까지 미확보를 실패로 계산한다.

| 확보전 반사실 | 최초 발동 | 발동 목표 확보율 | 실제 출격 | 출격 성공률 | 실제 경기 승/무/패 |
|---|---:|---:|---:|---:|---:|
| 내 BASE 열세 | 251 | 222/251 = **88.45%** | 224 | 216/224 = **96.43%** | 187/56/8 |
| 동률 | 1,058 | 863/1,058 = **81.57%** | 556 | 471/556 = **84.71%** | 751/276/31 |
| 내 BASE 우세 | 247 | 223/247 = **90.28%** | 205 | 189/205 = **92.20%** | 215/28/4 |

특히 확보전 반사실 열세군에서 첫 출격이 성공한 216판의 실제 승점률은
**86.57%**(162승 50무 4패)였지만, 출격 뒤 실패ㆍ해제ㆍ미확보 8판은
**50.00%**(3승 2무 3패)였다. 표본이 8판으로 작으므로 인과효과로 단정할
수는 없지만, 확보전 열세로 판정된 경우에는 첫 탈환 성공 여부가 실제
패배와 강하게 연결되는 위험 신호다.

### 필수 탈환의 증원 전 BASE 파괴 후 이탈 실험 (2026-08-28)

필수 탈환에도 고정 +1/+2 마진 대신 단기 `치고 빠지기`를 선택할 수 있는
실험 분기를 추가했다. 현재 HP 기준 고립 전투를 돌려, 상대의 가장 빠른
증원보다 먼저 BASE를 파괴하고 아군이 한 명 이상 생존하며 다음 즉시 가치가
양수일 때만 최소 파동을 출격시키는 방식이다.

`상대 주둔군 손실×120 + 상대 BASE 누적 건설비 - 아군 손실×120 - 이동비`

집결지의 `work_cap` 노동자 때문에 필요한 파동이 한 명 모자란 경우에는
이 조건에서만 노동자 한 명까지 일시 동원한다. 다만 현재 이동 중인 병력만
증원으로 보면, 출격을 본 상대가 HQ에서 즉시 신병을 생산해 대응하는 경우를
놓친다. 따라서 상대 현재 골드와 순수입으로 가능한 가장 빠른 HQ 생산 및
최단 이동 ETA도 파괴 데드라인에 포함했다.

83284 / Pretest #28 / map 1558 / battle 233427의 T46에서는 R27의 3명을
보내면 외부 반응이 없을 때 T53 전후 R49를 파괴할 수 있다. 그러나 실제
재대전에서는 상대가 반응 신병을 보내 BASE가 3HP 남은 순간 합류했다. 보정
후 계산도 내 도착 4턴 + 공성 4턴 - 1 = 7턴, 상대 최단 반응 6턴이므로 이
공격을 `FALLBACK` 처리하며 기존 최고판과 같은 T53 6명 출격으로 돌아간다.

공식 고유 맵 100개(seed 42), LEFT, 고정 pool 19모델, worker 16의
1,900판 결과는 다음과 같다.

| 판 | 승 | 무 | 패 | 승점 | 승점률 |
|---|---:|---:|---:|---:|---:|
| 현재 최고판 | **1462** | 395 | **43** | **1659.5** | **87.3421%** |
| 필수 탈환 BASE 치고 빠지기 | 1390 | **448** | 62 | 1614 | 84.9474% |

승리 -72판, 무승부 +53판, 패배 +19판으로 승점이 **-45.5점** 하락했다.
일부 판에서 단기 파괴 자체는 성립해도 기존 필수 탈환의 목표 잠금ㆍ연속 증원
상태와 결합하면서 전체 작전 흐름을 크게 바꾸는 것이 주된 문제다. 따라서
최고판에는 **비채택**하고, 실험 wrapper와 기본 비활성 매크로만 후속 작업용으로
보존한다. 결과는
`highest_mandatory_pre_reinforcement_base_race_vs_fixed_pool_100maps.csv`,
문제 맵의 최고판/수정판 리플레이와 디버그 로그는
`mandatory_base_race_vs_83284_pretest28_map1558_battle233427/`에 있다.
